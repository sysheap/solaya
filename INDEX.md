# Codebase Index: webserver

> Generated: 2026-04-01 12:48:24 UTC | Files: 291 | Lines: 48116
> Languages: C (2), HTML (1), Markdown (21), Python (4), Rust (249), Shell (1), TOML (13)

## Directory Structure

```
webserver/
  CLAUDE.md
  Cargo.toml
  INDEX.md
  README.md
  arch/
    Cargo.toml
    src/
      lib.rs
      riscv64/
        backtrace.rs
        cpu.rs
        linker_symbols.rs
        mod.rs
        sbi/
          extensions/
            base_extension.rs
            hart_state_extension.rs
            ipi_extension.rs
            mod.rs
            timer_extension.rs
          mod.rs
          sbi_call.rs
        timer.rs
        trap_cause.rs
      stub/
        backtrace.rs
        cpu.rs
        linker_symbols.rs
        mod.rs
        sbi.rs
        timer.rs
        trap_cause.rs
  boot/
    Cargo.toml
    build.rs
    src/
      main.rs
  common/
    Cargo.toml
    src/
      errors.rs
      ioctl.rs
      lib.rs
      macros.rs
      numbers.rs
      pid.rs
      pointer.rs
      syscalls/
        mod.rs
        trap_frame.rs
  doc/
    ai/
      ARCHITECTURE.md
      BUILD.md
      DEBUGGING.md
      DRIVERS.md
      FS.md
      INTERRUPTS.md
      MEMORY.md
      NETWORKING.md
      OVERVIEW.md
      PROCESSES.md
      SYSCALLS.md
      TESTING.md
  gdb_mcp_server/
    __init__.py
    __main__.py
    gdb_session.py
    server.py
  headers/
    Cargo.toml
    build.rs
    src/
      lib.rs
  kernel/
    Cargo.toml
    build.rs
    src/
      asm/
        mod.rs
      assert.rs
      autogenerated/
        mod.rs
      cpu.rs
      debugging/
        backtrace.rs
        eh_frame_parser.rs
        mod.rs
        symbols.rs
        unwinder.rs
      device_tree.rs
      drivers/
        bochs_display.rs
        mod.rs
        virtio/
          block.rs
          capability.rs
          input.rs
          mod.rs
          net/
            mod.rs
          rng.rs
          virtqueue.rs
      fs/
        devfs.rs
        ext2/
          dir.rs
          file.rs
          inode.rs
          mod.rs
          structures.rs
        mod.rs
        open_file.rs
        procfs.rs
        tmpfs.rs
        vfs.rs
      interrupts/
        mod.rs
        plic.rs
        trap.rs
      io/
        mod.rs
        pipe.rs
        tty_device.rs
        uart.rs
      klibc/
        big_endian.rs
        btreemap.rs
        consumable_buffer.rs
        elf.rs
        leb128.rs
        mmio.rs
        mod.rs
        non_empty_vec.rs
        util.rs
        writable_buffer.rs
      lib.rs
      logging/
        configuration.rs
        mod.rs
      memory/
        heap.rs
        linker_information.rs
        mod.rs
        page_table_entry.rs
        page_tables.rs
        runtime_mappings.rs
      net/
        arp.rs
        checksum.rs
        ethernet.rs
        ipv4.rs
        mac.rs
        mod.rs
        parsed_packets.rs
        sockets.rs
        tcp.rs
        tcp_connection.rs
        udp.rs
      panic.rs
      pci/
        address.rs
        allocator.rs
        devic_tree_parser.rs
        lookup.rs
        mod.rs
      processes/
        brk.rs
        credentials.rs
        fd_table.rs
        futex.rs
        kernel_tasks.rs
        loader.rs
        mod.rs
        process.rs
        process_table.rs
        scheduler.rs
        signal.rs
        task.rs
        thread.rs
        timer.rs
        userspace_ptr.rs
        wait_child.rs
        waker.rs
      syscalls/
        exec_ops.rs
        fs_ops.rs
        helpers.rs
        id_ops.rs
        io_ops.rs
        ioctl_ops.rs
        linux.rs
        linux_validator.rs
        macros.rs
        mm_ops.rs
        mod.rs
        net_ops.rs
        process_ops.rs
        signal_ops.rs
        sysinfo_ops.rs
        time_ops.rs
        trace_config.rs
        tracer.rs
      test/
        array_vec.rs
        leb128.rs
        mod.rs
        qemu_exit.rs
  mcp-server/
    Cargo.toml
    src/
      main.rs
      server.rs
  plans/
    00-overview.md
    01-roadmap.md
    02-testing-strategy.md
    03-driver-framework.md
    04-build-system.md
    05-organization.md
  qemu-infra/
    Cargo.toml
    src/
      lib.rs
      ppm.rs
      qemu.rs
      qmp.rs
      read_asserter.rs
      searchable_buffer.rs
  qemu_wrapper.sh
  rustfmt.toml
  sys/
    Cargo.toml
    build.rs
    src/
      asm/
        mod.rs
      cpu.rs
      io/
        mod.rs
        uart.rs
      klibc/
        array_vec.rs
        deconstructed_vec.rs
        mmio.rs
        mod.rs
        runtime_initialized.rs
        send_sync.rs
        sizes.rs
        spinlock.rs
        util.rs
        validated_ptr.rs
      lib.rs
      logging/
        configuration.rs
        mod.rs
      memory/
        address.rs
        heap.rs
        mod.rs
        page.rs
        page_allocator.rs
        page_table.rs
      panic_support.rs
  system-tests/
    Cargo.toml
    src/
      infra/
        mod.rs
      lib.rs
      tests/
        basics.rs
        block.rs
        clocktest.rs
        connect4.rs
        coreutils.rs
        doom.rs
        ext2.rs
        fork.rs
        framebuffer.rs
        job_control.rs
        mod.rs
        mutex.rs
        net.rs
        panic.rs
        process_relation.rs
        rng.rs
        shell.rs
        signals.rs
        sleep.rs
        stress.rs
        syscall_tracer.rs
        sysinfo.rs
        vfs.rs
  typos.toml
  userspace/
    Cargo.toml
    doom/
      dg_solaya.c
      i_video_solaya.c
    src/
      bin/
        bad-syscall.rs
        blktest.rs
        clocktest.rs
        connect4/
          game_board.rs
          main.rs
        cow-test.rs
        cred-test.rs
        devfs_test.rs
        dhcpd.rs
        ext2test.rs
        fbbench.rs
        fbtest.rs
        fmeta-test.rs
        fork-test.rs
        getppid.rs
        init.rs
        kbtest.rs
        loop.rs
        ls-test.rs
        metadata-test.rs
        mutex_test.rs
        panic.rs
        pgid-child.rs
        pgid-test.rs
        pread-test.rs
        prog1.rs
        prog2.rs
        rlimit-test.rs
        rng_test.rs
        sendfile-test.rs
        sigtest.rs
        sleep.rs
        stress-process.rs
        stress-thread.rs
        symlink-test.rs
        sysinfo_test.rs
        tcp_echo.rs
        thread-test.rs
        udp.rs
        vfs-test.rs
        webserver.rs
      lib.rs
      spawn.rs
      util.rs
    static/
      index.html
```

---

## Public API Surface

**CLAUDE.md**
- `# Solaya - AI Agent Reference`
- `# Run all system tests`
- `# Run specific test`

**Cargo.toml**
- `[workspace]`
- `[workspace.package]`
- `[profile.release]`
- `[profile.dev]`

**INDEX.md**
- `# Codebase Index: webserver`

**README.md**
- `# Solaya`
- `# Install nix`
- `# Enable nix-command and flakes`
- `# Restart nix daemon`
- `# Install direnv`
- `# Add direnv hook to your shell (see https://direnv.net/docs/hook.html for non-bash shells)`
- `# In the Solaya repository`
- `# Re-enter the directory — nix will pull all dependencies automatically`

**arch/Cargo.toml**
- `[package]`
- `[dependencies]`
- `[lib]`

**arch/src/lib.rs**
- `pub struct CpuId(usize)`

**arch/src/riscv64/backtrace.rs**
- `pub struct CalleeSavedRegs`

**arch/src/riscv64/cpu.rs**
- `pub unsafe fn write_satp_and_fence(satp_val: usize)`
- `pub fn memory_fence()`
- `pub unsafe fn disable_global_interrupts()`
- `pub fn wait_for_interrupt()`
- `pub fn is_timer_enabled() -> bool`
- `pub fn enable_timer_interrupt()`
- `pub fn clear_supervisor_software_interrupt()`
- `pub fn is_in_kernel_mode() -> bool`
- `pub fn set_ret_to_kernel_mode(kernel_mode: bool)`
- `pub fn trigger_supervisor_software_interrupt()`
- `pub extern "C" fn wfi_loop() -> !`
- `pub fn asm_panic_rust()`
- `pub struct InterruptGuard`

**arch/src/riscv64/linker_symbols.rs**
- `pub fn asm_handle_trap_addr() -> usize`
- `pub fn start_hart_addr() -> usize`
- `pub fn signal_trampoline_addr() -> usize`
- `pub fn powersave_fn_addr() -> usize`

**arch/src/riscv64/mod.rs**
- `pub mod backtrace`
- `pub mod cpu`
- `pub mod linker_symbols`
- `pub mod sbi`
- `pub mod timer`
- `pub mod trap_cause`

**arch/src/riscv64/sbi/extensions/base_extension.rs**
- `pub struct SbiSpecVersion`
- `pub fn sbi_get_spec_version() -> SbiSpecVersion`

**arch/src/riscv64/sbi/extensions/hart_state_extension.rs**
- `pub fn get_number_of_harts() -> usize`
- `pub fn start_hart(hart_id: usize, start_addr: usize, opaque: usize) -> SbiRet`

**arch/src/riscv64/sbi/extensions/ipi_extension.rs**
- `pub fn sbi_send_ipi(hart_mask: u64, hart_mask_base: i64) -> SbiRet`

**arch/src/riscv64/sbi/extensions/mod.rs**
- `pub mod base_extension`
- `pub mod hart_state_extension`
- `pub mod ipi_extension`
- `pub mod timer_extension`

**arch/src/riscv64/sbi/extensions/timer_extension.rs**
- `pub fn sbi_set_timer(stime_value: u64) -> SbiRet`

**arch/src/riscv64/sbi/mod.rs**
- `pub mod extensions`

**arch/src/riscv64/sbi/sbi_call.rs**
- `pub enum SbiError`
- `pub struct SbiRet`
- `pub fn sbi_call(eid: u64, fid: u64, arg0: u64, arg1: u64, arg2: u64) -> SbiRet`

**arch/src/riscv64/timer.rs**
- `pub const CLINT_BASE: usize = 0x2000000`
- `pub const CLINT_SIZE: usize = 0x10000`
- `pub fn get_current_clocks() -> u64`

**arch/src/riscv64/trap_cause.rs**
- `pub mod interrupt`
- `pub mod exception`
- `pub struct InterruptCause(usize)`

**arch/src/stub/backtrace.rs**
- `pub struct CalleeSavedRegs`

**arch/src/stub/cpu.rs**
- `pub unsafe fn write_satp_and_fence(_satp_val: usize)`
- `pub fn memory_fence()`
- `pub unsafe fn disable_global_interrupts()`
- `pub fn wait_for_interrupt()`
- `pub fn is_timer_enabled() -> bool`
- `pub fn enable_timer_interrupt()`
- `pub fn clear_supervisor_software_interrupt()`
- `pub fn is_in_kernel_mode() -> bool`
- `pub fn set_ret_to_kernel_mode(_kernel_mode: bool)`
- `pub fn trigger_supervisor_software_interrupt()`
- `pub extern "C" fn wfi_loop() -> !`
- `pub fn asm_panic_rust()`
- `pub struct InterruptGuard`

**arch/src/stub/linker_symbols.rs**
- `pub fn asm_handle_trap_addr() -> usize`
- `pub fn start_hart_addr() -> usize`
- `pub fn signal_trampoline_addr() -> usize`
- `pub fn powersave_fn_addr() -> usize`

**arch/src/stub/mod.rs**
- `pub mod backtrace`
- `pub mod cpu`
- `pub mod linker_symbols`
- `pub mod sbi`
- `pub mod timer`
- `pub mod trap_cause`

**arch/src/stub/sbi.rs**
- `pub mod sbi_call`
- `pub mod extensions`

**arch/src/stub/timer.rs**
- `pub const CLINT_BASE: usize = 0x2000000`
- `pub const CLINT_SIZE: usize = 0x10000`
- `pub fn get_current_clocks() -> u64`

**arch/src/stub/trap_cause.rs**
- `pub mod interrupt`
- `pub mod exception`
- `pub struct InterruptCause(usize)`

**boot/Cargo.toml**
- `[package]`
- `[dependencies]`

**common/Cargo.toml**
- `[package]`
- `[lib]`

**common/src/errors.rs**
- `pub enum LoaderError`

**common/src/ioctl.rs**
- `pub const SOLAYA_PANIC: u32 = 0x5301`
- `pub const SOLAYA_LIST_PROGRAMS: u32 = 0x5302`
- `pub const SIOCGIFHWADDR: u32 = 0x8927`
- `pub const SIOCSIFADDR: u32 = 0x8916`
- `pub const ARPHRD_ETHER: u16 = 1`
- `pub struct Ifreq`
- `pub fn trigger_kernel_panic()`
- `pub fn print_programs()`
- `pub fn get_mac_address(socket_fd: i32) -> Option<[u8; 6]>`
- `pub fn set_ip_address(socket_fd: i32, ip: [u8; 4])`

**common/src/lib.rs**
- `pub mod errors`
- `pub mod ioctl`
- `pub mod macros`
- `pub mod numbers`
- `pub mod pid`
- `pub mod pointer`
- `pub mod syscalls`

**common/src/macros.rs**
- `pub struct AlignedAs<Align, Bytes: ?Sized>`

**common/src/numbers.rs**
- `pub trait Number: Debug + Display + Copy + Clone`

**common/src/pid.rs**
- `pub struct Tid(u64)`

**common/src/pointer.rs**
- `pub trait Pointer: Clone + Copy + core::fmt::Pointer`

**common/src/syscalls/mod.rs**
- `pub mod trap_frame`

**common/src/syscalls/trap_frame.rs**
- `pub struct TrapFrame`
- `pub enum Register`

**doc/ai/ARCHITECTURE.md**
- `# Architecture`

**doc/ai/BUILD.md**
- `# Build System`
- `# Extract symbols`
- `# Embed in binary`

**doc/ai/DEBUGGING.md**
- `# Debugging`
- `# Set breakpoint`
- `# Continue execution`
- `# Step instruction`
- `# Print registers`
- `# Print memory`
- `# Print backtrace`
- `# Output: function_name at file.rs:123`
- `# Pipe to less for navigation`
- `# Or save to file`
- `# With MCP inspector`
- `# Direct run`

**doc/ai/DRIVERS.md**
- `# Device Drivers`

**doc/ai/FS.md**
- `# Filesystem (VFS)`

**doc/ai/INTERRUPTS.md**
- `# Interrupt Handling`

**doc/ai/MEMORY.md**
- `# Memory Management`

**doc/ai/NETWORKING.md**
- `# Networking`

**doc/ai/OVERVIEW.md**
- `# Solaya AI Documentation Index`

**doc/ai/PROCESSES.md**
- `# Process Management`

**doc/ai/SYSCALLS.md**
- `# Syscall Handling`

**doc/ai/TESTING.md**
- `# Testing`
- `# Run specific system test`
- `# Loop test until failure (for flaky tests)`
- `# or`

**gdb_mcp_server/gdb_session.py**
- `DEFAULT_KERNEL_PATH = "target/riscv64gc-unknown-none-elf/release/boot"`
- `GDB_PORT_FILE = ".gdb-port"`
- `def find_project_root() -> Path | None`
- `class GDBSession`

**gdb_mcp_server/server.py**
- `mcp = FastMCP("gdb")`
- `session = GDBSession()`
- `class ToolTimeout(Exception)`
- `@mcp.tool() def gdb_connect(
    port: int | None = None,
    kernel_path: str = DEFAULT_KERNEL_PATH,
    gdb_path: str = "gdb",
) -> str`
- `@mcp.tool() def gdb_disconnect() -> str`
- `@mcp.tool() def gdb_backtrace(full: bool = False) -> str`
- `@mcp.tool() def gdb_breakpoint(location: str, hardware: bool = True) -> str`
- `@mcp.tool() def gdb_continue() -> str`
- `@mcp.tool() def gdb_step() -> str`
- `@mcp.tool() def gdb_next() -> str`
- `@mcp.tool() def gdb_print(expression: str) -> str`
- `@mcp.tool() def gdb_execute(command: str, timeout_sec: int = 30) -> str`
- `@mcp.tool() def gdb_registers() -> str`
- `@mcp.tool() def gdb_locals(frame: int | None = None) -> str`
- `@mcp.tool() def gdb_examine(address: str, count: int = 16, unit: str = "g", fmt: str = "x") -> str`
- `@mcp.tool() def gdb_breakpoint_list() -> str`
- `@mcp.tool() def gdb_breakpoint_delete(number: int) -> str`
- `@mcp.tool() def gdb_interrupt() -> str`
- `@mcp.tool() def gdb_finish() -> str`
- `@mcp.tool() def gdb_threads() -> str`
- `@mcp.tool() def gdb_select_thread(thread_id: int) -> str`
- `@mcp.tool() def gdb_frame(frame_number: int) -> str`
- `@mcp.tool() def gdb_diagnose() -> str`

**headers/Cargo.toml**
- `[package]`
- `[build-dependencies]`
- `[lib]`

**headers/src/lib.rs**
- `pub mod syscalls`
- `pub mod syscall_types`
- `pub mod errno`
- `pub mod socket`
- `pub mod fs`
- `pub mod sysinfo_types`

**kernel/Cargo.toml**
- `[package]`
- `[dependencies]`
- `[lib]`
- `[dev-dependencies]`
- `[target.'cfg(not(miri))'.dev-dependencies]`

**kernel/src/asm/mod.rs**
- `pub fn signal_trampoline_phys_addr() -> PhysAddr`

**kernel/src/assert.rs**
- `pub fn assert_unreachable() -> !`

**kernel/src/autogenerated/mod.rs**
- `pub mod userspace_programs`

**kernel/src/cpu.rs**
- `pub struct Cpu`

**kernel/src/debugging/backtrace.rs**
- `pub fn init()`
- `pub fn print()`

**kernel/src/debugging/eh_frame_parser.rs**
- `pub struct EhFrameParser<'a>`
- `pub struct ParsedCIE<'a>`
- `pub struct ParsedFDE<'a>`
- `pub enum Bitness`
- `pub struct EhFrameIterator<'a>`
- `pub enum Instruction`

**kernel/src/debugging/mod.rs**
- `pub mod backtrace`
- `pub mod eh_frame_parser`
- `pub mod symbols`
- `pub mod unwinder`
- `pub fn dump_current_state()`

**kernel/src/debugging/symbols.rs**
- `pub static THE: RuntimeInitializedData<&'static str> = RuntimeInitializedData::new()`
- `pub fn init()`
- `pub fn symbols_end() -> usize`
- `pub fn symbols_size() -> usize`
- `pub fn symbols_size() -> usize`
- `pub struct AddressAndSymbol`
- `pub fn get_symbol(target_address: usize) -> Option<AddressAndSymbol>`

**kernel/src/debugging/unwinder.rs**
- `pub struct Unwinder<'a>`
- `pub struct Row`
- `pub enum RegisterRule`

**kernel/src/device_tree.rs**
- `pub static THE: RuntimeInitializedData<DeviceTree> = RuntimeInitializedData::new()`
- `pub struct Header`
- `pub struct DeviceTree`
- `pub struct ReserveEntry`
- `pub enum FdtToken<'a>`
- `pub struct Node<'a>`
- `pub struct Reg`
- `pub fn get_devicetree_range() -> Range<*const u8>`
- `pub fn init(device_tree_pointer: *const ())`

**kernel/src/drivers/bochs_display.rs**
- `pub const FB_WIDTH: usize = 640`
- `pub const FB_HEIGHT: usize = 480`
- `pub const FB_BPP: usize = 32`
- `pub const FB_STRIDE: usize = FB_WIDTH * (FB_BPP / 8)`
- `pub const FB_SIZE: usize = FB_STRIDE * FB_HEIGHT`
- `pub fn fb_base() -> Option<PciCpuAddr>`
- `pub fn is_bochs_display(device: &PCIDevice) -> bool`
- `pub fn initialize(mut pci_device: PCIDevice)`
- `pub fn register_devfs_node()`

**kernel/src/drivers/mod.rs**
- `pub mod bochs_display`
- `pub mod virtio`
- `pub fn init_all_pci_devices(mut pci_devices: Vec<PCIDevice>)`

**kernel/src/drivers/virtio/block.rs**
- `pub struct BlockDevice`
- `pub struct InitializedBlockDevice`
- `pub fn register_isr_status(isr: MMIO<u32>)`
- `pub fn on_block_interrupt()`
- `pub fn assign_block_device(device: BlockDevice) -> usize`
- `pub fn device_count() -> usize`
- `pub fn capacity(index: usize) -> u64`
- `pub fn register_devfs_node(index: usize)`
- `pub async fn read(index: usize, offset: usize, buf: &mut [u8]) -> Result<usize, Errno>`
- `pub async fn write(index: usize, offset: usize, data: &[u8]) -> Result<usize, Errno>`

**kernel/src/drivers/virtio/capability.rs**
- `pub const VIRTIO_VENDOR_SPECIFIC_CAPABILITY_ID: u8 = 0x9`
- `pub const VIRTIO_VENDOR_ID: u16 = 0x1AF4`
- `pub const VIRTIO_DEVICE_ID: core::ops::RangeInclusive<u16> = 0x1000..=0x107F`
- `pub const DEVICE_STATUS_ACKNOWLEDGE: u8 = 1`
- `pub const DEVICE_STATUS_DRIVER: u8 = 2`
- `pub const DEVICE_STATUS_DRIVER_OK: u8 = 4`
- `pub const DEVICE_STATUS_FEATURES_OK: u8 = 8`
- `pub const DEVICE_STATUS_FAILED: u8 = 128`
- `pub const VIRTIO_F_VERSION_1: u64 = 1 << 32`
- `pub const VIRTIO_PCI_CAP_COMMON_CFG: u8 = 1`
- `pub const VIRTIO_PCI_CAP_NOTIFY_CFG: u8 = 2`
- `pub const VIRTIO_PCI_CAP_ISR_CFG: u8 = 3`
- `pub const VIRTIO_PCI_CAP_DEVICE_CFG: u8 = 4`
- `pub const VIRTIO_PCI_CAP_PCI_CFG: u8 = 5`
- `pub const VIRTIO_PCI_CAP_SHARED_MEMORY_CFG: u8 = 8`
- `pub const VIRTIO_PCI_CAP_VENDOR_CFG: u8 = 9`

**kernel/src/drivers/virtio/input.rs**
- `pub struct VirtioInputEvent`
- `pub struct InputDevice`
- `pub fn read_events(buf: &mut [u8]) -> usize`
- `pub fn on_input_interrupt()`
- `pub fn init_isr_status(isr: MMIO<u32>)`
- `pub fn set_device(device: InputDevice)`
- `pub fn register_devfs_node()`
- `pub struct InitializedInput`

**kernel/src/drivers/virtio/mod.rs**
- `pub mod block`
- `pub mod input`
- `pub mod net`
- `pub mod rng`

**kernel/src/drivers/virtio/net/mod.rs**
- `pub struct NetworkDevice`
- `pub struct InitializedNetworkDevice`

**kernel/src/drivers/virtio/rng.rs**
- `pub struct RngDevice`
- `pub fn set_device(device: RngDevice)`
- `pub fn read_random(buf: &mut [u8])`
- `pub fn is_available() -> bool`
- `pub fn register_devfs_node()`

**kernel/src/drivers/virtio/virtqueue.rs**
- `pub struct VirtQueue<const QUEUE_SIZE: usize>`
- `pub enum BufferDirection`
- `pub enum QueueError`
- `pub struct UsedBuffer`

**kernel/src/fs/devfs.rs**
- `pub(super) fn new() -> VfsNodeRef`
- `pub fn register_device(name: &str, node: VfsNodeRef)`
- `pub fn alloc_dev_ino() -> u64`

**kernel/src/fs/ext2/dir.rs**
- `pub struct Ext2Dir`

**kernel/src/fs/ext2/file.rs**
- `pub struct Ext2File`

**kernel/src/fs/ext2/inode.rs**
- `pub async fn read_inode( dev: usize, sb: &Ext2Superblock, bgds: &[Ext2BlockGroupDescriptor], inode_number: u32, ) -> Ext2Inode`
- `pub async fn read_inode_data(dev: usize, sb: &Ext2Superblock, inode: &Ext2Inode) -> Vec<u8>`

**kernel/src/fs/ext2/mod.rs**
- `pub mod structures`
- `pub async fn mount_ext2(dev: usize)`

**kernel/src/fs/ext2/structures.rs**
- `pub const EXT2_MAGIC: u16 = 0xEF53`
- `pub const EXT2_ROOT_INODE: u32 = 2`
- `pub const EXT2_FT_REG_FILE: u8 = 1`
- `pub const EXT2_FT_DIR: u8 = 2`
- `pub const S_IFMT: u16 = 0xF000`
- `pub const S_IFDIR: u16 = 0x4000`
- `pub const S_IFREG: u16 = 0x8000`
- `pub const EXT2_NDIR_BLOCKS: usize = 12`
- `pub const EXT2_IND_BLOCK: usize = 12`
- `pub const EXT2_DIND_BLOCK: usize = 13`
- `pub const EXT2_TIND_BLOCK: usize = 14`
- `pub struct Ext2Superblock`
- `pub struct Ext2BlockGroupDescriptor`
- `pub struct Ext2Inode`
- `pub struct Ext2DirEntry`

**kernel/src/fs/mod.rs**
- `pub mod devfs`
- `pub mod ext2`
- `pub mod open_file`
- `pub mod vfs`
- `pub fn init()`

**kernel/src/fs/open_file.rs**
- `pub struct VfsOpenFileInner`
- `pub type VfsOpenFile = Arc<Spinlock<VfsOpenFileInner>>`
- `pub fn open(node: VfsNodeRef, flags: i32) -> VfsOpenFile`

**kernel/src/fs/procfs.rs**
- `pub(super) fn new() -> Arc<StaticDir>`

**kernel/src/fs/tmpfs.rs**
- `pub struct TmpfsFile`
- `pub struct TmpfsDir`
- `pub struct TmpfsSymlink`

**kernel/src/fs/vfs.rs**
- `pub fn alloc_ino() -> u64`
- `pub enum NodeType`
- `pub fn stat_from_node(node: &VfsNodeRef) -> headers::fs::stat`
- `pub fn statx_from_node(node: &VfsNodeRef) -> headers::fs::statx`
- `pub struct DirEntry`
- `pub type VfsNodeRef = Arc<dyn VfsNode>`
- `pub trait VfsNode: Send + Sync`
- `pub fn mount(path: &str, root: VfsNodeRef)`
- `pub fn resolve_path(path: &str) -> Result<VfsNodeRef, Errno>`
- `pub fn resolve_path_nofollow(path: &str) -> Result<VfsNodeRef, Errno>`
- `pub fn resolve_parent(path: &str) -> Result<(VfsNodeRef, &str), Errno>`
- `pub fn resolve_relative(base: VfsNodeRef, path: &str) -> Result<VfsNodeRef, Errno>`
- `pub(super) struct StaticDir`
- `pub(super) struct RootDir`

**kernel/src/interrupts/mod.rs**
- `pub mod plic`
- `pub mod trap`

**kernel/src/interrupts/plic.rs**
- `pub const PLIC_BASE: usize = 0x0c00_0000`
- `pub const PLIC_SIZE: usize = 0x1000_0000`
- `pub struct Plic`
- `pub static PLIC: RuntimeInitializedData<Spinlock<Plic>> = RuntimeInitializedData::new()`
- `pub fn init_plic(cpu_id: CpuId)`
- `pub fn register_interrupt(irq: u32, handler: fn())`
- `pub fn dispatch_interrupt(irq: u32)`

**kernel/src/interrupts/trap.rs**
- `pub extern "C" fn get_process_satp_value() -> usize`
- `pub extern "C" fn handle_trap()`

**kernel/src/io/mod.rs**
- `pub mod pipe`
- `pub mod tty_device`
- `pub mod uart`
- `pub const TEST_DEVICE_ADDRESS: usize = 0x100000`

**kernel/src/io/pipe.rs**
- `pub type SharedPipeBuffer = Arc<Spinlock<PipeInner>>`
- `pub struct PipeInner`
- `pub struct PipeReader(SharedPipeBuffer)`
- `pub struct PipeWriter(SharedPipeBuffer)`
- `pub fn new_pipe() -> (PipeReader, PipeWriter)`
- `pub struct ReadPipe`

**kernel/src/io/tty_device.rs**
- `pub static CONSOLE_TTY: RuntimeInitializedData<TtyDevice> = RuntimeInitializedData::new()`
- `pub fn console_tty() -> &'static TtyDevice`
- `pub type TtyDevice = Arc<Spinlock<TtyDeviceInner>>`
- `pub struct InputResult`
- `pub enum InputAction`
- `pub struct TtyDeviceInner`
- `pub struct ReadTty`

**kernel/src/io/uart.rs**
- `pub const UART_BASE_ADDRESS: usize = 0x1000_0000`
- `pub static QEMU_UART: Spinlock<UnsafeSendSync<Uart>> = Spinlock::new(UnsafeSendSync(Uart::new(UART_BASE_ADDRESS)))`
- `pub struct Uart`
- `pub fn on_uart_interrupt()`

**kernel/src/klibc/big_endian.rs**
- `pub struct BigEndian<T: Number>(T)`

**kernel/src/klibc/btreemap.rs**
- `pub trait SplitOffLowerThan<K, V>`

**kernel/src/klibc/consumable_buffer.rs**
- `pub struct ConsumableBuffer<'a>`
- `pub trait FromU8Buffer: Copy`
- `pub trait FromU8BufferUnsized: Copy`

**kernel/src/klibc/elf.rs**
- `pub enum BitFormat`
- `pub enum Endianness`
- `pub enum OsAbi`
- `pub enum FileType`
- `pub enum Machine`
- `pub struct ElfHeader`
- `pub enum ProgramHeaderType`
- `pub enum ProgramHeaderFlags`
- `pub struct ElfProgramHeaderEntry`
- `pub enum ElfParseErrors`
- `pub struct ElfFile<'a>`

**kernel/src/klibc/leb128.rs**
- `pub struct UnsignedLEB128`
- `pub struct SignedLEB128`

**kernel/src/klibc/mod.rs**
- `pub mod big_endian`
- `pub mod btreemap`
- `pub mod consumable_buffer`
- `pub mod elf`
- `pub mod leb128`
- `pub mod mmio`
- `pub mod non_empty_vec`
- `pub mod util`
- `pub mod writable_buffer`

**kernel/src/klibc/non_empty_vec.rs**
- `pub struct NonEmptyVec<T>`

**kernel/src/klibc/util.rs**
- `pub trait UsizeExt`
- `pub fn wrapping_add_signed(base: usize, offset: i64) -> usize`
- `pub fn align_up_page_size(value: usize) -> usize`
- `pub const fn align_up(value: usize, alignment: usize) -> usize`
- `pub fn align_down(value: usize, alignment: usize) -> usize`
- `pub struct PrintMemorySizeHumanFriendly(pub usize)`
- `pub fn copy_slice<T: Copy>(src: &[T], dst: &mut [T])`
- `pub const fn minimum_amount_of_pages(value: usize) -> usize`
- `pub trait BufferExtension`
- `pub trait ByteInterpretable`
- `pub fn is_power_of_2_or_zero<DataType>(value: DataType) -> bool where DataType: BitAnd<Output = DataType> + PartialEq<DataType> + From<u8> + Sub<Output = DataType> + Copy,`
- `pub fn is_aligned<DataType>(value: DataType, alignment: DataType) -> bool where DataType: Rem<DataType, Output = DataType> + PartialEq<DataType> + From<u8>,`
- `pub trait InBytes`

**kernel/src/klibc/writable_buffer.rs**
- `pub struct WritableBuffer<'a>`
- `pub enum WritableBufferError`

**kernel/src/lib.rs**
- `pub mod asm`
- `pub mod interrupts`
- `pub mod panic`
- `pub extern "C" fn kernel_init(hart_id: usize, device_tree_pointer: *const ()) -> !`
- `pub extern "C" fn prepare_for_scheduling() -> !`

**kernel/src/logging/configuration.rs**
- `pub const fn should_log_module(module_name: &str) -> bool`

**kernel/src/logging/mod.rs**
- `pub mod configuration`
- `pub fn _print(args: fmt::Arguments)`
- `pub fn _print(args: fmt::Arguments)`

**kernel/src/memory/heap.rs**
- `pub fn allocated_size() -> usize`
- `pub fn allocated_size() -> usize`

**kernel/src/memory/mod.rs**
- `pub mod heap`
- `pub mod linker_information`
- `pub mod page_table_entry`
- `pub mod page_tables`
- `pub fn kernel_device_mappings() -> alloc::vec::Vec<page_tables::MappingDescription>`
- `pub struct StaticPageAllocator`
- `pub fn heap_size() -> usize`
- `pub fn heap_size() -> usize`
- `pub fn init_page_allocator(reserved_areas: &[Range<*const u8>])`
- `pub fn used_heap_pages() -> usize`
- `pub fn total_heap_pages() -> usize`

**kernel/src/memory/page_tables.rs**
- `pub struct MappingDescription`
- `pub struct RootPageTableHolder`

**kernel/src/memory/runtime_mappings.rs**
- `pub fn initialize_runtime_mappings(mappings: &[MappingDescription])`
- `pub fn get_runtime_mappings() -> &'static [MappingDescription]`

**kernel/src/net/arp.rs**
- `pub fn cache_lookup(ip: &Ipv4Addr) -> Option<MacAddress>`
- `pub fn cache_insert(ip: Ipv4Addr, mac: MacAddress)`
- `pub fn process_and_respond(data: &[u8])`

**kernel/src/net/checksum.rs**
- `pub fn ones_complement_checksum(slices: &[&[u8]]) -> u16`

**kernel/src/net/ethernet.rs**
- `pub struct EthernetHeader`
- `pub enum ParseError`
- `pub enum EtherTypes`

**kernel/src/net/ipv4.rs**
- `pub struct IpV4Header`
- `pub enum IpV4ParseError`
- `pub const PROTOCOL_TCP: u8 = headers::socket::IPPROTO_TCP as u8`
- `pub const PROTOCOL_UDP: u8 = headers::socket::IPPROTO_UDP as u8`

**kernel/src/net/mac.rs**
- `pub struct MacAddress([u8`

**kernel/src/net/mod.rs**
- `pub mod arp`
- `pub mod mac`
- `pub mod sockets`
- `pub mod tcp`
- `pub mod tcp_connection`
- `pub mod udp`
- `pub fn init_isr_status(isr: MMIO<u32>)`
- `pub fn on_network_interrupt()`
- `pub async fn network_rx_task()`
- `pub fn ip_addr() -> Ipv4Addr`
- `pub fn set_ip_addr(addr: Ipv4Addr)`
- `pub fn has_network_device() -> bool`
- `pub fn open_sockets() -> &'static Spinlock<LazyCell<OpenSockets>>`
- `pub fn assign_network_device(device: NetworkDevice)`
- `pub fn send_packet(packet: Vec<u8>)`
- `pub fn current_mac_address() -> MacAddress`

**kernel/src/net/parsed_packets.rs**
- `pub enum ParsedPacket`

**kernel/src/net/sockets.rs**
- `pub fn wake_socket_waiters()`
- `pub fn socket_data_counter() -> u64`
- `pub struct SocketDataWait`
- `pub struct Port(u16)`
- `pub type SharedAssignedSocket = Arc<Spinlock<AssignedSocket>>`
- `pub struct OpenSockets`
- `pub struct AssignedSocket`

**kernel/src/net/tcp.rs**
- `pub const FLAG_FIN: u16 = headers::socket::TH_FIN as u16`
- `pub const FLAG_SYN: u16 = headers::socket::TH_SYN as u16`
- `pub const FLAG_RST: u16 = headers::socket::TH_RST as u16`
- `pub const FLAG_ACK: u16 = headers::socket::TH_ACK as u16`
- `pub struct TcpHeader`
- `pub enum TcpParseError`

**kernel/src/net/tcp_connection.rs**
- `pub fn allocate_ephemeral_port() -> u16`
- `pub struct TcpConnection`
- `pub type SharedTcpConnection = Arc<Spinlock<TcpConnection>>`
- `pub struct TcpListener`
- `pub type SharedTcpListener = Arc<Spinlock<TcpListener>>`
- `pub fn register_listener(listener: SharedTcpListener)`
- `pub fn process_tcp_packet(ip_header: &IpV4Header, data: &[u8], source_mac: MacAddress)`
- `pub fn create_listener(port: u16) -> SharedTcpListener`
- `pub async fn initiate_connect( local_port: u16, dest_ip: Ipv4Addr, dest_port: u16, ) -> Option<SharedTcpConnection>`
- `pub struct WaitForAccept`
- `pub fn wait_for_accept(listener: &SharedTcpListener) -> WaitForAccept`
- `pub struct WaitForRecvData`
- `pub fn wait_for_recv_data(conn: &SharedTcpConnection, count: usize) -> WaitForRecvData`

**kernel/src/net/udp.rs**
- `pub struct UdpHeader`
- `pub enum UdpParseError`

**kernel/src/panic.rs**
- `pub fn panic_handler(info: &PanicInfo) -> !`

**kernel/src/pci/address.rs**
- `pub struct PciAddr(usize)`
- `pub struct PciCpuAddr(usize)`

**kernel/src/pci/allocator.rs**
- `pub struct PCIAllocator`
- `pub struct PCIAllocatedSpace`

**kernel/src/pci/devic_tree_parser.rs**
- `pub struct PCIInformation`
- `pub struct PCIBitField(u32)`
- `pub struct PCIRange`
- `pub fn parse() -> Option<PCIInformation>`

**kernel/src/pci/lookup.rs**
- `pub fn lookup(vendor_id: u16, device_id: u16) -> Option<String>`

**kernel/src/pci/mod.rs**
- `pub mod address`
- `pub static PCI_ALLOCATOR_64_BIT: Spinlock<PCIAllocator> = Spinlock::new(PCIAllocator::new())`
- `pub static PCI_ALLOCATOR_32_BIT: Spinlock<PCIAllocator> = Spinlock::new(PCIAllocator::new())`
- `pub mod command_register`
- `pub trait GeneralDevicePciHeaderExt`
- `pub struct PciCapabilityIter<'a>`
- `pub struct PCIDevice`
- `pub fn enumerate_devices(pci_information: &PCIInformation) -> Vec<PCIDevice>`

**kernel/src/processes/brk.rs**
- `pub struct Brk`

**kernel/src/processes/credentials.rs**
- `pub struct Credentials`

**kernel/src/processes/fd_table.rs**
- `pub type RawFd = i32`
- `pub struct FdFlags(i32)`
- `pub enum FileDescriptor`
- `pub struct FdEntry`
- `pub struct FdTable`

**kernel/src/processes/futex.rs**
- `pub struct FutexWait`
- `pub fn futex_wake(main_tid: Tid, addr: usize, count: u32) -> i32`

**kernel/src/processes/kernel_tasks.rs**
- `pub fn spawn(future: impl Future<Output = ()> + Send + 'static)`
- `pub fn create_worker_thread()`
- `pub fn take_sleep_request() -> bool`
- `pub fn is_current_worker_tid(tid: Tid) -> bool`

**kernel/src/processes/loader.rs**
- `pub const STACK_START: VirtAddr = VirtAddr::new(usize::MAX)`
- `pub const STACK_SIZE_PAGES: usize = 32`
- `pub const STACK_SIZE: usize = PAGE_SIZE * STACK_SIZE_PAGES`
- `pub const STACK_END: VirtAddr = VirtAddr::new(usize::MAX - STACK_SIZE + 1)`
- `pub struct LoadedElf`
- `pub fn load_elf( elf_file: &ElfFile, name: &str, args: &[&str], env: &[&str], ) -> Result<LoadedElf, LoaderError>`

**kernel/src/processes/mod.rs**
- `pub mod credentials`
- `pub mod fd_table`
- `pub mod futex`
- `pub mod kernel_tasks`
- `pub mod process`
- `pub mod process_table`
- `pub mod scheduler`
- `pub mod signal`
- `pub mod task`
- `pub mod thread`
- `pub mod timer`
- `pub mod userspace_ptr`
- `pub mod wait_child`
- `pub mod waker`

**kernel/src/processes/process.rs**
- `pub const POWERSAVE_TID: Tid = Tid::new(0)`
- `pub struct CowPageInfo`
- `pub enum Mapping`
- `pub struct ForkedAddressSpace`
- `pub type ProcessRef = Arc<Spinlock<Process>>`
- `pub struct Process`

**kernel/src/processes/process_table.rs**
- `pub static RUN_QUEUE: Spinlock<VecDeque<ThreadRef>> = Spinlock::new(VecDeque::new())`
- `pub fn is_empty() -> bool`
- `pub fn live_thread_count() -> usize`
- `pub static THE: RuntimeInitializedData<Spinlock<ProcessTable>> = RuntimeInitializedData::new()`
- `pub fn init()`
- `pub struct ProcessTable`

**kernel/src/processes/scheduler.rs**
- `pub struct CpuScheduler`

**kernel/src/processes/signal.rs**
- `pub const TRAMPOLINE_VADDR: VirtAddr = VirtAddr::new(0x1000)`
- `pub enum ExitStatus`
- `pub struct PendingSignals(u64)`
- `pub enum DefaultAction`
- `pub fn validate_signal(sig: i32) -> Result<Option<u32>, headers::errno::Errno>`
- `pub fn default_action(sig: u32) -> DefaultAction`
- `pub enum SignalDeliveryResult`
- `pub fn deliver_signal(thread: &mut Thread) -> SignalDeliveryResult`
- `pub fn restore_signal_frame(thread: &mut Thread) -> Result<(), headers::errno::Errno>`

**kernel/src/processes/task.rs**
- `pub struct Task<Output = ()>`

**kernel/src/processes/thread.rs**
- `pub type ThreadRef = Arc<Spinlock<Thread>>`
- `pub type ThreadWeakRef = Weak<Spinlock<Thread>>`
- `pub type SyscallTask = Task<Result<isize, Errno>>`
- `pub fn get_next_tid() -> Tid`
- `pub enum ThreadState`
- `pub struct Thread`

**kernel/src/processes/timer.rs**
- `pub fn init()`
- `pub struct Sleep`
- `pub fn sleep(duration: &timespec) -> Result<Sleep, Errno>`
- `pub fn wakeup_wakers()`
- `pub fn current_time() -> timespec`
- `pub fn set_timer(milliseconds: u64)`

**kernel/src/processes/userspace_ptr.rs**
- `pub struct UserspacePtr<PTR: Pointer>`
- `pub struct ContainsUserspacePtr<T>(pub UnsafeSendSync<T>)`

**kernel/src/processes/wait_child.rs**
- `pub enum WaitPid`
- `pub struct WaitChild`

**kernel/src/processes/waker.rs**
- `pub struct ThreadWaker`

**kernel/src/syscalls/linux.rs**
- `pub struct LinuxSyscallHandler`

**kernel/src/syscalls/linux_validator.rs**
- `pub struct LinuxUserspaceArg<T>`

**kernel/src/syscalls/macros.rs**
- `pub trait NeedsUserSpaceWrapper`

**kernel/src/syscalls/mod.rs**
- `pub mod linux`
- `pub mod linux_validator`
- `pub mod trace_config`
- `pub mod tracer`

**kernel/src/syscalls/trace_config.rs**
- `pub const TRACED_PROCESSES: &[&str] = &["prog2"]`

**kernel/src/syscalls/tracer.rs**
- `pub enum ArgFormat`
- `pub struct SyscallMetadata`
- `pub trait SyscallArgFormat`
- `pub async fn trace_syscall( trap_frame: &TrapFrame, handler: &mut LinuxSyscallHandler, ) -> Result<isize, Errno>`
- `pub fn log_unimplemented_and_kill(trap_frame: &TrapFrame)`

**kernel/src/test/mod.rs**
- `pub mod qemu_exit`
- `pub trait Testable`
- `pub fn test_runner(tests: &[&dyn Testable])`

**kernel/src/test/qemu_exit.rs**
- `pub fn exit_success() -> !`
- `pub fn exit_failure(code: u16) -> !`
- `pub fn exit_reset() -> !`
- `pub fn wait_for_the_end() -> !`

**mcp-server/Cargo.toml**
- `[package]`
- `[workspace]`
- `[dependencies]`

**mcp-server/src/server.rs**
- `pub struct QemuMcpServer`
- `pub struct BootParams`
- `pub struct SendCommandParams`
- `pub struct SendInputParams`
- `pub struct BuildKernelParams`
- `pub struct ScreenshotParams`
- `pub struct RunSystemTestsParams`

**plans/00-overview.md**
- `# Solaya Linux Compatibility: Master Plan`

**plans/01-roadmap.md**
- `# Solaya to Linux-Compatible OS: Comprehensive Roadmap`

**plans/02-testing-strategy.md**
- `# Testing Strategy for Linux Binary Compatibility`
- `# Disable dynamic tests (Solaya has no dynamic linker)`
- `# Build only static variants`
- `# From Linux kernel source tree`
- `# Stress fork/exec for 10 seconds`
- `# Stress signal handling`
- `# Stress mmap`
- `# Solaya Compliance Dashboard`
- `# Create empty image`
- `# Process management`
- `# File I/O`
- `# File system`
- `# Memory`
- `# Signals`
- `# Time`
- `# IPC`
- `# Network`
- `# Misc`
- `# .github/workflows/compliance.yml`
- `# Common environment variables`
- `# libc-test`
- `# Edit config.mak with CC and CFLAGS above`
- `# LTP`
- `# kselftest (from Linux kernel tree)`
- `# stress-ng`
- `# scripts/build-compliance-image.sh`
- `# Generate manifest`
- `# Create ext2 image`

**plans/03-driver-framework.md**
- `# Driver Framework Plan`

**plans/04-build-system.md**
- `# Plan 04: Build System and Multi-Architecture Support`
- `# Install Rust nightly with riscv64 target`

**plans/05-organization.md**
- `# Plan 05: Project Organization and AI-Assisted Development`
- `# Task: <Short Title>`
- `# Start an agent in its own worktree`
- `# Or configure subagents with worktree isolation`
- `# In .claude/agents/implementer.md frontmatter:`
- `# isolation: worktree`

**qemu-infra/Cargo.toml**
- `[package]`
- `[workspace]`
- `[dependencies]`

**qemu-infra/src/lib.rs**
- `pub mod ppm`
- `pub mod qemu`
- `pub mod qmp`
- `pub mod read_asserter`
- `pub const PROMPT: &str = "$ "`

**qemu-infra/src/ppm.rs**
- `pub struct PpmImage`

**qemu-infra/src/qemu.rs**
- `pub fn project_root() -> anyhow::Result<PathBuf>`
- `pub struct QemuOptions`
- `pub struct QemuInstance`

**qemu-infra/src/qmp.rs**
- `pub struct QmpClient`

**qemu-infra/src/read_asserter.rs**
- `pub enum ReadError`
- `pub struct ReadAsserter<Reader: AsyncRead + Unpin>`

**qemu-infra/src/searchable_buffer.rs**
- `pub struct SearchableBuffer`

**sys/Cargo.toml**
- `[package]`
- `[lib]`
- `[dependencies]`

**sys/src/cpu.rs**
- `pub struct CpuBase`
- `pub const TRAP_FRAME_OFFSET: usize = offset_of!(CpuBase, trap_frame)`
- `pub const KERNEL_PAGE_TABLES_SATP_OFFSET: usize = offset_of!(CpuBase, kernel_page_tables_satp_value)`
- `pub const CPU_ID_OFFSET: usize = offset_of!(CpuBase, cpu_id)`
- `pub static STARTING_CPU_ID: RuntimeInitializedData<CpuId> = RuntimeInitializedData::new()`
- `pub fn cpu_id() -> CpuId`
- `pub fn cpu_id() -> CpuId`
- `pub fn per_cpu_ref<T>() -> &'static T`
- `pub fn try_per_cpu_ref<T>() -> Option<&'static T>`
- `pub fn per_cpu_volatile_read<T>(offset: usize) -> T`
- `pub fn per_cpu_volatile_write<T>(offset: usize, value: T)`
- `pub fn disable_interrupts_and_halt() -> !`

**sys/src/io/mod.rs**
- `pub mod uart`

**sys/src/io/uart.rs**
- `pub const UART_BASE_ADDRESS: usize = 0x1000_0000`
- `pub static QEMU_UART: Spinlock<UnsafeSendSync<Uart>> = Spinlock::new(UnsafeSendSync(Uart::new(UART_BASE_ADDRESS)))`
- `pub struct Uart`

**sys/src/klibc/array_vec.rs**
- `pub enum ArrayVecError<T>`
- `pub struct ArrayVec<T, const LENGTH: usize>`
- `pub struct ArrayVecIter<'a, T, const LENGTH: usize>`

**sys/src/klibc/deconstructed_vec.rs**
- `pub struct DeconstructedVec`

**sys/src/klibc/mmio.rs**
- `pub fn write_bytes(addr: usize, data: &[u8])`
- `pub fn read_bytes(addr: usize, buf: &mut [u8])`
- `pub struct MMIO<T>`

**sys/src/klibc/mod.rs**
- `pub mod array_vec`
- `pub mod deconstructed_vec`
- `pub mod mmio`
- `pub mod runtime_initialized`
- `pub mod send_sync`
- `pub mod sizes`
- `pub mod spinlock`
- `pub mod util`
- `pub mod validated_ptr`

**sys/src/klibc/runtime_initialized.rs**
- `pub struct RuntimeInitializedData<T>`

**sys/src/klibc/send_sync.rs**
- `pub struct UnsafeSendSync<T>(pub T)`

**sys/src/klibc/sizes.rs**
- `pub const fn KiB(value: usize) -> usize`
- `pub const fn MiB(value: usize) -> usize`
- `pub const fn GiB(value: usize) -> usize`

**sys/src/klibc/spinlock.rs**
- `pub struct Spinlock<T>`
- `pub struct SpinlockGuard<'a, T>`

**sys/src/klibc/util.rs**
- `pub trait UsizeExt`
- `pub fn wrapping_add_signed(base: usize, offset: i64) -> usize`
- `pub fn align_up_page_size(value: usize) -> usize`
- `pub const fn align_up(value: usize, alignment: usize) -> usize`
- `pub fn align_down_ptr<T>(ptr: *const T, alignment: usize) -> *const T`
- `pub fn align_down(value: usize, alignment: usize) -> usize`
- `pub struct PrintMemorySizeHumanFriendly(pub usize)`
- `pub fn copy_slice<T: Copy>(src: &[T], dst: &mut [T])`
- `pub const fn minimum_amount_of_pages(value: usize) -> usize`
- `pub trait BufferExtension`
- `pub trait ByteInterpretable`
- `pub fn cstr_from_null_terminated_ptr(ptr: *const core::ffi::c_char) -> &'static core::ffi::CStr`
- `pub fn as_byte_slice<T: ?Sized>(value: &T) -> &[u8]`
- `pub fn ref_from_bytes<T>(data: &[u8]) -> &T`
- `pub fn slice_from_bytes<T>(data: &[u8], offset: usize, count: usize) -> &[T]`
- `pub fn read_from_bytes<T: Copy>(data: &[u8]) -> T`
- `pub fn is_power_of_2_or_zero<DataType>(value: DataType) -> bool where DataType: BitAnd<Output = DataType> + PartialEq<DataType> + From<u8> + Sub<Output = DataType> + Copy,`
- `pub fn is_aligned<DataType>(value: DataType, alignment: DataType) -> bool where DataType: Rem<DataType, Output = DataType> + PartialEq<DataType> + From<u8>,`
- `pub fn set_or_clear_bit<DataType>( data: &mut DataType, should_set_bit: bool, bit_position: usize, ) -> DataType where DataType: BitOrAssign + BitAndAssign + Not<Output = DataType> + From<u8> + Shl<usize, Output = DataType> + Copy,`
- `pub fn set_bit<DataType>(data: &mut DataType, bit_position: usize) where DataType: BitOrAssign + Not<Output = DataType> + From<u8> + Shl<usize, Output = DataType>,`
- `pub fn clear_bit<DataType>(data: &mut DataType, bit_position: usize) where DataType: BitAndAssign + Not<Output = DataType> + From<u8> + Shl<usize, Output = DataType>,`
- `pub fn get_bit<DataType>(data: DataType, bit_position: usize) -> bool where DataType: Shr<usize, Output = DataType> + BitAnd<DataType, Output = DataType> + PartialEq<DataType> + From<u8>,`
- `pub fn set_multiple_bits<DataType, ValueType>( data: &mut DataType, value: ValueType, number_of_bits: usize, bit_position: usize, ) -> DataType where DataType: BitAndAssign + BitOrAssign + Not<Output = DataType> + From<u8> + Shl<usize, Output = DataType> + Copy, ValueType: Copy + BitAnd + From<u8> + Shl<usize, Output = ValueType>, <ValueType as BitAnd>::Output: PartialOrd<ValueType>,`
- `pub fn get_multiple_bits<DataType, ValueType>( data: DataType, number_of_bits: usize, bit_position: usize, ) -> ValueType where DataType: Shr<usize, Output = DataType> + BitAnd<u64, Output = ValueType>,`
- `pub trait InBytes`

**sys/src/klibc/validated_ptr.rs**
- `pub trait PtrValidator`
- `pub struct ValidatedPtr<T>`

**sys/src/lib.rs**
- `pub mod cpu`
- `pub mod io`
- `pub mod klibc`
- `pub mod logging`
- `pub mod memory`
- `pub mod panic_support`

**sys/src/logging/configuration.rs**
- `pub const fn should_log_module(module_name: &str) -> bool`

**sys/src/logging/mod.rs**
- `pub mod configuration`
- `pub fn _print(args: fmt::Arguments)`
- `pub fn _print(args: fmt::Arguments)`

**sys/src/memory/address.rs**
- `pub struct PhysAddr(usize)`
- `pub struct VirtAddr(usize)`

**sys/src/memory/heap.rs**
- `pub struct Heap<Allocator: PageAllocator>`
- `pub struct SpinlockHeap<Allocator: PageAllocator>`

**sys/src/memory/mod.rs**
- `pub mod address`
- `pub mod heap`
- `pub mod page`
- `pub mod page_allocator`
- `pub mod page_table`
- `pub fn linker_region_as_uninit_slice( start: VirtAddr, size: usize, ) -> &'static mut [MaybeUninit<u8>]`

**sys/src/memory/page.rs**
- `pub const PAGE_SIZE: usize = 4096`
- `pub struct Pages(usize)`
- `pub struct Page([u8`
- `pub trait PagesAsSlice`
- `pub fn page_slice_at_phys(addr: super::PhysAddr) -> &'static [u8; PAGE_SIZE]`
- `pub struct PinnedHeapPages`

**sys/src/memory/page_allocator.rs**
- `pub struct MetadataPageAllocator<'a>`
- `pub trait PageAllocator`

**sys/src/memory/page_table.rs**
- `pub enum XWRMode`
- `pub struct PageTableEntry(pub *mut PageTable)`
- `pub struct PageTable(pub [PageTableEntry`
- `pub struct OwnedPageTable`
- `pub fn activate_page_table(satp_val: usize)`

**sys/src/panic_support.rs**
- `pub fn panic_disable_interrupts()`

**system-tests/Cargo.toml**
- `[package]`
- `[workspace]`
- `[dependencies]`

**system-tests/src/infra/mod.rs**
- `pub mod qemu`

**typos.toml**
- `[default.extend-identifiers]`
- `[default.extend-words]`

**userspace/Cargo.toml**
- `[package]`
- `[dependencies]`

**userspace/doom/dg_solaya.c**
- `#define FB_WIDTH 640`
- `#define FB_HEIGHT 480`
- `#define WAD_PATH "/tmp/doom1.wad"`
- `#define EV_KEY 1`
- `#define KEY_ESC_LC 1`
- `#define KEY_1_LC 2`
- `#define KEY_2_LC 3`
- `#define KEY_3_LC 4`
- `#define KEY_4_LC 5`
- `#define KEY_5_LC 6`
- `#define KEY_6_LC 7`
- `#define KEY_7_LC 8`
- `#define KEY_8_LC 9`
- `#define KEY_9_LC 10`
- `#define KEY_0_LC 11`
- `#define KEY_MINUS_LC 12`
- `#define KEY_EQUAL_LC 13`
- `#define KEY_TAB_LC 15`
- `#define KEY_Q_LC 16`
- `#define KEY_W_LC 17`
- `#define KEY_E_LC 18`
- `#define KEY_R_LC 19`
- `#define KEY_T_LC 20`
- `#define KEY_Y_LC 21`
- `#define KEY_ENTER_LC 28`
- `#define KEY_A_LC 30`
- `#define KEY_S_LC 31`
- `#define KEY_D_LC 32`
- `#define KEY_F_LC 33`
- `#define KEY_LSHIFT_LC 42`
- `#define KEY_COMMA_LC 51`
- `#define KEY_DOT_LC 52`
- `#define KEY_RSHIFT_LC 54`
- `#define KEY_SPACE_LC 57`
- `#define KEY_UP_LC 103`
- `#define KEY_LEFT_LC 105`
- `#define KEY_RIGHT_LC 106`
- `#define KEY_DOWN_LC 108`
- `struct virtio_input_event`
- `void DG_Init(void)`
- `void DG_DrawFrame(void)`
- `void DG_SleepMs(uint32_t ms)`
- `uint32_t DG_GetTicksMs(void)`
- `#define MAX_EVENTS 16`
- `int DG_GetKey(int *pressed, unsigned char *doomKey)`
- `void DG_SetWindowTitle(const char *title)`
- `int main(int argc, char **argv)`

**userspace/doom/i_video_solaya.c**
- `struct FB_BitField`
- `struct FB_ScreenInfo`
- `int fb_scaling = 1`
- `int usemouse = 0`
- `void I_GetEvent(void)`
- `byte *I_VideoBuffer = NULL`
- `boolean screensaver_mode = false`
- `boolean screenvisible`
- `float mouse_acceleration = 2.0`
- `int mouse_threshold = 10`
- `int usegamma = 0`
- `typedef struct`
- `void cmap_to_rgb565(uint16_t * out, uint8_t * in, int in_pixels)`
- `void cmap_to_fb(uint8_t *out, uint8_t *in, int in_pixels)`
- `void I_InitGraphics (void)`
- `void I_ShutdownGraphics (void)`
- `void I_StartFrame (void)`
- `void I_StartTic (void)`
- `void I_UpdateNoBlit (void)`
- `void I_FinishUpdate (void)`
- `void I_ReadScreen (byte* scr)`
- `void I_SetPalette (byte* palette)`
- `int I_GetPaletteIndex (int r, int g, int b)`
- `void I_BeginRead (void)`
- `void I_EndRead (void)`
- `void I_SetWindowTitle (char *title)`
- `void I_GraphicsCheckCommandLine (void)`
- `void I_SetGrabMouseCallback (grabmouse_callback_t func)`
- `void I_EnableLoadingDisk(void)`
- `void I_BindVideoVariables (void)`
- `void I_DisplayFPSDots (boolean dots_on)`
- `void I_CheckIsScreensaver (void)`

**userspace/src/bin/connect4/game_board.rs**
- `pub enum Player`
- `pub struct GameBoard`

**userspace/src/lib.rs**
- `pub mod spawn`
- `pub mod util`

**userspace/src/spawn.rs**
- `pub fn spawn(program: &str, args: &[&str]) -> Result<Child, std::io::Error>`

**userspace/src/util.rs**
- `pub fn read_line() -> String`

**userspace/static/index.html**
- `<head>`
- `title: Solaya`
- `<style>`
- `<body>`

---

## CLAUDE.md

**Language:** Markdown | **Size:** 16.5 KB | **Lines:** 327

**Declarations:**

---

## Cargo.toml

**Language:** TOML | **Size:** 433 B | **Lines:** 27

**Declarations:**

---

## INDEX.md

**Language:** Markdown | **Size:** 230.6 KB | **Lines:** 9834

**Declarations:**

---

## README.md

**Language:** Markdown | **Size:** 5.8 KB | **Lines:** 121

**Declarations:**

---

## arch/Cargo.toml

**Language:** TOML | **Size:** 202 B | **Lines:** 13

**Imports:**
- `common`

**Declarations:**

---

## arch/src/lib.rs

**Language:** Rust | **Size:** 680 B | **Lines:** 31

**Imports:**
- `pub use riscv64::*`
- `pub use stub::*`

**Declarations:**

`mod riscv64`

`mod stub`

**`impl CpuId`**
  `pub fn from_hart_id(hart_id: usize) -> Self`

  `pub fn as_usize(self) -> usize`


**`impl core::fmt::Display for CpuId`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


---

## arch/src/riscv64/backtrace.rs

**Language:** Rust | **Size:** 4.3 KB | **Lines:** 162

**Declarations:**

**`impl core::fmt::Display for CalleeSavedRegs`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl core::ops::Index<usize> for CalleeSavedRegs`**
  `fn index(&self, index: usize) -> &Self::Output`


**`impl core::ops::IndexMut<usize> for CalleeSavedRegs`**
  `fn index_mut(&mut self, index: usize) -> &mut Self::Output`


**`impl CalleeSavedRegs`**
  `pub fn ra(&self) -> usize`

  `pub fn set_ra(&mut self, value: usize)`

  `pub fn sp(&self) -> usize`

  `pub fn set_sp(&mut self, value: usize)`

  `pub fn with_context<F: FnMut(&mut CalleeSavedRegs)>(f: F)`


---

## arch/src/riscv64/cpu.rs

**Language:** Rust | **Size:** 4.2 KB | **Lines:** 171

**Imports:**
- `core::arch::asm`

**Declarations:**

`const SIE_STIE: usize = 5`

`const SSTATUS_SPP: usize = 8`

`const SIP_SSIP: usize = 1`

**`impl InterruptGuard`**
  `pub fn new() -> Self`


**`impl Drop for InterruptGuard`**
  `fn drop(&mut self)`


---

## arch/src/riscv64/linker_symbols.rs

**Language:** Rust | **Size:** 1.5 KB | **Lines:** 56

**Declarations:**

---

## arch/src/riscv64/mod.rs

**Language:** Rust | **Size:** 104 B | **Lines:** 6

**Declarations:**

---

## arch/src/riscv64/sbi/extensions/base_extension.rs

**Language:** Rust | **Size:** 428 B | **Lines:** 16

**Imports:**
- `crate::sbi`

**Declarations:**

`const EID: u64 = 0x10`

---

## arch/src/riscv64/sbi/extensions/hart_state_extension.rs

**Language:** Rust | **Size:** 578 B | **Lines:** 28

**Imports:**
- `crate::sbi::{self, sbi_call::SbiRet}`

**Declarations:**

`const EID: u64 = 0x48534D`

`const FID_HART_START: u64 = 0x0`

`const FID_GET_STATUS: u64 = 0x2`

---

## arch/src/riscv64/sbi/extensions/ipi_extension.rs

**Language:** Rust | **Size:** 280 B | **Lines:** 9

**Imports:**
- `crate::sbi::{self, sbi_call::SbiRet}`

**Declarations:**

`const EID: u64 = 0x735049`

`const FID_SEND_IPI: u64 = 0x0`

---

## arch/src/riscv64/sbi/extensions/mod.rs

**Language:** Rust | **Size:** 102 B | **Lines:** 4

**Declarations:**

---

## arch/src/riscv64/sbi/extensions/timer_extension.rs

**Language:** Rust | **Size:** 215 B | **Lines:** 8

**Imports:**
- `crate::sbi::{self, sbi_call::SbiRet}`

**Declarations:**

`const EID: u64 = 0x54494D45`

`const FID_SET_TIMER: u64 = 0x0`

---

## arch/src/riscv64/sbi/mod.rs

**Language:** Rust | **Size:** 59 B | **Lines:** 4

**Imports:**
- `sbi_call::sbi_call`

**Declarations:**

`mod sbi_call`

---

## arch/src/riscv64/sbi/sbi_call.rs

**Language:** Rust | **Size:** 2.2 KB | **Lines:** 85

**Imports:**
- `core::arch::asm`

**Declarations:**

**`impl SbiRet`**
  `fn new(error: i64, value: i64) -> Self`

  `pub fn assert_success(&self)`

  `pub fn is_error(&self) -> bool`


**`impl Default for SbiRet`**
  `fn default() -> Self`


---

## arch/src/riscv64/timer.rs

**Language:** Rust | **Size:** 388 B | **Lines:** 14

**Imports:**
- `core::arch::asm`

**Declarations:**

---

## arch/src/riscv64/trap_cause.rs

**Language:** Rust | **Size:** 3.2 KB | **Lines:** 80

**Imports:**
- `exception::*`
- `interrupt::*`

**Declarations:**

**`impl InterruptCause`**
  `pub fn from_scause() -> Self`

  `pub fn is_interrupt(&self) -> bool`

  `pub fn get_exception_code(&self) -> usize`

  `pub fn get_reason(&self) -> &'static str`


---

## arch/src/stub/backtrace.rs

**Language:** Rust | **Size:** 1.1 KB | **Lines:** 46

**Declarations:**

**`impl core::fmt::Display for CalleeSavedRegs`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl core::ops::Index<usize> for CalleeSavedRegs`**
  `fn index(&self, index: usize) -> &Self::Output`


**`impl core::ops::IndexMut<usize> for CalleeSavedRegs`**
  `fn index_mut(&mut self, index: usize) -> &mut Self::Output`


**`impl CalleeSavedRegs`**
  `pub fn ra(&self) -> usize`

  `pub fn set_ra(&mut self, value: usize)`

  `pub fn sp(&self) -> usize`

  `pub fn set_sp(&mut self, value: usize)`

  `pub fn with_context<F: FnMut(&mut CalleeSavedRegs)>(_f: F)`


---

## arch/src/stub/cpu.rs

**Language:** Rust | **Size:** 1.7 KB | **Lines:** 88

**Declarations:**

**`impl InterruptGuard`**
  `pub fn new() -> Self`


---

## arch/src/stub/linker_symbols.rs

**Language:** Rust | **Size:** 882 B | **Lines:** 39

**Declarations:**

`const DUMMY_ADDR: usize = 0xFFFF_F000`

---

## arch/src/stub/mod.rs

**Language:** Rust | **Size:** 104 B | **Lines:** 6

**Declarations:**

---

## arch/src/stub/sbi.rs

**Language:** Rust | **Size:** 2.2 KB | **Lines:** 94

**Declarations:**

---

## arch/src/stub/timer.rs

**Language:** Rust | **Size:** 186 B | **Lines:** 9

**Declarations:**

---

## arch/src/stub/trap_cause.rs

**Language:** Rust | **Size:** 3.6 KB | **Lines:** 95

**Declarations:**

**`impl InterruptCause`**
  `pub fn from_scause() -> Self`

  `pub fn is_interrupt(&self) -> bool`

  `pub fn get_exception_code(&self) -> usize`

  `pub fn get_reason(&self) -> &'static str`


---

## boot/Cargo.toml

**Language:** TOML | **Size:** 255 B | **Lines:** 12

**Imports:**
- `solaya`

**Declarations:**

`cargo-features = ["per-package-target"]`

---

## boot/build.rs

**Language:** Rust | **Size:** 127 B | **Lines:** 4

**Declarations:**

`fn main()`

---

## boot/src/main.rs

**Language:** Rust | **Size:** 1.1 KB | **Lines:** 42

**Declarations:**

`extern "C" fn kernel_init(hart_id: usize, device_tree_pointer: *const ()) -> !`

`extern "C" fn prepare_for_scheduling() -> !`

`extern "C" fn handle_trap()`

`extern "C" fn get_process_satp_value() -> usize`

`fn asm_panic_rust()`

`fn panic(info: &core::panic::PanicInfo) -> !`

---

## common/Cargo.toml

**Language:** TOML | **Size:** 254 B | **Lines:** 12

**Declarations:**

---

## common/src/errors.rs

**Language:** Rust | **Size:** 60 B | **Lines:** 4

**Declarations:**

---

## common/src/ioctl.rs

**Language:** Rust | **Size:** 1.7 KB | **Lines:** 64

**Declarations:**

---

## common/src/lib.rs

**Language:** Rust | **Size:** 258 B | **Lines:** 14

**Declarations:**

---

## common/src/macros.rs

**Language:** Rust | **Size:** 1.3 KB | **Lines:** 46

**Declarations:**

---

## common/src/numbers.rs

**Language:** Rust | **Size:** 815 B | **Lines:** 32

**Imports:**
- `core::fmt::{Debug, Display}`

**Declarations:**

---

## common/src/pid.rs

**Language:** Rust | **Size:** 586 B | **Lines:** 26

**Declarations:**

**`impl Tid`**
  `pub const fn new(value: u64) -> Self`

  `pub const fn as_u64(self) -> u64`

  `pub fn as_isize(self) -> isize`

  `pub fn try_from_i32(value: i32) -> Option<Self>`


**`impl core::fmt::Display for Tid`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


---

## common/src/pointer.rs

**Language:** Rust | **Size:** 727 B | **Lines:** 34

**Declarations:**

**`impl<T> Pointer for *const T`**
  `fn as_raw(&self) -> usize`

  `fn as_pointer(ptr: usize) -> Self`


**`impl<T> Pointer for *mut T`**
  `fn as_raw(&self) -> usize`

  `fn as_pointer(ptr: usize) -> Self`


---

## common/src/syscalls/mod.rs

**Language:** Rust | **Size:** 20 B | **Lines:** 1

**Declarations:**

---

## common/src/syscalls/trap_frame.rs

**Language:** Rust | **Size:** 3.3 KB | **Lines:** 157

**Imports:**
- `core::fmt::Debug`

**Declarations:**

**`impl Debug for TrapFrame`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl core::ops::Index<Register> for TrapFrame`**
  `fn index(&self, index: Register) -> &Self::Output`


**`impl core::ops::IndexMut<Register> for TrapFrame`**
  `fn index_mut(&mut self, index: Register) -> &mut Self::Output`


**`impl TrapFrame`**
  `pub const fn zero() -> Self`

  `pub fn gp_registers(&self) -> &[usize; 32]`

  `pub fn fp_registers(&self) -> &[usize; 32]`

  `pub fn gp_registers_mut(&mut self) -> &mut [usize; 32]`

  `pub fn fp_registers_mut(&mut self) -> &mut [usize; 32]`


---

## doc/ai/ARCHITECTURE.md

**Language:** Markdown | **Size:** 9.6 KB | **Lines:** 258

**Declarations:**

---

## doc/ai/BUILD.md

**Language:** Markdown | **Size:** 4.4 KB | **Lines:** 154

**Declarations:**

---

## doc/ai/DEBUGGING.md

**Language:** Markdown | **Size:** 7.8 KB | **Lines:** 339

**Declarations:**

---

## doc/ai/DRIVERS.md

**Language:** Markdown | **Size:** 8.8 KB | **Lines:** 309

**Declarations:**

---

## doc/ai/FS.md

**Language:** Markdown | **Size:** 3.5 KB | **Lines:** 93

**Declarations:**

---

## doc/ai/INTERRUPTS.md

**Language:** Markdown | **Size:** 7.1 KB | **Lines:** 280

**Declarations:**

---

## doc/ai/MEMORY.md

**Language:** Markdown | **Size:** 8.2 KB | **Lines:** 320

**Declarations:**

---

## doc/ai/NETWORKING.md

**Language:** Markdown | **Size:** 11.1 KB | **Lines:** 364

**Declarations:**

---

## doc/ai/OVERVIEW.md

**Language:** Markdown | **Size:** 4.1 KB | **Lines:** 91

**Declarations:**

---

## doc/ai/PROCESSES.md

**Language:** Markdown | **Size:** 12.5 KB | **Lines:** 396

**Declarations:**

---

## doc/ai/SYSCALLS.md

**Language:** Markdown | **Size:** 11.0 KB | **Lines:** 244

**Declarations:**

---

## doc/ai/TESTING.md

**Language:** Markdown | **Size:** 10.0 KB | **Lines:** 344

**Declarations:**

---

## gdb_mcp_server/__init__.py

**Language:** Python | **Size:** 0 B | **Lines:** 0

---

## gdb_mcp_server/__main__.py

**Language:** Python | **Size:** 35 B | **Lines:** 3

**Imports:**
- `from .server import mcp`

---

## gdb_mcp_server/gdb_session.py

**Language:** Python | **Size:** 2.9 KB | **Lines:** 97

**Imports:**
- `import os`
- `import signal`
- `from pathlib import Path`
- `from pygdbmi.gdbcontroller import GdbController`

**Declarations:**

---

## gdb_mcp_server/server.py

**Language:** Python | **Size:** 8.9 KB | **Lines:** 299

**Imports:**
- `import os`
- `import signal`
- `from mcp.server.fastmcp import FastMCP`
- `from .gdb_session import GDBSession, DEFAULT_KERNEL_PATH, find_project_root`

**Declarations:**

`def _with_timeout(func, timeout_sec)`

`def _format_responses(responses: list[dict]) -> str`

`def _format_error(responses: list[dict]) -> str | None`

`def _run_mi(command: str, timeout_sec: int = 30) -> str`

`def _run_cli(command: str, timeout_sec: int = 30) -> str`

---

## headers/Cargo.toml

**Language:** TOML | **Size:** 196 B | **Lines:** 13

**Imports:**
- `bindgen`

**Declarations:**

---

## headers/build.rs

**Language:** Rust | **Size:** 10.4 KB | **Lines:** 327

**Imports:**
- `std::{
    env,
    fs::File,
    io::Write,
    path::{Path, PathBuf},
    process::Command,
    sync::{Arc, Mutex},
}`
- `bindgen::callbacks::ParseCallbacks`

**Declarations:**

`const SYSCALL_PREFIX: &str = "__NR_"`

`struct SyscallReaderCallback`
> Fields: `syscalls: Arc<Mutex<Vec<String>>>`

**`impl ParseCallbacks for SyscallReaderCallback`**
  `fn int_macro(&self, name: &str, _value: i64) -> Option<bindgen::callbacks::IntKind>`

  `fn item_name(&self, item_info: bindgen::callbacks::ItemInfo) -> Option<String>`


`struct ErrnoCallback`
> Fields: `errnos: Arc<Mutex<Vec<(String, isize)>>>`

**`impl ParseCallbacks for ErrnoCallback`**
  `fn int_macro(&self, name: &str, value: i64) -> Option<bindgen::callbacks::IntKind>`


`fn main() -> Result<(), Box<dyn std::error::Error>>`

`fn default_bindgen_builder() -> bindgen::Builder`

`fn generate_syscall_types(out_path: &Path) -> Result<(), Box<dyn std::error::Error>>`

`fn generate_error_types(out_path: &Path) -> Result<(), Box<dyn std::error::Error>>`

`fn generate_syscall_nr_file(out_path: &Path) -> Result<(), Box<dyn std::error::Error>>`

`struct SocketConstantCallback`

**`impl ParseCallbacks for SocketConstantCallback`**
  `fn int_macro(&self, _name: &str, _value: i64) -> Option<bindgen::callbacks::IntKind>`


`fn generate_socket_types(out_path: &Path) -> Result<(), Box<dyn std::error::Error>>`

`struct FsConstantCallback`

**`impl ParseCallbacks for FsConstantCallback`**
  `fn int_macro(&self, name: &str, _value: i64) -> Option<bindgen::callbacks::IntKind>`


`struct DtConstantCallback`

**`impl ParseCallbacks for DtConstantCallback`**
  `fn int_macro(&self, _name: &str, _value: i64) -> Option<bindgen::callbacks::IntKind>`


`fn generate_fs_types(out_path: &Path) -> Result<(), Box<dyn std::error::Error>>`

`fn generate_sysinfo_types(out_path: &Path) -> Result<(), Box<dyn std::error::Error>>`

`fn format_file(path: PathBuf) -> Result<(), Box<dyn std::error::Error>>`

---

## headers/src/lib.rs

**Language:** Rust | **Size:** 521 B | **Lines:** 26

**Declarations:**

---

## kernel/Cargo.toml

**Language:** TOML | **Size:** 813 B | **Lines:** 29

**Imports:**
- `arch`
- `common`
- `headers`
- `sys`
- `elf`
- `gimli`

**Declarations:**

`cargo-features = ["per-package-target"]`

---

## kernel/build.rs

**Language:** Rust | **Size:** 3.3 KB | **Lines:** 104

**Imports:**
- `std::{
    collections::BTreeMap,
    env,
    error::Error,
    fmt::Write as _,
    fs::read_dir,
    io::Write as _,
    path::PathBuf,
    process::{Command, Stdio},
}`

**Declarations:**

`fn main() -> Result<(), Box<dyn Error>>`

`fn is_miri_execution() -> bool`

`fn is_kani_execution() -> bool`

`fn generate_userspace_programs_include() -> Result<(), Box<dyn Error>>`

---

## kernel/src/asm/mod.rs

**Language:** Rust | **Size:** 245 B | **Lines:** 10

**Imports:**
- `pub use arch::{
    cpu::{asm_panic_rust, wfi_loop},
    linker_symbols::powersave_fn_addr,
}`
- `crate::memory::PhysAddr`

**Declarations:**

---

## kernel/src/assert.rs

**Language:** Rust | **Size:** 135 B | **Lines:** 6

**Imports:**
- `pub(crate) use common::static_assert_size`

**Declarations:**

---

## kernel/src/autogenerated/mod.rs

**Language:** Rust | **Size:** 28 B | **Lines:** 1

**Declarations:**

---

## kernel/src/cpu.rs

**Language:** Rust | **Size:** 4.0 KB | **Lines:** 134

**Imports:**
- `alloc::{boxed::Box, sync::Arc}`
- `common::syscalls::trap_frame::TrapFrame`
- `pub use arch::CpuId`
- `crate::{
    klibc::{Spinlock, SpinlockGuard, sizes::KiB},
    memory::page_tables::RootPageTableHolder,
    processes::{
        process::Process,
        scheduler::CpuScheduler,
        thread::{ThreadRef, ThreadWeakRef},
    },
}`
- `arch::sbi::extensions::ipi_extension::sbi_send_ipi`

**Declarations:**

`pub(crate) const KERNEL_STACK_SIZE: usize = KiB(512)`

**`impl Cpu`**
  `pub fn ipi_to_all_but_me(&self)`

  `pub fn init(cpu_id: CpuId, number_cpus: usize) -> *const Cpu`

  `pub fn current() -> &'static Cpu`

  `pub fn read_trap_frame() -> TrapFrame`

  `pub fn write_trap_frame(trap_frame: TrapFrame)`

  `pub fn with_scheduler<R>(f: impl FnOnce(SpinlockGuard<'_, CpuScheduler>) -> R) -> R`

  `pub fn current_thread() -> ThreadRef`

  `pub fn current_thread_weak() -> ThreadWeakRef`

  `pub fn with_current_process<R>(f: impl FnOnce(SpinlockGuard<'_, Process>) -> R) -> R`

  `pub fn maybe_kernel_page_tables() -> Option<&'static RootPageTableHolder>`

  `pub fn cpu_id() -> CpuId`

  `pub fn number_cpus(&self) -> usize`

  `pub fn activate_kernel_page_table(&self)`

  `pub fn scheduler(&self) -> &Spinlock<CpuScheduler>`


**`impl Drop for Cpu`**
  `fn drop(&mut self)`


---

## kernel/src/debugging/backtrace.rs

**Language:** Rust | **Size:** 8.1 KB | **Lines:** 262

**Imports:**
- `super::eh_frame_parser`
- `crate::{
    cpu::KERNEL_STACK_SIZE,
    debugging::{
        self,
        eh_frame_parser::EhFrameParser,
        unwinder::{RegisterRule, Unwinder},
    },
    info,
    klibc::{runtime_initialized::RuntimeInitializedData, util::UsizeExt},
    memory::{VirtAddr, linker_information::LinkerInformation},
}`
- `alloc::vec::Vec`
- `arch::backtrace::CalleeSavedRegs`
- `sys::klibc::validated_ptr::ValidatedPtr`

**Declarations:**

`enum BacktraceNextError`
> Variants: `RaIsZero`, `CouldNotGetFde`, `RaOutsideText`

`fn is_in_text_segment(address: usize) -> bool`

`struct Backtrace<'a>`
> Fields: `fdes: Vec<eh_frame_parser::ParsedFDE<'a>>`

`static BACKTRACE: RuntimeInitializedData<Backtrace> = RuntimeInitializedData::new()`

**`impl<'a> Backtrace<'a>`**
  `fn new() -> Self`

  `fn find(&self, pc: usize) -> Option<&eh_frame_parser::ParsedFDE<'a>>`

  `fn init(&mut self)`

  `fn next(&self, regs: &mut CalleeSavedRegs) -> Result<usize, BacktraceNextError>`


`const MAX_STACK_SCAN_SLOTS: usize = 512`

`fn scan_stack_for_return_addresses(sp: usize, counter: &mut u64)`

`fn print_stacktrace_frame(counter: u64, address: usize)`

`fn print_uncertain_stacktrace_frame(counter: u64, address: usize)`

`fn print_stacktrace_frame_inner(counter: u64, address: usize, prefix: &str)`

`mod tests`

---

## kernel/src/debugging/eh_frame_parser.rs

**Language:** Rust | **Size:** 21.2 KB | **Lines:** 583

**Imports:**
- `crate::klibc::{
    consumable_buffer::ConsumableBuffer,
    leb128::{SignedLEB128, UnsignedLEB128},
    util::UsizeExt,
}`
- `alloc::{collections::BTreeMap, sync::Arc, vec::Vec}`

**Declarations:**

**`impl<'a> EhFrameParser<'a>`**
  `pub fn new(data: &'a [u8]) -> Self`

  `pub fn iter(&self, base_address: usize) -> EhFrameIterator<'a>`


**`impl ParsedFDE<'_>`**
  `pub fn contains(&self, address: usize) -> bool`

  `fn end_address(&self) -> usize`


**`impl<'a> Iterator for EhFrameIterator<'a>`**
  `fn next(&mut self) -> Option<Self::Item>`


**`impl<'a> EhFrameIterator<'a>`**
  `fn parse_cie(&mut self, length: usize, offset_of_cie: usize) -> Option<()>`

  `fn parse_fde(&mut self, cie_offset: usize, length: usize) -> Option<ParsedFDE<'a>>`

  `fn parse_pc_begin(&mut self, augmentation_byte: u8) -> Option<usize>`

  `fn parse_length_and_bitness(&mut self) -> Option<(usize, Bitness)>`

  `fn parse_instructions(&self, instructions: &[u8]) -> Vec<Instruction>`

  `fn parse_instruction(&self, instructions: &mut ConsumableBuffer) -> Option<Instruction>`


`mod consts`

`mod tests`

---

## kernel/src/debugging/mod.rs

**Language:** Rust | **Size:** 735 B | **Lines:** 27

**Imports:**
- `crate::{cpu::Cpu, info, klibc::sizes::MiB, processes::process_table}`

**Declarations:**

---

## kernel/src/debugging/symbols.rs

**Language:** Rust | **Size:** 2.0 KB | **Lines:** 67

**Imports:**
- `crate::{
    debug, info, klibc::runtime_initialized::RuntimeInitializedData,
    memory::linker_information::LinkerInformation,
}`

**Declarations:**

---

## kernel/src/debugging/unwinder.rs

**Language:** Rust | **Size:** 6.9 KB | **Lines:** 198

**Imports:**
- `crate::{debug, klibc::array_vec::ArrayVec}`
- `super::eh_frame_parser::{Instruction, ParsedFDE}`

**Declarations:**

**`impl<'a> Unwinder<'a>`**
  `pub fn new(fde: &'a ParsedFDE<'a>) -> Self`

  `pub fn find_row_for_address(&self, address: usize) -> &Row`

  `pub fn rows(&self) -> &[Row]`

  `fn last_row(&self) -> Row`

  `fn update_last_row(&mut self, row: &Row)`

  `fn evaluate_instructions(&mut self, instructions: &[Instruction])`


**`impl core::fmt::Display for Row`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl Row`**
  `fn new(address: usize) -> Self`


**`impl PartialEq<gimli::RegisterRule<usize>> for RegisterRule`**
  `fn eq(&self, other: &gimli::RegisterRule<usize>) -> bool`


---

## kernel/src/device_tree.rs

**Language:** Rust | **Size:** 15.2 KB | **Lines:** 493

**Imports:**
- `crate::{
    assert::static_assert_size,
    debug, info,
    klibc::{
        big_endian::BigEndian, consumable_buffer::ConsumableBuffer,
        runtime_initialized::RuntimeInitializedData, util::UsizeExt,
    },
}`
- `core::{
    fmt::{Debug, Display},
    mem::size_of,
    ops::Range,
}`
- `sys::klibc::validated_ptr::ValidatedPtr`

**Declarations:**

`const FDT_MAGIC: u32 = 0xd00dfeed`

`const FDT_VERSION: u32 = 17`

**`impl DeviceTree`**
  `fn new(device_tree_pointer: *const ()) -> Self`

  `fn header(&self) -> &Header`

  `pub fn get_reserved_areas(&self) -> &[ReserveEntry]`

  `pub fn root_node(&self) -> Node<'_>`

  `fn get_string(&self, offset: usize) -> Option<&str>`


**`impl Display for ReserveEntry`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


`const FDT_BEGIN_NODE: u32 = 0x1`

`const FDT_END_NODE: u32 = 0x2`

`const FDT_PROP: u32 = 0x3`

`const FDT_NOP: u32 = 0x4`

`const FDT_END: u32 = 0x9`

**`impl<'a> Node<'a>`**
  `fn new( name: &'a str, device_tree: &'a DeviceTree, structure_block: ConsumableBuffer<'a>, ) -> Self`

  `pub fn find_node(&self, needle: &'a str) -> Option<Self>`

  `fn find_node_recursive(&mut self, needle: &'a str) -> Option<Self>`

  `pub fn get_property(&self, name: &'a str) -> Option<ConsumableBuffer<'a>>`

  `pub fn parse_reg_property(&self) -> Option<Reg>`


**`impl<'a> IntoIterator for &Node<'a>`**
  `fn into_iter(self) -> Self::IntoIter`


**`impl<'a> Iterator for Node<'a>`**
  `fn next(&mut self) -> Option<Self::Item>`


`mod tests`

---

## kernel/src/drivers/bochs_display.rs

**Language:** Rust | **Size:** 3.9 KB | **Lines:** 137

**Imports:**
- `crate::{
    info,
    klibc::{MMIO, mmio},
    pci::{GeneralDevicePciHeaderExt, GeneralDevicePciHeaderFields, PCIDevice, PciCpuAddr},
}`
- `core::sync::atomic::{AtomicUsize, Ordering}`
- `mmio::write_bytes`

**Declarations:**

`const VBE_DISPI_INDEX_XRES: u16 = 1`

`const VBE_DISPI_INDEX_YRES: u16 = 2`

`const VBE_DISPI_INDEX_BPP: u16 = 3`

`const VBE_DISPI_INDEX_ENABLE: u16 = 4`

`const VBE_DISPI_ENABLED: u16 = 0x01`

`const VBE_DISPI_LFB_ENABLED: u16 = 0x40`

`const BOCHS_VENDOR_ID: u16 = 0x1234`

`const BOCHS_DEVICE_ID: u16 = 0x1111`

`static FB_BASE: AtomicUsize = AtomicUsize::new(0)`

`fn write_vbe_reg(dispi_base: usize, index: u16, value: u16)`

---

## kernel/src/drivers/mod.rs

**Language:** Rust | **Size:** 3.1 KB | **Lines:** 87

**Imports:**
- `alloc::vec::Vec`
- `crate::{fs, interrupts::plic, net, pci::PCIDevice, processes::kernel_tasks}`

**Declarations:**

`fn init_network_device(pci_devices: &mut Vec<PCIDevice>)`

`fn init_block_devices(pci_devices: &mut Vec<PCIDevice>)`

`fn init_display_device(pci_devices: &mut Vec<PCIDevice>)`

`fn init_rng_device(pci_devices: &mut Vec<PCIDevice>)`

`fn init_input_device(pci_devices: &mut Vec<PCIDevice>)`

---

## kernel/src/drivers/virtio/block.rs

**Language:** Rust | **Size:** 18.4 KB | **Lines:** 577

**Imports:**
- `alloc::{collections::BTreeMap, vec::Vec}`
- `core::{
    pin::Pin,
    sync::atomic::{AtomicU64, Ordering},
    task::{Context, Poll, Waker},
}`
- `headers::errno::Errno`
- `crate::{
    drivers::virtio::{
        capability::{
            DEVICE_STATUS_ACKNOWLEDGE, DEVICE_STATUS_DRIVER, DEVICE_STATUS_DRIVER_OK,
            DEVICE_STATUS_FAILED, DEVICE_STATUS_FEATURES_OK, VIRTIO_DEVICE_ID, VIRTIO_F_VERSION_1,
            VIRTIO_PCI_CAP_COMMON_CFG, VIRTIO_PCI_CAP_DEVICE_CFG, VIRTIO_PCI_CAP_ISR_CFG,
            VIRTIO_PCI_CAP_NOTIFY_CFG, VIRTIO_VENDOR_ID, VIRTIO_VENDOR_SPECIFIC_CAPABILITY_ID,
            virtio_pci_cap, virtio_pci_capFields, virtio_pci_common_cfg,
            virtio_pci_common_cfgFields, virtio_pci_notify_cap, virtio_pci_notify_capFields,
        },
        virtqueue::{BufferDirection, UsedBuffer, VirtQueue},
    },
    info,
    klibc::{
        MMIO, Spinlock,
        non_empty_vec::NonEmptyVec,
        util::{ByteInterpretable, is_power_of_2_or_zero},
    },
    mmio_struct,
    pci::{
        GeneralDevicePciHeaderExt, GeneralDevicePciHeaderFields, PCIDevice, PciCapabilityFields,
    },
}`

**Declarations:**

`const EXPECTED_QUEUE_SIZE: usize = 0x100`

`const SECTOR_SIZE: usize = 512`

`const VIRTIO_BLOCK_SUBSYSTEM_ID: u16 = 2`

`const VIRTIO_BLK_T_IN: u32 = 0`

`const VIRTIO_BLK_T_OUT: u32 = 1`

`const VIRTIO_BLK_S_OK: u8 = 0`

`struct VirtioBlkReqHeader`
> Fields: `request_type: u32`, `reserved: u32`, `sector: u64`

**`impl ByteInterpretable for VirtioBlkReqHeader`**

`static BLOCK_DEVICES: Spinlock<Vec<BlockDevice>> = Spinlock::new(Vec::new())`

`static BLOCK_ISR_STATUSES: Spinlock<Vec<MMIO<u32>>> = Spinlock::new(Vec::new())`

`static BLOCK_INTERRUPT_COUNTER: AtomicU64 = AtomicU64::new(0)`

`static BLOCK_INTERRUPT_WAKERS: Spinlock<Vec<Waker>> = Spinlock::new(Vec::new())`

`static BLOCK_COMPLETIONS: Spinlock<BTreeMap<u16, UsedBuffer>> = Spinlock::new(BTreeMap::new())`

`struct BlockInterruptWait`
> Fields: `seen_counter: u64`, `registered: bool`

**`impl BlockInterruptWait`**
  `fn new(seen_counter: u64) -> Self`


**`impl Future for BlockInterruptWait`**
  `fn poll(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output>`


`fn harvest_completions(device_index: usize)`

`async fn wait_for_completion(device_index: usize, head_index: u16) -> UsedBuffer`

**`impl BlockDevice`**
  `pub fn is_virtio_block(device: &PCIDevice) -> bool`

  `pub fn initialize(mut pci_device: PCIDevice) -> Result<InitializedBlockDevice, &'static str>`

  `fn capacity_bytes(&self) -> u64`

  `fn submit_read(&mut self, start_sector: u64, buf_len: usize) -> u16`

  `fn submit_write(&mut self, start_sector: u64, data: &[u8]) -> u16`


**`impl Drop for BlockDevice`**
  `fn drop(&mut self)`


---

## kernel/src/drivers/virtio/capability.rs

**Language:** Rust | **Size:** 2.4 KB | **Lines:** 79

**Imports:**
- `crate::mmio_struct`

**Declarations:**

---

## kernel/src/drivers/virtio/input.rs

**Language:** Rust | **Size:** 10.7 KB | **Lines:** 331

**Imports:**
- `alloc::{collections::VecDeque, vec}`
- `crate::{
    drivers::virtio::{
        capability::{
            DEVICE_STATUS_ACKNOWLEDGE, DEVICE_STATUS_DRIVER, DEVICE_STATUS_DRIVER_OK,
            DEVICE_STATUS_FAILED, DEVICE_STATUS_FEATURES_OK, VIRTIO_DEVICE_ID, VIRTIO_F_VERSION_1,
            VIRTIO_PCI_CAP_COMMON_CFG, VIRTIO_PCI_CAP_ISR_CFG, VIRTIO_PCI_CAP_NOTIFY_CFG,
            VIRTIO_VENDOR_ID, VIRTIO_VENDOR_SPECIFIC_CAPABILITY_ID, virtio_pci_cap,
            virtio_pci_capFields, virtio_pci_common_cfg, virtio_pci_common_cfgFields,
            virtio_pci_notify_cap, virtio_pci_notify_capFields,
        },
        virtqueue::{BufferDirection, VirtQueue},
    },
    info,
    klibc::{
        MMIO, Spinlock,
        runtime_initialized::RuntimeInitializedData,
        util::{ByteInterpretable, is_power_of_2_or_zero},
    },
    pci::{
        GeneralDevicePciHeaderExt, GeneralDevicePciHeaderFields, PCIDevice, PciCapabilityFields,
    },
}`

**Declarations:**

`const QUEUE_SIZE: usize = 32`

`const VIRTIO_INPUT_SUBSYSTEM_ID: u16 = 18`

`const EVENT_SIZE: usize = core::mem::size_of::<VirtioInputEvent>()`

`const MAX_BUFFERED_EVENTS: usize = 128`

**`impl ByteInterpretable for VirtioInputEvent`**

`static INPUT_DEVICE: Spinlock<Option<InputDevice>> = Spinlock::new(None)`

`static EVENT_BUFFER: Spinlock<VecDeque<VirtioInputEvent>> = Spinlock::new(VecDeque::new())`

`static ISR_STATUS: RuntimeInitializedData<MMIO<u32>> = RuntimeInitializedData::new()`

**`impl InputDevice`**
  `pub fn is_virtio_input(device: &PCIDevice) -> bool`

  `pub fn initialize(mut pci_device: PCIDevice) -> Result<InitializedInput, &'static str>`

  `fn fill_event_queue(&mut self)`

  `fn process_events(&mut self)`


**`impl Drop for InputDevice`**
  `fn drop(&mut self)`


---

## kernel/src/drivers/virtio/mod.rs

**Language:** Rust | **Size:** 87 B | **Lines:** 6

**Declarations:**

`mod capability`

`mod virtqueue`

---

## kernel/src/drivers/virtio/net/mod.rs

**Language:** Rust | **Size:** 13.7 KB | **Lines:** 424

**Imports:**
- `crate::{
    assert::static_assert_size,
    debug,
    drivers::virtio::{
        capability::{
            DEVICE_STATUS_ACKNOWLEDGE, DEVICE_STATUS_DRIVER, DEVICE_STATUS_DRIVER_OK,
            DEVICE_STATUS_FAILED, DEVICE_STATUS_FEATURES_OK, VIRTIO_DEVICE_ID, VIRTIO_F_VERSION_1,
            VIRTIO_PCI_CAP_COMMON_CFG, VIRTIO_PCI_CAP_DEVICE_CFG, VIRTIO_PCI_CAP_ISR_CFG,
            VIRTIO_PCI_CAP_NOTIFY_CFG, VIRTIO_VENDOR_ID, VIRTIO_VENDOR_SPECIFIC_CAPABILITY_ID,
            virtio_pci_cap, virtio_pci_capFields, virtio_pci_common_cfg,
            virtio_pci_common_cfgFields, virtio_pci_notify_cap, virtio_pci_notify_capFields,
        },
        virtqueue::{BufferDirection, VirtQueue},
    },
    info,
    klibc::{
        MMIO,
        util::{BufferExtension, ByteInterpretable, is_power_of_2_or_zero},
    },
    mmio_struct,
    net::mac::MacAddress,
    pci::{
        GeneralDevicePciHeaderExt, GeneralDevicePciHeaderFields, PCIAllocatedSpace, PCIDevice,
        PciCapabilityFields,
    },
}`
- `alloc::vec::Vec`
- `super::virtqueue::QueueError`

**Declarations:**

`const EXPECTED_QUEUE_SIZE: usize = 0x100`

`const VIRTIO_NET_F_MAC: u64 = 1 << 5`

`const VIRTIO_NETWORK_SUBSYSTEM_ID: u16 = 1`

**`impl NetworkDevice`**
  `pub fn is_virtio_net(device: &PCIDevice) -> bool`

  `pub fn initialize(mut pci_device: PCIDevice) -> Result<InitializedNetworkDevice, &'static str>`

  `fn reset_and_acknowledge(common_cfg: &MMIO<virtio_pci_common_cfg>)`

  `fn negotiate_features(common_cfg: &MMIO<virtio_pci_common_cfg>)`

  `fn setup_virtqueues( common_cfg: &MMIO<virtio_pci_common_cfg>, notify_cfg: &MMIO<virtio_pci_notify_cap>, notify_bar: &PCIAllocatedSpace, ) -> ( VirtQueue<EXPECTED_QUEUE_SIZE>, VirtQueue<EXPECTED_QUEUE_SIZE>, )`

  `fn configure_queue_on_device( common_cfg: &MMIO<virtio_pci_common_cfg>, queue: &VirtQueue<EXPECTED_QUEUE_SIZE>, index: u16, )`

  `fn fill_receive_buffers(receive_queue: &mut VirtQueue<EXPECTED_QUEUE_SIZE>)`

  `pub fn receive_packets(&mut self) -> Vec<Vec<u8>>`

  `pub fn send_packet(&mut self, data: Vec<u8>) -> Result<u16, QueueError>`

  `pub fn get_mac_address(&self) -> MacAddress`


**`impl Drop for NetworkDevice`**
  `fn drop(&mut self)`


`const VIRTIO_NET_HDR_GSO_NONE: u8 = 0`

`struct virtio_net_hdr`
> Fields: `flags: u8`, `gso_type: u8`, `hdr_len: u16`, `gso_size: u16`, `csum_start: u16`, `csum_offset: u16`, `num_buffers: u16`

**`impl ByteInterpretable for virtio_net_hdr`**

---

## kernel/src/drivers/virtio/rng.rs

**Language:** Rust | **Size:** 8.8 KB | **Lines:** 261

**Imports:**
- `alloc::vec`
- `crate::{
    drivers::virtio::{
        capability::{
            DEVICE_STATUS_ACKNOWLEDGE, DEVICE_STATUS_DRIVER, DEVICE_STATUS_DRIVER_OK,
            DEVICE_STATUS_FAILED, DEVICE_STATUS_FEATURES_OK, VIRTIO_DEVICE_ID, VIRTIO_F_VERSION_1,
            VIRTIO_PCI_CAP_COMMON_CFG, VIRTIO_PCI_CAP_NOTIFY_CFG, VIRTIO_VENDOR_ID,
            VIRTIO_VENDOR_SPECIFIC_CAPABILITY_ID, virtio_pci_cap, virtio_pci_capFields,
            virtio_pci_common_cfg, virtio_pci_common_cfgFields, virtio_pci_notify_cap,
            virtio_pci_notify_capFields,
        },
        virtqueue::{BufferDirection, VirtQueue},
    },
    info,
    klibc::{MMIO, Spinlock, util::is_power_of_2_or_zero},
    pci::{
        GeneralDevicePciHeaderExt, GeneralDevicePciHeaderFields, PCIDevice, PciCapabilityFields,
    },
}`

**Declarations:**

`const QUEUE_SIZE: usize = 0x10`

`const VIRTIO_RNG_SUBSYSTEM_ID: u16 = 4`

`static RNG_DEVICE: Spinlock<Option<RngDevice>> = Spinlock::new(None)`

**`impl RngDevice`**
  `pub fn is_virtio_rng(device: &PCIDevice) -> bool`

  `pub fn initialize(mut pci_device: PCIDevice) -> Result<RngDevice, &'static str>`

  `fn read(&mut self, buf: &mut [u8])`


**`impl Drop for RngDevice`**
  `fn drop(&mut self)`


---

## kernel/src/drivers/virtio/virtqueue.rs

**Language:** Rust | **Size:** 10.3 KB | **Lines:** 325

**Imports:**
- `alloc::{boxed::Box, collections::BTreeMap, vec::Vec}`
- `crate::{
    debug,
    klibc::{MMIO, non_empty_vec::NonEmptyVec},
}`
- `sys::klibc::deconstructed_vec::DeconstructedVec`

**Declarations:**

**`impl<const QUEUE_SIZE: usize> VirtQueue<QUEUE_SIZE>`**
  `pub fn new(queue_size: u16, queue_index: u16) -> Self`

  `pub fn set_notify(&mut self, notify: MMIO<u16>)`

  `pub fn descriptor_area_physical_address(&self) -> u64`

  `pub fn driver_area_physical_address(&self) -> u64`

  `pub fn device_area_physical_address(&self) -> u64`

  `pub fn put_buffer( &mut self, buffer: Vec<u8>, direction: BufferDirection, ) -> Result<u16, QueueError>`

  `pub fn put_buffer_chain( &mut self, buffers: NonEmptyVec<(Vec<u8>, BufferDirection)>, ) -> Result<u16, QueueError>`

  `pub fn receive_buffer(&mut self) -> Vec<UsedBuffer>`

  `pub fn enable_interrupts(&mut self)`

  `pub fn notify(&mut self)`


`const VIRTQ_DESC_F_NEXT: u16 = 1`

`const VIRTQ_DESC_F_WRITE: u16 = 2`

`const VIRTQ_DESC_F_INDIRECT: u16 = 4`

`struct virtq_desc`
> Fields: `addr: u64`, `len: u32`, `flags: u16`, `next: u16`

`const VIRTQ_AVAIL_F_NO_INTERRUPT: u16 = 1`

`struct virtq_avail<const QUEUE_SIZE: usize>`
> Fields: `flags: u16`, `idx: u16`, `ring: [u16; QUEUE_SIZE]`, `used_event: u16`

**`impl<const QUEUE_SIZE: usize> Default for virtq_avail<QUEUE_SIZE>`**
  `fn default() -> Self`


`const VIRTQ_USED_F_NO_NOTIFY: u16 = 1`

`struct virtq_used<const QUEUE_SIZE: usize>`
> Fields: `flags: u16`, `idx: u16`, `ring: [virtq_used_elem; QUEUE_SIZE]`, `avail_event: u16`

**`impl<const QUEUE_SIZE: usize> Default for virtq_used<QUEUE_SIZE>`**
  `fn default() -> Self`


`struct virtq_used_elem`
> Fields: `id: u32`, `len: u32`

---

## kernel/src/fs/devfs.rs

**Language:** Rust | **Size:** 4.0 KB | **Lines:** 188

**Imports:**
- `alloc::{collections::BTreeMap, string::String, sync::Arc, vec::Vec}`
- `headers::errno::Errno`
- `crate::{
    io::tty_device::{TtyDevice, console_tty},
    klibc::Spinlock,
}`
- `super::vfs::{DirEntry, NodeType, VfsNode, VfsNodeRef, alloc_ino}`

**Declarations:**

`struct DevNull`
> Fields: `ino: u64`

**`impl VfsNode for DevNull`**
  `fn node_type(&self) -> NodeType`

  `fn ino(&self) -> u64`

  `fn size(&self) -> usize`

  `fn read(&self, _offset: usize, _buf: &mut [u8]) -> Result<usize, Errno>`

  `fn write(&self, _offset: usize, data: &[u8]) -> Result<usize, Errno>`

  `fn truncate(&self, _length: usize) -> Result<(), Errno>`


`struct DevZero`
> Fields: `ino: u64`

**`impl VfsNode for DevZero`**
  `fn node_type(&self) -> NodeType`

  `fn ino(&self) -> u64`

  `fn size(&self) -> usize`

  `fn read(&self, _offset: usize, buf: &mut [u8]) -> Result<usize, Errno>`

  `fn write(&self, _offset: usize, data: &[u8]) -> Result<usize, Errno>`

  `fn truncate(&self, _length: usize) -> Result<(), Errno>`


`struct DevConsole`
> Fields: `ino: u64`, `device: TtyDevice`

**`impl VfsNode for DevConsole`**
  `fn node_type(&self) -> NodeType`

  `fn ino(&self) -> u64`

  `fn size(&self) -> usize`

  `fn read(&self, _offset: usize, buf: &mut [u8]) -> Result<usize, Errno>`

  `fn write(&self, _offset: usize, data: &[u8]) -> Result<usize, Errno>`

  `fn truncate(&self, _length: usize) -> Result<(), Errno>`


`struct DevfsDir`
> Fields: `ino: u64`, `entries: Spinlock<BTreeMap<String, VfsNodeRef>>`

**`impl VfsNode for DevfsDir`**
  `fn node_type(&self) -> NodeType`

  `fn ino(&self) -> u64`

  `fn size(&self) -> usize`

  `fn lookup(&self, name: &str) -> Result<VfsNodeRef, Errno>`

  `fn readdir(&self) -> Result<Vec<DirEntry>, Errno>`


`static DEVFS: Spinlock<Option<Arc<DevfsDir>>> = Spinlock::new(None)`

---

## kernel/src/fs/ext2/dir.rs

**Language:** Rust | **Size:** 1.4 KB | **Lines:** 61

**Imports:**
- `alloc::{collections::BTreeMap, string::String, sync::Arc, vec::Vec}`
- `headers::errno::Errno`
- `crate::fs::vfs::{DirEntry, NodeType, VfsNode, VfsNodeRef}`

**Declarations:**

**`impl Ext2Dir`**
  `pub fn new(ino: u64, children: BTreeMap<String, VfsNodeRef>) -> Arc<Self>`


**`impl VfsNode for Ext2Dir`**
  `fn node_type(&self) -> NodeType`

  `fn ino(&self) -> u64`

  `fn size(&self) -> usize`

  `fn lookup(&self, name: &str) -> Result<VfsNodeRef, Errno>`

  `fn readdir(&self) -> Result<Vec<DirEntry>, Errno>`

  `fn create(&self, _name: &str, _node_type: NodeType) -> Result<VfsNodeRef, Errno>`

  `fn unlink(&self, _name: &str) -> Result<(), Errno>`

  `fn read(&self, _offset: usize, _buf: &mut [u8]) -> Result<usize, Errno>`

  `fn write(&self, _offset: usize, _data: &[u8]) -> Result<usize, Errno>`


---

## kernel/src/fs/ext2/file.rs

**Language:** Rust | **Size:** 1.1 KB | **Lines:** 52

**Imports:**
- `alloc::{sync::Arc, vec::Vec}`
- `headers::errno::Errno`
- `crate::fs::vfs::{NodeType, VfsNode}`

**Declarations:**

**`impl Ext2File`**
  `pub fn new(ino: u64, data: Vec<u8>, file_size: usize) -> Arc<Self>`


**`impl VfsNode for Ext2File`**
  `fn node_type(&self) -> NodeType`

  `fn ino(&self) -> u64`

  `fn size(&self) -> usize`

  `fn read(&self, offset: usize, buf: &mut [u8]) -> Result<usize, Errno>`

  `fn write(&self, _offset: usize, _data: &[u8]) -> Result<usize, Errno>`

  `fn truncate(&self, _length: usize) -> Result<(), Errno>`


---

## kernel/src/fs/ext2/inode.rs

**Language:** Rust | **Size:** 4.9 KB | **Lines:** 176

**Imports:**
- `alloc::{vec, vec::Vec}`
- `crate::{drivers::virtio::block, klibc::util::BufferExtension}`
- `super::structures::{
    EXT2_DIND_BLOCK, EXT2_IND_BLOCK, EXT2_NDIR_BLOCKS, EXT2_TIND_BLOCK, Ext2BlockGroupDescriptor,
    Ext2Inode, Ext2Superblock,
}`

**Declarations:**

`async fn read_block_data( dev: usize, block_num: u32, block_size: usize, remaining: usize, data: &mut Vec<u8>, )`

`async fn read_block_pointers(dev: usize, block_num: u32, block_size: usize) -> Vec<u32>`

`async fn read_indirect( dev: usize, indirect_block: u32, block_size: usize, file_size: usize, data: &mut Vec<u8>, )`

`async fn read_doubly_indirect( dev: usize, dind_block: u32, block_size: usize, file_size: usize, data: &mut Vec<u8>, )`

`async fn read_triply_indirect( dev: usize, tind_block: u32, block_size: usize, file_size: usize, data: &mut Vec<u8>, )`

---

## kernel/src/fs/ext2/mod.rs

**Language:** Rust | **Size:** 4.9 KB | **Lines:** 153

**Imports:**
- `alloc::{boxed::Box, collections::BTreeMap, string::String, vec}`
- `crate::{
    drivers::virtio::block,
    fs::vfs::{self, VfsNodeRef, alloc_ino},
    info,
    klibc::util::BufferExtension,
    warn,
}`
- `dir::Ext2Dir`
- `file::Ext2File`
- `inode::{read_inode, read_inode_data}`
- `structures::{
    EXT2_FT_DIR, EXT2_FT_REG_FILE, EXT2_MAGIC, EXT2_ROOT_INODE, Ext2BlockGroupDescriptor,
    Ext2DirEntry, Ext2Superblock,
}`

**Declarations:**

`mod dir`

`mod file`

`mod inode`

`async fn read_superblock(dev: usize) -> Ext2Superblock`

`async fn read_block_group_descriptors( dev: usize, sb: &Ext2Superblock, ) -> alloc::vec::Vec<Ext2BlockGroupDescriptor>`

`fn build_tree<'a>( dev: usize, sb: &'a Ext2Superblock, bgds: &'a [Ext2BlockGroupDescriptor], inode_number: u32, ) -> core::pin::Pin<Box<dyn Future<Output = VfsNodeRef> + Send + 'a>>`

`fn parse_dir_entries(data: &[u8]) -> alloc::vec::Vec<(String, u32, u8)>`

---

## kernel/src/fs/ext2/structures.rs

**Language:** Rust | **Size:** 2.7 KB | **Lines:** 120

**Declarations:**

**`impl Ext2Superblock`**
  `pub fn block_size(&self) -> usize`

  `pub fn inode_size(&self) -> usize`

  `pub fn num_block_groups(&self) -> u32`


**`impl Ext2Inode`**
  `pub fn is_dir(&self) -> bool`

  `pub fn is_regular(&self) -> bool`


---

## kernel/src/fs/mod.rs

**Language:** Rust | **Size:** 477 B | **Lines:** 20

**Imports:**
- `pub use open_file::VfsOpenFile`
- `pub use vfs::{
    resolve_parent, resolve_path, resolve_path_nofollow, resolve_relative, stat_from_node,
    statx_from_node,
}`

**Declarations:**

`mod procfs`

`pub(crate) mod tmpfs`

---

## kernel/src/fs/open_file.rs

**Language:** Rust | **Size:** 2.7 KB | **Lines:** 103

**Imports:**
- `alloc::sync::Arc`
- `headers::errno::Errno`
- `crate::klibc::Spinlock`
- `super::vfs::{NodeType, VfsNodeRef}`

**Declarations:**

**`impl VfsOpenFileInner`**
  `pub fn node(&self) -> &VfsNodeRef`

  `pub fn read(&mut self, buf: &mut [u8]) -> Result<usize, Errno>`

  `pub fn pread(&self, offset: usize, buf: &mut [u8]) -> Result<usize, Errno>`

  `pub fn pwrite(&self, offset: usize, data: &[u8]) -> Result<usize, Errno>`

  `pub fn write(&mut self, data: &[u8]) -> Result<usize, Errno>`

  `pub fn seek(&mut self, offset: isize, whence: i32) -> Result<usize, Errno>`

  `pub fn offset(&self) -> usize`

  `pub fn advance_offset(&mut self, n: usize)`

  `pub fn effective_write_offset(&mut self) -> usize`

  `pub fn is_directory(&self) -> bool`


---

## kernel/src/fs/procfs.rs

**Language:** Rust | **Size:** 1.0 KB | **Lines:** 45

**Imports:**
- `alloc::sync::Arc`
- `headers::errno::Errno`
- `super::vfs::{NodeType, StaticDir, VfsNode, VfsNodeRef, alloc_ino}`

**Declarations:**

`struct ProcVersionFile`
> Fields: `ino: u64`

`const VERSION_STRING: &[u8] = b"Solaya 0.1.0\n"`

**`impl VfsNode for ProcVersionFile`**
  `fn node_type(&self) -> NodeType`

  `fn ino(&self) -> u64`

  `fn size(&self) -> usize`

  `fn read(&self, offset: usize, buf: &mut [u8]) -> Result<usize, Errno>`

  `fn write(&self, _offset: usize, _data: &[u8]) -> Result<usize, Errno>`


---

## kernel/src/fs/tmpfs.rs

**Language:** Rust | **Size:** 6.0 KB | **Lines:** 269

**Imports:**
- `alloc::{
    collections::BTreeMap,
    string::{String, ToString},
    sync::Arc,
    vec::Vec,
}`
- `headers::errno::Errno`
- `crate::klibc::Spinlock`
- `super::vfs::{DirEntry, NodeType, VfsNode, VfsNodeRef, alloc_ino}`

**Declarations:**

`struct TmpfsMetadata`
> Fields: `mode: u32`, `uid: u32`, `gid: u32`, `nlink: u32`

**`impl TmpfsFile`**
  `fn new() -> Arc<Self>`


**`impl VfsNode for TmpfsFile`**
  `fn node_type(&self) -> NodeType`

  `fn ino(&self) -> u64`

  `fn size(&self) -> usize`

  `fn mode(&self) -> u32`

  `fn uid(&self) -> u32`

  `fn gid(&self) -> u32`

  `fn nlink(&self) -> u32`

  `fn set_mode(&self, mode: u32) -> Result<(), Errno>`

  `fn set_owner(&self, uid: u32, gid: u32) -> Result<(), Errno>`

  `fn inc_nlink(&self)`

  `fn dec_nlink(&self)`

  `fn read(&self, offset: usize, buf: &mut [u8]) -> Result<usize, Errno>`

  `fn write(&self, offset: usize, data: &[u8]) -> Result<usize, Errno>`

  `fn truncate(&self, length: usize) -> Result<(), Errno>`


**`impl TmpfsDir`**
  `pub fn new() -> Arc<Self>`


**`impl VfsNode for TmpfsDir`**
  `fn node_type(&self) -> NodeType`

  `fn ino(&self) -> u64`

  `fn size(&self) -> usize`

  `fn mode(&self) -> u32`

  `fn uid(&self) -> u32`

  `fn gid(&self) -> u32`

  `fn nlink(&self) -> u32`

  `fn set_mode(&self, mode: u32) -> Result<(), Errno>`

  `fn set_owner(&self, uid: u32, gid: u32) -> Result<(), Errno>`

  `fn lookup(&self, name: &str) -> Result<VfsNodeRef, Errno>`

  `fn create(&self, name: &str, node_type: NodeType) -> Result<VfsNodeRef, Errno>`

  `fn unlink(&self, name: &str) -> Result<(), Errno>`

  `fn link(&self, name: &str, node: VfsNodeRef) -> Result<(), Errno>`

  `fn remove_child(&self, name: &str) -> Result<VfsNodeRef, Errno>`

  `fn readdir(&self) -> Result<Vec<DirEntry>, Errno>`


**`impl TmpfsSymlink`**
  `pub fn new(target: String) -> Arc<Self>`


**`impl VfsNode for TmpfsSymlink`**
  `fn node_type(&self) -> NodeType`

  `fn ino(&self) -> u64`

  `fn size(&self) -> usize`

  `fn nlink(&self) -> u32`

  `fn readlink(&self) -> Result<String, Errno>`


---

## kernel/src/fs/vfs.rs

**Language:** Rust | **Size:** 10.9 KB | **Lines:** 435

**Imports:**
- `alloc::{
    collections::BTreeMap,
    string::{String, ToString},
    sync::Arc,
    vec::Vec,
}`
- `core::sync::atomic::{AtomicU64, Ordering}`
- `headers::errno::Errno`
- `crate::klibc::Spinlock`

**Declarations:**

`fn programs() -> &'static [(&'static str, &'static [u8])]`

`fn programs() -> &'static [(&'static str, &'static [u8])]`

`static NEXT_INO: AtomicU64 = AtomicU64::new(1)`

**`impl NodeType`**
  `pub fn stat_mode(self) -> u32`


`static MOUNT_TABLE: Spinlock<BTreeMap<String, VfsNodeRef>> = Spinlock::new(BTreeMap::new())`

`fn resolve_path_with_depth(path: &str, depth: u32) -> Result<VfsNodeRef, Errno>`

`fn find_mount<'a>( table: &'a BTreeMap<String, VfsNodeRef>, path: &str, ) -> Result<(&'a str, VfsNodeRef), Errno>`

`const MAX_SYMLINK_DEPTH: u32 = 8`

`fn walk_with_depth(mut node: VfsNodeRef, path: &str, mut depth: u32) -> Result<VfsNodeRef, Errno>`

**`impl StaticDir`**
  `pub fn new(entries: Vec<(&str, VfsNodeRef)>) -> Arc<Self>`


**`impl VfsNode for StaticDir`**
  `fn node_type(&self) -> NodeType`

  `fn ino(&self) -> u64`

  `fn size(&self) -> usize`

  `fn lookup(&self, name: &str) -> Result<VfsNodeRef, Errno>`

  `fn readdir(&self) -> Result<Vec<DirEntry>, Errno>`


**`impl RootDir`**
  `pub fn new() -> Arc<Self>`


**`impl VfsNode for RootDir`**
  `fn node_type(&self) -> NodeType`

  `fn ino(&self) -> u64`

  `fn size(&self) -> usize`

  `fn lookup(&self, name: &str) -> Result<VfsNodeRef, Errno>`

  `fn readdir(&self) -> Result<Vec<DirEntry>, Errno>`


`fn program_ino(name: &str) -> Option<u64>`

`struct ProgramNode`
> Fields: `ino: u64`

**`impl VfsNode for ProgramNode`**
  `fn node_type(&self) -> NodeType`

  `fn ino(&self) -> u64`

  `fn size(&self) -> usize`


---

## kernel/src/interrupts/mod.rs

**Language:** Rust | **Size:** 28 B | **Lines:** 2

**Declarations:**

---

## kernel/src/interrupts/plic.rs

**Language:** Rust | **Size:** 2.8 KB | **Lines:** 97

**Imports:**
- `crate::{
    info,
    klibc::{MMIO, Spinlock, runtime_initialized::RuntimeInitializedData},
}`
- `alloc::vec::Vec`
- `arch::CpuId`

**Declarations:**

`struct InterruptHandler`
> Fields: `irq: u32`, `handler: fn()`

`static INTERRUPT_HANDLERS: Spinlock<Vec<InterruptHandler>> = Spinlock::new(Vec::new())`

**`impl Plic`**
  `fn new(plic_base: usize, cpu_id: CpuId) -> Self`

  `fn enable(&mut self, interrupt_id: u32)`

  `fn set_priority(&mut self, interrupt_id: u32, priority: u32)`

  `fn set_threshold(&mut self, threshold: u32)`

  `pub fn claim(&mut self) -> Option<u32>`

  `pub fn complete(&mut self, irq: u32)`


---

## kernel/src/interrupts/trap.rs

**Language:** Rust | **Size:** 11.3 KB | **Lines:** 304

**Imports:**
- `crate::{
    cpu::Cpu,
    debug, info,
    interrupts::plic::{self, PLIC},
    memory::VirtAddr,
    processes::{task::Task, thread::ThreadState, timer, waker::ThreadWaker},
    syscalls::linux::LinuxSyscallHandler,
}`
- `arch::trap_cause::{
    InterruptCause,
    exception::{ENVIRONMENT_CALL_FROM_U_MODE, STORE_AMO_PAGE_FAULT},
    interrupt,
}`
- `common::syscalls::trap_frame::{Register, TrapFrame}`
- `core::{
    panic,
    task::{Context, Poll},
}`
- `headers::syscall_types::SIGSEGV`

**Declarations:**

`fn assert_sepc_not_in_trap_handler()`

`fn handle_timer_interrupt()`

`fn handle_external_interrupt()`

`fn check_thread_ownership_and_reschedule_if_needed(trap_frame: TrapFrame) -> bool`

`fn handle_syscall()`

`fn handle_unhandled_exception()`

`fn handle_store_page_fault()`

`fn handle_exception()`

`fn handle_supervisor_software_interrupt()`

`fn handle_unimplemented()`

---

## kernel/src/io/mod.rs

**Language:** Rust | **Size:** 98 B | **Lines:** 5

**Declarations:**

---

## kernel/src/io/pipe.rs

**Language:** Rust | **Size:** 5.4 KB | **Lines:** 229

**Imports:**
- `crate::klibc::Spinlock`
- `alloc::{collections::VecDeque, sync::Arc, vec::Vec}`
- `core::{
    cmp::min,
    future::Future,
    pin::Pin,
    task::{Context, Poll, Waker},
}`
- `headers::errno::Errno`

**Declarations:**

**`impl PipeInner`**
  `fn new() -> Self`

  `fn read_closed(&self) -> bool`

  `fn write_closed(&self) -> bool`

  `pub fn write(&mut self, data: &[u8]) -> Result<usize, Errno>`

  `pub fn try_read(&mut self, count: usize) -> Result<Vec<u8>, Errno>`

  `fn add_reader(&mut self)`

  `fn add_writer(&mut self)`

  `fn close_write(&mut self)`

  `fn close_read(&mut self)`

  `fn wake_readers(&mut self)`


**`impl PipeReader`**
  `pub fn shared_buffer(&self) -> SharedPipeBuffer`


**`impl Clone for PipeReader`**
  `fn clone(&self) -> Self`


**`impl Drop for PipeReader`**
  `fn drop(&mut self)`


**`impl PipeWriter`**
  `pub fn shared_buffer(&self) -> SharedPipeBuffer`


**`impl Clone for PipeWriter`**
  `fn clone(&self) -> Self`


**`impl Drop for PipeWriter`**
  `fn drop(&mut self)`


**`impl ReadPipe`**
  `pub fn new(buffer: SharedPipeBuffer, max_count: usize) -> Self`


**`impl Future for ReadPipe`**
  `fn poll(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output>`


`mod tests`

---

## kernel/src/io/tty_device.rs

**Language:** Rust | **Size:** 16.4 KB | **Lines:** 546

**Imports:**
- `alloc::{collections::VecDeque, sync::Arc, vec::Vec}`
- `common::pid::Tid`
- `core::{cmp::min, task::Waker}`
- `core::{
    pin::Pin,
    task::{Context, Poll},
}`
- `headers::errno::Errno`
- `headers::syscall_types::SIGTTIN`
- `headers::syscall_types::{
    CLOCAL, CREAD, CS8, ECHO, ECHOE, ICANON, ICRNL, ISIG, NCCS, ONLCR, OPOST, VEOF, VERASE, VINTR,
    VKILL, VMIN, VQUIT, VSUSP, VTIME, termios,
}`
- `crate::klibc::{Spinlock, array_vec::ArrayVec, runtime_initialized::RuntimeInitializedData}`
- `crate::processes::{process::ProcessRef, process_table}`

**Declarations:**

**`impl TtyDeviceInner`**
  `pub fn new() -> Self`

  `pub fn get_termios(&self) -> termios`

  `pub fn set_termios(&mut self, new: termios)`

  `pub fn fg_pgid(&self) -> Tid`

  `pub fn set_fg_pgid(&mut self, pgid: Tid)`

  `pub fn record_tty_signal(&mut self, sig: u32, target_pgid: Tid)`

  `pub fn take_signal_for_new_fg(&mut self, caller_pgid: Tid) -> Option<u32>`

  `fn has_lflag(&self, flag: u32) -> bool`

  `fn has_iflag(&self, flag: u32) -> bool`

  `fn has_oflag(&self, flag: u32) -> bool`

  `fn echo_newline(&self, echo: &mut ArrayVec<u8, 192>)`

  `pub fn process_output(&self, data: &[u8]) -> Vec<u8>`

  `pub fn process_input_byte(&mut self, mut byte: u8) -> InputResult`

  `fn flush_line_buf(&mut self)`

  `fn push_input(&mut self, byte: u8)`

  `fn wake_all(&mut self)`

  `pub fn get_input(&mut self, count: usize) -> Vec<u8>`

  `pub fn is_input_empty(&self) -> bool`

  `fn register_wakeup(&mut self, waker: Waker)`

  `fn vmin(&self) -> usize`


**`impl ReadTty`**
  `pub fn new(device: TtyDevice, max_count: usize, process: ProcessRef, tid: Tid) -> Self`


**`impl Future for ReadTty`**
  `fn poll(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output>`


`mod tests`

---

## kernel/src/io/uart.rs

**Language:** Rust | **Size:** 3.3 KB | **Lines:** 129

**Imports:**
- `core::fmt::Write`
- `crate::{
    klibc::{MMIO, Spinlock},
    mmio_struct,
}`
- `sys::klibc::send_sync::UnsafeSendSync`

**Declarations:**

`const LCR_WORD_LEN_8BIT: u8 = 0b11`

`const LCR_DLAB: u8 = 1 << 7`

`const FCR_ENABLE: u8 = 1`

`const IER_RX_AVAILABLE: u8 = 1`

`const LSR_DATA_READY: u8 = 1`

`const BAUD_DIVISOR: u16 = 592`

**`impl Uart`**
  `const fn new(uart_base_address: usize) -> Self`

  `pub fn init(&mut self)`

  `pub fn write_byte(&mut self, character: u8)`

  `pub fn read(&self) -> Option<u8>`


**`impl Write for Uart`**
  `fn write_str(&mut self, s: &str) -> core::fmt::Result`


---

## kernel/src/klibc/big_endian.rs

**Language:** Rust | **Size:** 2.6 KB | **Lines:** 99

**Imports:**
- `core::fmt::{Debug, Display}`
- `super::consumable_buffer::FromU8Buffer`
- `common::numbers::Number`

**Declarations:**

**`impl<T: Number> BigEndian<T>`**
  `pub fn from_big_endian(value: T) -> Self`

  `pub fn from_little_endian(value: T) -> Self`

  `pub fn get(&self) -> T`


**`impl<T: Number> FromU8Buffer for BigEndian<T>`**
  `fn from_u8_buffer(buffer: &[u8]) -> Self`


**`impl<T: Number> Debug for BigEndian<T>`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl<T: Number> Display for BigEndian<T>`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


`mod tests`

---

## kernel/src/klibc/btreemap.rs

**Language:** Rust | **Size:** 2.3 KB | **Lines:** 93

**Imports:**
- `core::borrow::Borrow`
- `alloc::collections::BTreeMap`

**Declarations:**

**`impl<K, V> SplitOffLowerThan<K, V> for BTreeMap<K, V>`**
  `fn split_off_lower_than<Q: ?Sized + Ord>(&mut self, key: &Q) -> BTreeMap<K, V> where K: Borrow<Q> + Ord,`


`mod tests`

---

## kernel/src/klibc/consumable_buffer.rs

**Language:** Rust | **Size:** 5.9 KB | **Lines:** 228

**Imports:**
- `super::util::align_up`

**Declarations:**

**`impl<'a> ConsumableBuffer<'a>`**
  `pub fn new(buffer: &'a [u8]) -> Self`

  `pub fn buffer(&self) -> &'a [u8]`

  `pub fn reset(&mut self)`

  `pub fn reset_and_clone(&self) -> Self`

  `pub fn consume_slice(&mut self, size: usize) -> Option<&'a [u8]>`

  `pub fn consume_sized_type<T: FromU8Buffer>(&mut self) -> Option<T>`

  `pub fn consume_unsized_type<T: FromU8BufferUnsized>(&mut self) -> Option<T>`

  `pub fn consume_alignment(&mut self, alignment: usize) -> Option<()>`

  `pub fn consume_str(&mut self) -> Option<&'a str>`

  `pub fn empty(&self) -> bool`

  `pub fn size_left(&self) -> usize`

  `pub fn position(&self) -> usize`

  `pub fn rest(&self) -> &[u8]`


**`impl<T: common::numbers::Number> FromU8Buffer for T`**
  `fn from_u8_buffer(buffer: &[u8]) -> Self`


`mod tests`

---

## kernel/src/klibc/elf.rs

**Language:** Rust | **Size:** 15.6 KB | **Lines:** 535

**Imports:**
- `super::big_endian::BigEndian`
- `crate::{assert::static_assert_size, debug, klibc::util::UsizeExt}`

**Declarations:**

`const ELF_MAGIC_NUMBER: u32 = 0x7f454c46`

`struct ElfSectionHeader`
> Fields: `sh_name: u32`, `sh_type: u32`, `sh_flags: u64`, `sh_addr: u64`, `sh_offset: u64`, `sh_size: u64`, `sh_link: u32`, `sh_info: u32`, `sh_addralign: u64`, `sh_entsize: u64`

`struct Elf64Sym`
> Fields: `st_name: u32`, `st_info: u8`, `st_other: u8`, `st_shndx: u16`, `st_value: u64`, `st_size: u64`

`const SHT_SYMTAB: u32 = 2`

`const STT_FUNC: u8 = 2`

**`impl<'a> ElfFile<'a>`**
  `pub fn parse(data: &'a [u8]) -> Result<Self, ElfParseErrors>`

  `pub fn get_header(&self) -> &ElfHeader`

  `pub fn get_program_headers(&self) -> &[ElfProgramHeaderEntry]`

  `pub fn get_program_header_data(&self, program_header: &ElfProgramHeaderEntry) -> &[u8]`

  `fn get_section_headers(&self) -> &[ElfSectionHeader]`

  `fn get_section_name(&self, section: &ElfSectionHeader) -> Option<&'a str>`

  `pub fn find_section_data_by_name(&self, name: &str) -> Option<(&'a [u8], usize)>`

  `pub fn find_symbol(&self, address: usize) -> Option<(&str, usize)>`

  `fn check_validity(data: &[u8]) -> Option<ElfParseErrors>`


`mod tests`

---

## kernel/src/klibc/leb128.rs

**Language:** Rust | **Size:** 2.2 KB | **Lines:** 99

**Imports:**
- `super::consumable_buffer::{ConsumableBuffer, FromU8BufferUnsized}`

**Declarations:**

**`impl UnsignedLEB128`**
  `pub fn get(self) -> u64`

  `fn parse(buffer: &[u8]) -> Option<Self>`


**`impl FromU8BufferUnsized for UnsignedLEB128`**
  `fn from_u8_buffer(buffer: &[u8]) -> Option<Self>`

  `fn size_in_bytes(&self) -> usize`


**`impl SignedLEB128`**
  `pub fn get(&self) -> i64`

  `fn parse(buffer: &[u8]) -> Option<Self>`


**`impl FromU8BufferUnsized for SignedLEB128`**
  `fn from_u8_buffer(buffer: &[u8]) -> Option<Self>`

  `fn size_in_bytes(&self) -> usize`


---

## kernel/src/klibc/mmio.rs

**Language:** Rust | **Size:** 1.1 KB | **Lines:** 35

**Imports:**
- `pub use sys::klibc::mmio::{MMIO, read_bytes, write_bytes}`

---

## kernel/src/klibc/mod.rs

**Language:** Rust | **Size:** 329 B | **Lines:** 14

**Imports:**
- `pub use sys::klibc::array_vec`
- `pub use sys::klibc::{runtime_initialized, sizes}`
- `pub use mmio::MMIO`
- `pub use sys::klibc::spinlock::{Spinlock, SpinlockGuard}`

**Declarations:**

---

## kernel/src/klibc/non_empty_vec.rs

**Language:** Rust | **Size:** 2.3 KB | **Lines:** 104

**Imports:**
- `alloc::vec::Vec`
- `core::{fmt, ops::Index}`

**Declarations:**

**`impl<T> NonEmptyVec<T>`**
  `pub fn new(first: T) -> Self`

  `pub fn push(mut self, item: T) -> Self`

  `pub fn len(&self) -> usize`

  `pub fn into_first(self) -> T`

  `pub fn iter(&self) -> impl Iterator<Item = &T>`


**`impl<T> IntoIterator for NonEmptyVec<T>`**
  `fn into_iter(self) -> Self::IntoIter`


**`impl<T> Index<usize> for NonEmptyVec<T>`**
  `fn index(&self, index: usize) -> &Self::Output`


**`impl<T: fmt::Debug> fmt::Debug for NonEmptyVec<T>`**
  `fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result`


`mod tests`

---

## kernel/src/klibc/util.rs

**Language:** Rust | **Size:** 8.2 KB | **Lines:** 285

**Imports:**
- `core::ops::{BitAndAssign, BitOrAssign, Not, Shl, Shr}`
- `core::{
    fmt::Display,
    ops::{BitAnd, Rem, Sub},
}`
- `crate::memory::PAGE_SIZE`

**Declarations:**

`const _: () = assert!(core::mem::size_of::<usize>() == core::mem::size_of::<u64>())`

**`impl UsizeExt for u64`**
  `fn as_usize(self) -> usize`


**`impl Display for PrintMemorySizeHumanFriendly`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl BufferExtension for [u8]`**
  `fn interpret_as<T>(&self) -> &T`

  `fn split_as<T>(&self) -> (&T, &[u8])`


**`impl<T> InBytes for alloc::vec::Vec<T>`**
  `fn in_bytes(&self) -> usize`


**`impl<T, const N: usize> InBytes for [T; N]`**
  `fn in_bytes(&self) -> usize`


`mod kani_proofs`

`mod tests`

---

## kernel/src/klibc/writable_buffer.rs

**Language:** Rust | **Size:** 1.0 KB | **Lines:** 37

**Imports:**
- `super::util::copy_slice`

**Declarations:**

**`impl<'a> WritableBuffer<'a>`**
  `pub fn new(buf: &'a mut [u8]) -> Self`

  `pub fn write_usize(&mut self, value: usize) -> Result<(), WritableBufferError>`

  `pub fn write_slice(&mut self, value: &[u8]) -> Result<(), WritableBufferError>`


---

## kernel/src/lib.rs

**Language:** Rust | **Size:** 7.1 KB | **Lines:** 243

**Imports:**
- `crate::{
    interrupts::plic, io::uart::QEMU_UART, memory::page_tables, pci::enumerate_devices,
    processes::timer,
}`
- `alloc::vec::Vec`
- `asm::wfi_loop`
- `cpu::Cpu`
- `debugging::{backtrace, symbols}`
- `device_tree::get_devicetree_range`
- `memory::page_tables::MappingDescription`
- `processes::process_table`

**Declarations:**

`mod assert`

`mod autogenerated`

`mod cpu`

`mod debugging`

`mod device_tree`

`mod drivers`

`mod fs`

`mod io`

`mod klibc`

`mod logging`

`mod memory`

`mod net`

`mod pci`

`mod processes`

`mod syscalls`

`mod test`

`mod test_entry_points`

`fn start_other_harts(current_hart_id: usize, number_of_cpus: usize)`

`fn main()`

---

## kernel/src/logging/configuration.rs

**Language:** Rust | **Size:** 1.2 KB | **Lines:** 45

**Declarations:**

`const LOG_FOLLOWING_MODULES: &[&str] = &[]`

`const DONT_LOG_FOLLOWING_MODULES: &[&str] = &[ "kernel::interrupts::trap", "kernel::debugging::unwinder", "kernel::debugging::symbols", "kernel::processes::scheduler", "kernel::processes::process_table", "kernel::processes::timer", "kernel::io::tty_device", ]`

`const fn const_starts_with(haystack: &str, needle: &str) -> bool`

---

## kernel/src/logging/mod.rs

**Language:** Rust | **Size:** 2.2 KB | **Lines:** 101

**Imports:**
- `core::fmt`

**Declarations:**

---

## kernel/src/memory/heap.rs

**Language:** Rust | **Size:** 402 B | **Lines:** 15

**Imports:**
- `sys::memory::heap::SpinlockHeap`

**Declarations:**

`static HEAP: SpinlockHeap<super::StaticPageAllocator> = SpinlockHeap::new()`

---

## kernel/src/memory/linker_information.rs

**Language:** Rust | **Size:** 2.3 KB | **Lines:** 75

---

## kernel/src/memory/mod.rs

**Language:** Rust | **Size:** 3.5 KB | **Lines:** 119

**Imports:**
- `crate::klibc::Spinlock`
- `crate::{device_tree, info}`
- `core::{ops::Range, ptr::NonNull}`
- `linker_information::LinkerInformation`
- `sys::memory::page_allocator::{MetadataPageAllocator, PageAllocator}`
- `pub use sys::memory::{
    address::{PhysAddr, VirtAddr},
    page::{PAGE_SIZE, Page, Pages, PagesAsSlice, PinnedHeapPages, page_slice_at_phys},
}`
- `pub use runtime_mappings::initialize_runtime_mappings`

**Declarations:**

`mod runtime_mappings`

`static PAGE_ALLOCATOR: Spinlock<MetadataPageAllocator> = Spinlock::new(MetadataPageAllocator::new())`

**`impl PageAllocator for StaticPageAllocator`**
  `fn alloc(number_of_pages_requested: usize) -> Option<Range<NonNull<Page>>>`

  `fn dealloc(page: NonNull<Page>) -> usize`


---

## kernel/src/memory/page_table_entry.rs

**Language:** Rust | **Size:** 6.9 KB | **Lines:** 216

**Imports:**
- `sys::memory::page_table::PageTableEntry`
- `sys::memory::page_table::XWRMode`
- `crate::klibc::elf`

**Declarations:**

**`impl From<elf::ProgramHeaderFlags> for XWRMode`**
  `fn from(value: elf::ProgramHeaderFlags) -> Self`


`mod kani_proofs`

`mod tests`

---

## kernel/src/memory/page_tables.rs

**Language:** Rust | **Size:** 21.9 KB | **Lines:** 681

**Imports:**
- `core::{
    fmt::{Debug, Display},
    ops::Range,
}`
- `crate::klibc::util::align_up`
- `alloc::{
    boxed::Box,
    string::{String, ToString},
    vec::Vec,
}`
- `common::{pointer::Pointer, unwrap_or_return}`
- `headers::errno::Errno`
- `crate::{
    debug, debugging,
    klibc::{
        sizes::{GiB, MiB},
        util::is_aligned,
    },
    memory::PAGE_SIZE,
}`
- `super::{PhysAddr, VirtAddr, heap_size, linker_information::LinkerInformation}`
- `pub use sys::memory::page_table::XWRMode`
- `sys::memory::page_table::{OwnedPageTable, PageTable, PageTableEntry, activate_page_table}`

**Declarations:**

`struct MappingEntry`
> Fields: `virtual_range: core::ops::Range<VirtAddr>`, `name: String`, `privileges: XWRMode`

**`impl MappingEntry`**
  `fn new(virtual_range: Range<VirtAddr>, name: String, privileges: XWRMode) -> Self`

  `fn contains(&self, range: &Range<VirtAddr>) -> bool`


**`impl Display for MappingEntry`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl Debug for RootPageTableHolder`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl Display for RootPageTableHolder`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl Drop for RootPageTableHolder`**
  `fn drop(&mut self)`


**`impl RootPageTableHolder`**
  `fn empty() -> Self`

  `fn is_active(&self) -> bool`

  `pub fn new_with_kernel_mapping(extra_mappings: &[MappingDescription]) -> Self`

  `pub fn map_userspace( &mut self, virtual_address_start: VirtAddr, physical_address_start: PhysAddr, size: usize, privileges: XWRMode, name: String, )`

  `fn walk_to_entry(&self, address: VirtAddr) -> Option<&PageTableEntry>`

  `fn walk_to_entry_mut(&mut self, address: VirtAddr) -> Option<&mut PageTableEntry>`

  `pub fn mprotect(&mut self, addr: VirtAddr, size: usize, mode: XWRMode)`

  `pub fn map( &mut self, virtual_address_start: VirtAddr, physical_address_start: PhysAddr, mut size: usize, privileges: XWRMode, is_user_mode_accessible: bool, name: String, )`

  `pub fn map_identity_kernel( &mut self, virtual_address_start: VirtAddr, size: usize, privileges: XWRMode, name: String, )`

  `fn map_identity( &mut self, virtual_address_start: VirtAddr, size: usize, privileges: XWRMode, is_user_mode_accessible: bool, name: String, )`

  `pub fn is_userspace_address(&self, address: VirtAddr) -> bool`

  `pub fn is_valid_userspace_fat_ptr<PTR: Pointer>( &self, ptr: PTR, len: usize, writable: bool, ) -> bool`

  `pub fn remap_page(&mut self, va: VirtAddr, new_phys: PhysAddr, new_mode: XWRMode)`

  `pub fn get_userspace_permissions(&self, va: VirtAddr) -> Option<XWRMode>`

  `pub fn translate_userspace_address_to_physical_address<PTR: Pointer>( &self, ptr: PTR, ) -> Option<PTR>`

  `pub fn get_satp_value_from_page_tables(&self) -> usize`

  `pub fn activate_page_table(&self)`

  `pub fn unmap_userspace(&mut self, virtual_address_start: VirtAddr, size: usize)`

  `pub fn is_mapped(&self, range: Range<VirtAddr>) -> bool`


**`impl sys::klibc::validated_ptr::PtrValidator for RootPageTableHolder`**
  `fn validate_userspace<PTR: Pointer>(&self, ptr: PTR, len: usize) -> Result<PTR, Errno>`

  `fn validate_kernel<PTR: Pointer>(&self, _ptr: PTR, _len: usize) -> Result<PTR, Errno>`


`mod tests`

---

## kernel/src/memory/runtime_mappings.rs

**Language:** Rust | **Size:** 455 B | **Lines:** 15

**Imports:**
- `crate::klibc::runtime_initialized::RuntimeInitializedData`
- `alloc::vec::Vec`
- `super::page_tables::MappingDescription`

**Declarations:**

`static RUNTIME_MAPPINGS: RuntimeInitializedData<Vec<MappingDescription>> = RuntimeInitializedData::new()`

---

## kernel/src/net/arp.rs

**Language:** Rust | **Size:** 4.1 KB | **Lines:** 127

**Imports:**
- `core::{fmt::Display, net::Ipv4Addr}`
- `alloc::collections::BTreeMap`
- `crate::{
    assert::static_assert_size,
    debug,
    klibc::{
        Spinlock,
        big_endian::BigEndian,
        util::{BufferExtension, ByteInterpretable},
    },
    net::ethernet::{EtherTypes, EthernetHeader},
}`
- `super::{current_mac_address, mac::MacAddress}`

**Declarations:**

`static ARP_CACHE: Spinlock<BTreeMap<Ipv4Addr, MacAddress>> = Spinlock::new(BTreeMap::new())`

`const ARP_REQUEST: u16 = 1`

`const ARP_RESPONSE: u16 = 2`

`const HARDWARE_ADDRESS_TYPE_ETHERNET: u16 = 1`

`const PROTOCOL_ADDRESS_TYPE_IPV4: u16 = 0x0800`

`struct ArpPacket`
> Fields: `hardware_address_type: BigEndian<u16>`, `protocol_address_type: BigEndian<u16>`, `hardware_address_length: BigEndian<u8>`, `protocol_address_length: BigEndian<u8>`, `operation: BigEndian<u16>`, `source_mac_address: MacAddress`, `source_ip_address: Ipv4Addr`, `destination_mac_address: MacAddress`, `destination_ip_address: Ipv4Addr`

**`impl ByteInterpretable for ArpPacket`**

**`impl ArpPacket`**
  `fn new_reply(destination_mac_address: MacAddress, destination_ip_address: Ipv4Addr) -> Self`


**`impl Display for ArpPacket`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


---

## kernel/src/net/checksum.rs

**Language:** Rust | **Size:** 1021 B | **Lines:** 39

**Declarations:**

---

## kernel/src/net/ethernet.rs

**Language:** Rust | **Size:** 3.3 KB | **Lines:** 131

**Imports:**
- `core::fmt::Display`
- `crate::{
    assert::static_assert_size,
    debug,
    klibc::{
        big_endian::BigEndian,
        util::{BufferExtension, ByteInterpretable},
    },
}`
- `super::{current_mac_address, mac::MacAddress}`

**Declarations:**

`const BROADCAST_MAC: MacAddress = MacAddress::new([0xff, 0xff, 0xff, 0xff, 0xff, 0xff])`

**`impl ByteInterpretable for EthernetHeader`**

`const ETHERTYPE_ARP: u16 = 0x0806`

`const ETHERTYPE_IPV4: u16 = 0x0800`

**`impl TryFrom<BigEndian<u16>> for EtherTypes`**
  `fn try_from(value: BigEndian<u16>) -> Result<Self, Self::Error>`


**`impl From<EtherTypes> for BigEndian<u16>`**
  `fn from(value: EtherTypes) -> Self`


**`impl EthernetHeader`**
  `pub fn new( destination_mac: MacAddress, source_mac: MacAddress, ether_type: EtherTypes, ) -> Self`

  `pub fn try_parse(data: &[u8]) -> Result<(&Self, &[u8]), ParseError>`

  `fn is_valid_ether_type(&self) -> bool`

  `pub fn ether_type(&self) -> EtherTypes`

  `pub fn source_mac(&self) -> MacAddress`


**`impl Display for EthernetHeader`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


---

## kernel/src/net/ipv4.rs

**Language:** Rust | **Size:** 6.1 KB | **Lines:** 180

**Imports:**
- `core::net::Ipv4Addr`
- `crate::{
    assert::static_assert_size,
    klibc::{
        big_endian::BigEndian,
        util::{BufferExtension, ByteInterpretable},
    },
}`

**Declarations:**

**`impl ByteInterpretable for IpV4Header`**

**`impl IpV4Header`**
  `pub fn new(destination_ip: Ipv4Addr, protocol: u8, payload_size: usize) -> Self`

  `pub fn process(data: &[u8]) -> Result<(&IpV4Header, &[u8]), IpV4ParseError>`

  `pub fn calculate_checksum(&self) -> u16`

  `fn checksum_correct(&self) -> bool`


`mod tests`

---

## kernel/src/net/mac.rs

**Language:** Rust | **Size:** 572 B | **Lines:** 25

**Imports:**
- `core::fmt::Display`

**Declarations:**

**`impl MacAddress`**
  `pub const fn new(address: [u8; 6]) -> Self`

  `pub fn as_bytes(&self) -> [u8; 6]`


**`impl Display for MacAddress`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


---

## kernel/src/net/mod.rs

**Language:** Rust | **Size:** 5.5 KB | **Lines:** 205

**Imports:**
- `core::{
    cell::LazyCell,
    net::Ipv4Addr,
    pin::Pin,
    sync::atomic::{AtomicU64, Ordering},
    task::{Context, Poll, Waker},
}`
- `alloc::vec::Vec`
- `crate::{
    debug,
    drivers::virtio::net::NetworkDevice,
    klibc::{MMIO, Spinlock, runtime_initialized::RuntimeInitializedData},
    net::{ipv4::IpV4Header, udp::UdpHeader},
}`
- `self::ipv4::PROTOCOL_TCP`
- `self::{ethernet::EthernetHeader, ipv4::PROTOCOL_UDP, mac::MacAddress, sockets::OpenSockets}`

**Declarations:**

`mod checksum`

`mod ethernet`

`mod ipv4`

`struct NetworkStack`
> Fields: `device: Spinlock<Option<NetworkDevice>>`, `ip_addr: Spinlock<Ipv4Addr>`, `open_sockets: Spinlock<LazyCell<OpenSockets>>`

`static NETWORK_STACK: NetworkStack = NetworkStack`

`static ISR_STATUS: RuntimeInitializedData<MMIO<u32>> = RuntimeInitializedData::new()`

`static NETWORK_INTERRUPT_COUNTER: AtomicU64 = AtomicU64::new(0)`

`static NETWORK_INTERRUPT_WAKERS: Spinlock<Vec<Waker>> = Spinlock::new(Vec::new())`

`struct NetworkInterruptWait`
> Fields: `seen_counter: u64`, `registered: bool`

**`impl NetworkInterruptWait`**
  `fn new(seen_counter: u64) -> Self`


**`impl Future for NetworkInterruptWait`**
  `fn poll(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output>`


`fn receive_and_process_packets() -> usize`

`fn process_packet(packet: Vec<u8>)`

`fn process_ipv4_packet(data: &[u8], source_mac: MacAddress)`

---

## kernel/src/net/parsed_packets.rs

**Language:** Rust | **Size:** 118 B | **Lines:** 5

**Imports:**
- `super::{arp::ArpPacket, ethernet::EthernetHeader}`

**Declarations:**

---

## kernel/src/net/sockets.rs

**Language:** Rust | **Size:** 10.1 KB | **Lines:** 376

**Imports:**
- `core::{
    net::Ipv4Addr,
    pin::Pin,
    sync::atomic::{AtomicU64, Ordering},
    task::{Context, Poll, Waker},
}`
- `alloc::{
    collections::{BTreeMap, VecDeque, btree_map::Entry},
    sync::{Arc, Weak},
    vec::Vec,
}`
- `crate::{debug, klibc::Spinlock}`

**Declarations:**

`static SOCKET_DATA_COUNTER: AtomicU64 = AtomicU64::new(0)`

`static SOCKET_WAITERS: Spinlock<Vec<Waker>> = Spinlock::new(Vec::new())`

**`impl SocketDataWait`**
  `pub fn new(seen_counter: u64) -> Self`


**`impl Future for SocketDataWait`**
  `fn poll(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output>`


**`impl Port`**
  `pub const fn new(port: u16) -> Self`

  `pub fn as_u16(self) -> u16`


**`impl core::fmt::Display for Port`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


`type WeakSharedAssignedSocket = Weak<Spinlock<AssignedSocket>>`

`type SpinlockSocketMap = Spinlock<BTreeMap<Port, WeakSharedAssignedSocket>>`

`type SharedSocketMap = Arc<SpinlockSocketMap>`

`type WeakSharedSocketMap = Weak<SpinlockSocketMap>`

**`impl OpenSockets`**
  `pub fn new() -> Self`

  `pub fn try_get_socket(&self, port: Port) -> Option<SharedAssignedSocket>`

  `pub fn put_data(&self, from: Ipv4Addr, from_port: Port, to_port: Port, data: &[u8])`


`struct Datagram`
> Fields: `from: Ipv4Addr`, `from_port: Port`, `data: Vec<u8>`

**`impl AssignedSocket`**
  `fn new(port: Port, open_sockets: WeakSharedSocketMap) -> Self`

  `pub fn get_port(&self) -> Port`

  `fn put_data(&mut self, from: Ipv4Addr, from_port: Port, data: &[u8])`

  `pub fn get_datagram(&mut self, out_buffer: &mut [u8]) -> Option<(usize, Ipv4Addr, Port)>`


**`impl Drop for AssignedSocket`**
  `fn drop(&mut self)`


`mod tests`

---

## kernel/src/net/tcp.rs

**Language:** Rust | **Size:** 9.8 KB | **Lines:** 280

**Imports:**
- `alloc::vec::Vec`
- `core::net::Ipv4Addr`
- `crate::{
    assert::static_assert_size,
    debug,
    klibc::{big_endian::BigEndian, util::ByteInterpretable},
    net::ethernet::EthernetHeader,
}`
- `super::{ipv4::IpV4Header, mac::MacAddress}`

**Declarations:**

**`impl ByteInterpretable for TcpHeader`**

**`impl TcpHeader`**
  `pub fn source_port(&self) -> u16`

  `pub fn destination_port(&self) -> u16`

  `pub fn sequence_number(&self) -> u32`

  `pub fn acknowledgment_number(&self) -> u32`

  `pub fn flags(&self) -> u16`

  `pub fn create_tcp_packet( destination_ip: Ipv4Addr, destination_mac: MacAddress, source_port: u16, destination_port: u16, seq: u32, ack: u32, flags: u16, window: u16, data: &[u8], ) -> Vec<u8>`

  `pub fn process<'a>( data: &'a [u8], ip_header: &IpV4Header, ) -> Result<(TcpHeader, &'a [u8]), TcpParseError>`

  `fn pseudo_header(ip_header: &IpV4Header, tcp_length: usize) -> [u8; 12]`

  `fn compute_checksum(data: &[u8], tcp_header: &TcpHeader, ip_header: &IpV4Header) -> u16`

  `fn compute_checksum_raw(tcp_bytes: &[u8], ip_header: &IpV4Header, tcp_length: usize) -> u16`


`mod tests`

---

## kernel/src/net/tcp_connection.rs

**Language:** Rust | **Size:** 18.9 KB | **Lines:** 678

**Imports:**
- `core::{
    net::Ipv4Addr,
    pin::Pin,
    sync::atomic::{AtomicU16, Ordering},
    task::{Context, Poll, Waker},
}`
- `alloc::{
    collections::{BTreeMap, VecDeque},
    sync::Arc,
    vec::Vec,
}`
- `crate::{
    debug, info,
    klibc::Spinlock,
    net::{
        arp,
        mac::MacAddress,
        tcp::{FLAG_ACK, FLAG_FIN, FLAG_RST, FLAG_SYN, TcpHeader},
    },
    processes::kernel_tasks,
}`
- `super::ipv4::IpV4Header`
- `headers::syscall_types::timespec`

**Declarations:**

`const WINDOW_SIZE: u16 = 8192`

`const MAX_RETRANSMITS: usize = 5`

`static NEXT_EPHEMERAL_PORT: AtomicU16 = AtomicU16::new(49152)`

`fn generate_iss() -> u32`

`fn len_as_seq(len: usize) -> u32`

`struct ConnectionId`
> Fields: `local_port: u16`, `remote_ip: Ipv4Addr`, `remote_port: u16`

`struct ReceivedSegment`
> Fields: `seq: u32`, `ack: u32`, `flags: u16`, `data: Vec<u8>`

**`impl TcpConnection`**
  `fn new(id: ConnectionId, remote_mac: MacAddress, initial_seq: u32) -> Self`

  `fn deliver_segment(&mut self, segment: ReceivedSegment) -> Option<Waker>`

  `pub fn local_port(&self) -> u16`

  `pub fn remote_ip(&self) -> Ipv4Addr`

  `pub fn remote_port(&self) -> u16`

  `pub fn is_closed(&self) -> bool`

  `pub fn recv_data(&mut self, count: usize) -> Vec<u8>`

  `pub fn has_recv_data(&self) -> bool`

  `pub fn register_recv_waker(&mut self, waker: Waker)`

  `pub fn queue_send_data(&mut self, data: &[u8]) -> Option<Waker>`

  `pub fn request_close(&mut self) -> Option<Waker>`


**`impl TcpListener`**
  `pub fn new(port: u16) -> Self`

  `pub fn port(&self) -> u16`

  `fn push_connection(&mut self, conn: SharedTcpConnection) -> Option<Waker>`

  `pub fn accept(&mut self) -> Option<SharedTcpConnection>`

  `pub fn register_waker(&mut self, waker: Waker)`


`static TCP_CONNECTIONS: Spinlock<BTreeMap<ConnectionId, SharedTcpConnection>> = Spinlock::new(BTreeMap::new())`

`static TCP_LISTENERS: Spinlock<BTreeMap<u16, SharedTcpListener>> = Spinlock::new(BTreeMap::new())`

`fn send_rst( dest_ip: Ipv4Addr, dest_mac: MacAddress, src_port: u16, dst_port: u16, seq: u32, ack: u32, )`

`struct WaitForSegment`
> Fields: `conn: SharedTcpConnection`

**`impl Future for WaitForSegment`**
  `fn poll(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output>`


`fn wait_for_segment(conn: &SharedTcpConnection) -> WaitForSegment`

`async fn wait_for_segment_or_timeout( conn: &SharedTcpConnection, seconds: i64, ) -> Option<ReceivedSegment>`

`struct SegmentOrTimeout`
> Fields: `segment: WaitForSegment`, `timeout: crate::processes::timer::Sleep`, `done: bool`

**`impl Future for SegmentOrTimeout`**
  `fn poll(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output>`


`async fn server_connection_task( conn: SharedTcpConnection, initial_syn: ReceivedSegment, listener: SharedTcpListener, )`

`fn flush_send_buffer(conn: &SharedTcpConnection)`

`fn send_fin(conn: &SharedTcpConnection)`

`async fn established_loop(conn: &SharedTcpConnection)`

`fn send_data_packet(conn: &SharedTcpConnection, flags: u16, seq: u32, ack: u32, data: &[u8])`

`async fn wait_for_fin_ack(conn: &SharedTcpConnection)`

`fn cleanup_connection(id: ConnectionId)`

**`impl Future for WaitForAccept`**
  `fn poll(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output>`


**`impl Future for WaitForRecvData`**
  `fn poll(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output>`


---

## kernel/src/net/udp.rs

**Language:** Rust | **Size:** 5.4 KB | **Lines:** 177

**Imports:**
- `alloc::vec::Vec`
- `core::net::Ipv4Addr`
- `crate::{
    assert::static_assert_size,
    debug,
    klibc::{
        big_endian::BigEndian,
        util::{BufferExtension, ByteInterpretable},
    },
    net::ethernet::EthernetHeader,
}`
- `super::{ipv4::IpV4Header, mac::MacAddress}`

**Declarations:**

**`impl ByteInterpretable for UdpHeader`**

**`impl UdpHeader`**
  `pub fn destination_port(&self) -> u16`

  `pub fn source_port(&self) -> u16`

  `pub fn create_udp_packet( destination_ip: Ipv4Addr, destination_port: u16, destination_mac: MacAddress, source_port: u16, data: &[u8], ) -> Vec<u8>`

  `pub fn process<'a>( data: &'a [u8], ip_header: &IpV4Header, ) -> Result<(&'a UdpHeader, &'a [u8]), UdpParseError>`

  `fn compute_checksum(data: &[u8], udp_header: &UdpHeader, ip_header: &IpV4Header) -> u16`


`mod tests`

---

## kernel/src/panic.rs

**Language:** Rust | **Size:** 2.3 KB | **Lines:** 83

**Imports:**
- `crate::{println, test::qemu_exit::wait_for_the_end}`
- `core::{
    panic::PanicInfo,
    sync::atomic::{AtomicIsize, AtomicU8},
}`
- `crate::test::qemu_exit::exit_failure`

**Declarations:**

`static PANIC_COUNTER: AtomicU8 = AtomicU8::new(0)`

`static CPU_ENTERED_PANIC: AtomicIsize = AtomicIsize::new(-1)`

`fn test_panic_handler(info: &PanicInfo) -> !`

`fn abort_if_double_panic()`

---

## kernel/src/pci/address.rs

**Language:** Rust | **Size:** 2.6 KB | **Lines:** 106

**Imports:**
- `core::{fmt, ops::Add}`

**Declarations:**

**`impl PciAddr`**
  `pub const fn new(addr: usize) -> Self`

  `pub const fn as_usize(self) -> usize`

  `pub const fn to_cpu_addr(self, offset: i64) -> PciCpuAddr`


**`impl PciCpuAddr`**
  `pub const fn new(addr: usize) -> Self`

  `pub const fn as_usize(self) -> usize`


**`impl Add<usize> for PciCpuAddr`**
  `fn add(self, rhs: usize) -> Self`


**`impl fmt::Display for PciAddr`**
  `fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result`


**`impl fmt::Display for PciCpuAddr`**
  `fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result`


`mod tests`

---

## kernel/src/pci/allocator.rs

**Language:** Rust | **Size:** 3.2 KB | **Lines:** 108

**Imports:**
- `core::ops::Range`
- `crate::klibc::util`
- `super::{PCIRange, PciAddr, PciCpuAddr}`

**Declarations:**

**`impl PCIAllocator`**
  `pub const fn new() -> Self`

  `pub fn init(&mut self, pci_range: &PCIRange)`

  `pub fn allocate(&mut self, size: usize) -> Option<PCIAllocatedSpace>`


`mod tests`

---

## kernel/src/pci/devic_tree_parser.rs

**Language:** Rust | **Size:** 4.3 KB | **Lines:** 154

**Imports:**
- `core::fmt::Debug`
- `crate::{
    device_tree::{self},
    klibc::{big_endian::BigEndian, util::UsizeExt},
}`
- `alloc::vec::Vec`
- `super::{PciAddr, PciCpuAddr}`

**Declarations:**

**`impl PCIInformation`**
  `pub fn get_first_range_for_type(&self, space_code: u8) -> Option<&PCIRange>`


**`impl PCIBitField`**
  `fn high_bits(&self) -> u8`

  `pub fn relocatable(&self) -> bool`

  `pub fn prefetchable(&self) -> bool`

  `pub fn alias_address(&self) -> bool`

  `pub fn space_code(&self) -> u8`


**`impl Debug for PCIBitField`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl From<u32> for PCIBitField`**
  `fn from(value: u32) -> Self`


---

## kernel/src/pci/lookup.rs

**Language:** Rust | **Size:** 761 B | **Lines:** 28

**Imports:**
- `alloc::string::String`

**Declarations:**

`static PCI_IDS: &str = include_str!("pci.ids")`

---

## kernel/src/pci/mod.rs

**Language:** Rust | **Size:** 8.4 KB | **Lines:** 267

**Imports:**
- `crate::{
    debug, info,
    klibc::{MMIO, Spinlock},
    mmio_struct, pci,
}`
- `alloc::{collections::BTreeMap, vec::Vec}`
- `lookup::lookup`
- `pub use address::{PciAddr, PciCpuAddr}`
- `pub use devic_tree_parser::parse`
- `self::allocator::PCIAllocator`
- `pub use self::{
    allocator::PCIAllocatedSpace,
    devic_tree_parser::{PCIBitField, PCIInformation, PCIRange},
}`

**Declarations:**

`mod allocator`

`mod devic_tree_parser`

`mod lookup`

`const INVALID_VENDOR_ID: u16 = 0xffff`

`const GENERAL_DEVICE_TYPE: u8 = 0x0`

`const GENERAL_DEVICE_TYPE_MASK: u8 = !0x80`

`const CAPABILITY_POINTER_MASK: u8 = !0x3`

**`impl GeneralDevicePciHeaderExt for MMIO<GeneralDevicePciHeader>`**
  `fn bar(&self, index: u8) -> u32`

  `fn write_bar(&mut self, index: u8, value: u32)`

  `fn set_command_register_bits(&mut self, bits: u16)`

  `fn clear_command_register_bits(&mut self, bits: u16)`


**`impl Iterator for PciCapabilityIter<'_>`**
  `fn next(&mut self) -> Option<Self::Item>`


**`impl PCIDevice`**
  `pub fn configuration_space_mut(&mut self) -> &mut MMIO<GeneralDevicePciHeader>`

  `pub fn configuration_space(&self) -> &MMIO<GeneralDevicePciHeader>`

  `pub fn plic_interrupt_id(&self) -> u32`

  `fn try_new(address: PciCpuAddr, device_number: u8) -> Option<Self>`

  `pub fn capabilities(&self) -> PciCapabilityIter<'_>`

  `pub fn get_or_initialize_bar(&mut self, index: u8) -> PCIAllocatedSpace`


`fn pci_address(starting_address: PciCpuAddr, bus: u8, device: u8, function: u8) -> PciCpuAddr`

---

## kernel/src/processes/brk.rs

**Language:** Rust | **Size:** 2.8 KB | **Lines:** 105

**Imports:**
- `crate::{
    klibc::util::align_up_page_size,
    memory::{
        Pages, PhysAddr, PinnedHeapPages, VirtAddr,
        page_tables::{RootPageTableHolder, XWRMode},
    },
}`

**Declarations:**

`const BRK_SIZE: Pages = Pages::new(4)`

**`impl Brk`**
  `pub fn new( bss_end: VirtAddr, page_tables: &mut RootPageTableHolder, ) -> (PinnedHeapPages, Self)`

  `pub fn empty() -> Self`

  `pub fn start(&self) -> VirtAddr`

  `pub fn brk(&mut self, brk: VirtAddr) -> VirtAddr`


`mod tests`

---

## kernel/src/processes/credentials.rs

**Language:** Rust | **Size:** 448 B | **Lines:** 26

**Imports:**
- `alloc::vec::Vec`

**Declarations:**

**`impl Credentials`**
  `pub fn root() -> Self`


---

## kernel/src/processes/fd_table.rs

**Language:** Rust | **Size:** 10.7 KB | **Lines:** 355

**Imports:**
- `alloc::collections::BTreeMap`
- `common::pid::Tid`
- `core::fmt`
- `headers::{
    errno::Errno,
    syscall_types::{O_CLOEXEC, O_NONBLOCK},
}`
- `crate::{
    fs::VfsOpenFile,
    io::{
        pipe::{PipeReader, PipeWriter, ReadPipe},
        tty_device::{ReadTty, TtyDevice},
        uart::QEMU_UART,
    },
    net::{
        sockets::SharedAssignedSocket,
        tcp_connection::{SharedTcpConnection, SharedTcpListener},
    },
    processes::process::ProcessRef,
}`

**Declarations:**

**`impl FdFlags`**
  `pub fn is_nonblocking(self) -> bool`

  `pub fn as_raw(self) -> i32`

  `pub fn from_raw(raw: i32) -> Self`

  `pub fn is_cloexec(self) -> bool`


**`impl Clone for FileDescriptor`**
  `fn clone(&self) -> Self`


**`impl fmt::Debug for FileDescriptor`**
  `fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result`


**`impl FileDescriptor`**
  `pub async fn read( &self, count: usize, process: ProcessRef, tid: Tid, ) -> Result<alloc::vec::Vec<u8>, Errno>`

  `pub fn try_read(&self, count: usize) -> Result<alloc::vec::Vec<u8>, Errno>`

  `pub async fn write(&self, data: &[u8]) -> Result<usize, Errno>`


**`impl FdTable`**
  `pub fn new() -> Self`

  `pub fn get(&self, fd: RawFd) -> Option<&FdEntry>`

  `pub fn allocate(&mut self, descriptor: FileDescriptor) -> Result<RawFd, Errno>`

  `pub fn replace_descriptor( &mut self, fd: RawFd, descriptor: FileDescriptor, ) -> Result<(), Errno>`

  `pub fn dup_from( &mut self, oldfd: RawFd, min_fd: RawFd, flags: FdFlags, ) -> Result<RawFd, Errno>`

  `pub fn dup_to(&mut self, oldfd: RawFd, newfd: RawFd, flags: i32) -> Result<RawFd, Errno>`

  `pub fn close(&mut self, fd: RawFd) -> Result<FdEntry, Errno>`

  `pub fn get_descriptor(&self, fd: RawFd) -> Result<FileDescriptor, Errno>`

  `pub fn get_descriptor_and_flags(&self, fd: RawFd) -> Result<(FileDescriptor, FdFlags), Errno>`

  `pub fn get_vfs_file(&self, fd: RawFd) -> Result<VfsOpenFile, Errno>`

  `pub fn get_flags(&self, fd: RawFd) -> Result<FdFlags, Errno>`

  `pub fn set_flags(&mut self, fd: RawFd, flags: FdFlags) -> Result<(), Errno>`

  `pub fn close_all(&mut self)`

  `pub fn close_cloexec_fds(&mut self)`

  `fn close_tcp_if_needed(descriptor: &FileDescriptor)`


---

## kernel/src/processes/futex.rs

**Language:** Rust | **Size:** 2.1 KB | **Lines:** 81

**Imports:**
- `alloc::{collections::BTreeMap, vec::Vec}`
- `common::pid::Tid`
- `core::{
    future::Future,
    pin::Pin,
    task::{Context, Poll, Waker},
}`
- `headers::errno::Errno`
- `crate::{klibc::Spinlock, processes::process::ProcessRef}`
- `super::userspace_ptr::UserspacePtr`

**Declarations:**

`type FutexKey = (Tid, usize)`

`static WAITERS: Spinlock<BTreeMap<FutexKey, Vec<Waker>>> = Spinlock::new(BTreeMap::new())`

**`impl FutexWait`**
  `pub fn new(process: ProcessRef, addr: usize, expected: u32, main_tid: Tid) -> Self`


**`impl Future for FutexWait`**
  `fn poll(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output>`


---

## kernel/src/processes/kernel_tasks.rs

**Language:** Rust | **Size:** 3.7 KB | **Lines:** 130

**Imports:**
- `alloc::{
    collections::{BTreeMap, VecDeque},
    sync::Arc,
    task::Wake,
}`
- `common::{pid::Tid, syscalls::trap_frame::TrapFrame}`
- `core::{
    sync::atomic::{AtomicBool, AtomicU64, AtomicUsize, Ordering},
    task::{Context, Waker},
}`
- `crate::{
    klibc::Spinlock,
    memory::{VirtAddr, page_tables::RootPageTableHolder},
    processes::brk::Brk,
}`
- `super::{
    process::POWERSAVE_TID,
    process_table,
    task::Task,
    thread::{ThreadRef, get_next_tid},
}`

**Declarations:**

`const WORKER_STACK_SIZE: usize = 16 * 1024`

`static TASKS: Spinlock<BTreeMap<usize, Task<()>>> = Spinlock::new(BTreeMap::new())`

`static READY_IDS: Spinlock<VecDeque<usize>> = Spinlock::new(VecDeque::new())`

`static NEXT_ID: AtomicUsize = AtomicUsize::new(0)`

`static WORKER_THREAD: Spinlock<Option<ThreadRef>> = Spinlock::new(None)`

`static WORKER_SLEEP_REQUESTED: AtomicBool = AtomicBool::new(false)`

`static WORKER_TID: AtomicU64 = AtomicU64::new(u64::MAX)`

`fn poll_ready_tasks()`

`extern "C" fn kernel_worker_entry() -> !`

`fn wake_worker_thread()`

`struct KernelTaskWaker`
> Fields: `task_id: usize`

**`impl Wake for KernelTaskWaker`**
  `fn wake(self: Arc<Self>)`


---

## kernel/src/processes/loader.rs

**Language:** Rust | **Size:** 9.1 KB | **Lines:** 297

**Imports:**
- `alloc::{collections::BTreeMap, string::ToString, vec::Vec}`
- `common::errors::LoaderError`
- `headers::syscall_types::{AT_NULL, AT_PAGESZ, AT_PHDR, AT_PHENT, AT_PHNUM, AT_RANDOM}`
- `crate::klibc::{util::align_up, writable_buffer::WritableBuffer}`
- `crate::{
    debug,
    klibc::{
        elf::{ElfFile, ProgramHeaderType},
        util::{InBytes, UsizeExt, minimum_amount_of_pages},
    },
    memory::{
        PAGE_SIZE, PagesAsSlice, PhysAddr, PinnedHeapPages, VirtAddr,
        page_tables::{RootPageTableHolder, XWRMode},
    },
    processes::brk::Brk,
}`

**Declarations:**

`struct AuxvInfo`
> Fields: `phdr_vaddr: usize`, `phent: usize`, `phnum: usize`

`const AT_RANDOM_SIZE: usize = 16`

`fn set_up_arguments( stack: &mut [u8], name: &str, args: &[&str], env: &[&str], auxv_info: &AuxvInfo, ) -> Result<VirtAddr, LoaderError>`

---

## kernel/src/processes/mod.rs

**Language:** Rust | **Size:** 297 B | **Lines:** 16

**Declarations:**

`pub(crate) mod brk`

`pub(crate) mod loader`

---

## kernel/src/processes/process.rs

**Language:** Rust | **Size:** 18.3 KB | **Lines:** 590

**Imports:**
- `crate::{
    debug,
    klibc::Spinlock,
    memory::{
        PAGE_SIZE, Pages, PhysAddr, PinnedHeapPages, VirtAddr,
        page_tables::{RootPageTableHolder, XWRMode},
    },
    processes::{
        brk::Brk, credentials::Credentials, fd_table::FdTable, thread::ThreadWeakRef,
        userspace_ptr::UserspacePtr,
    },
}`
- `alloc::{
    collections::BTreeMap,
    string::{String, ToString},
    sync::Arc,
    vec::Vec,
}`
- `common::pid::Tid`
- `core::{self, fmt::Debug, ptr::null_mut}`
- `headers::errno::Errno`
- `sys::klibc::validated_ptr::ValidatedPtr`

**Declarations:**

`const FREE_MMAP_START_ADDRESS: usize = 0x2000000000`

**`impl Debug for Process`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl Process`**
  `pub fn new( name: Arc<String>, page_table: RootPageTableHolder, allocated_pages: BTreeMap<VirtAddr, PinnedHeapPages>, brk: Brk, main_thread: Tid, pgid: Tid, sid: Tid, ) -> Self`

  `pub fn brk(&mut self, brk: VirtAddr) -> VirtAddr`

  `pub fn add_thread(&mut self, tid: Tid, thread: ThreadWeakRef)`

  `pub fn read_userspace_slice<T: Clone>( &self, ptr: &UserspacePtr<*const T>, len: usize, ) -> Result<Vec<T>, Errno>`

  `pub fn write_userspace_slice<T: Copy>( &mut self, ptr: &UserspacePtr<*mut T>, data: &[T], ) -> Result<(), Errno>`

  `pub fn read_userspace_ptr<T: Copy>(&self, ptr: &UserspacePtr<*const T>) -> Result<T, Errno>`

  `pub fn write_userspace_ptr<T>( &mut self, ptr: &UserspacePtr<*mut T>, value: T, ) -> Result<(), Errno>`

  `pub fn mmap_pages_with_address( &mut self, num_pages: Pages, addr: VirtAddr, permission: XWRMode, ) -> *mut u8`

  `pub fn mmap_pages(&mut self, num_pages: Pages, permission: XWRMode) -> *mut u8`

  `pub fn munmap_pages(&mut self, addr: VirtAddr, length: usize) -> Result<(), Errno>`

  `pub fn get_page_table(&self) -> &RootPageTableHolder`

  `pub fn get_page_table_mut(&mut self) -> &mut RootPageTableHolder`

  `pub fn get_name(&self) -> &str`

  `pub fn main_tid(&self) -> Tid`

  `pub fn pgid(&self) -> Tid`

  `pub fn sid(&self) -> Tid`

  `pub fn set_pgid(&mut self, pgid: Tid)`

  `pub fn set_sid(&mut self, sid: Tid)`

  `pub fn umask(&self) -> u32`

  `pub fn set_umask(&mut self, mask: u32)`

  `pub fn cwd(&self) -> &str`

  `pub fn set_cwd(&mut self, cwd: String)`

  `pub fn credentials(&self) -> &Credentials`

  `pub fn credentials_mut(&mut self) -> &mut Credentials`

  `pub fn set_credentials(&mut self, creds: Credentials)`

  `pub fn fd_table(&self) -> crate::klibc::SpinlockGuard<'_, FdTable>`

  `pub fn set_fd_table(&mut self, fd_table: FdTable)`

  `pub fn get_satp_value(&self) -> usize`

  `pub fn remove_thread(&mut self, tid: Tid)`

  `pub fn has_no_threads(&self) -> bool`

  `pub fn close_all_fds(&self)`

  `pub fn thread_tids(&self) -> Vec<Tid>`

  `pub fn fork_address_space(&mut self) -> ForkedAddressSpace`

  `pub fn set_fork_state( &mut self, cow_pages: BTreeMap<VirtAddr, CowPageInfo>, free_mmap_address: VirtAddr, )`

  `pub fn resolve_cow_page(&mut self, faulting_va: VirtAddr) -> bool`

  `fn ensure_cow_resolved_for_write(&mut self, addr: usize, len: usize)`


**`impl core::fmt::Display for Process`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl Drop for Process`**
  `fn drop(&mut self)`


`mod tests`

---

## kernel/src/processes/process_table.rs

**Language:** Rust | **Size:** 11.6 KB | **Lines:** 348

**Imports:**
- `alloc::{
    collections::{BTreeMap, VecDeque},
    vec::Vec,
}`
- `common::pid::Tid`
- `core::{
    sync::atomic::{AtomicUsize, Ordering},
    task::Waker,
}`
- `crate::{
    autogenerated::userspace_programs::INIT,
    cpu::Cpu,
    debug, info,
    klibc::{Spinlock, elf::ElfFile, runtime_initialized::RuntimeInitializedData},
    processes::{futex, process::POWERSAVE_TID, thread::Thread},
}`
- `super::{
    thread::{ThreadRef, ThreadState},
    wait_child::WaitPid,
}`

**Declarations:**

`static LIVE_THREAD_COUNT: AtomicUsize = AtomicUsize::new(0)`

**`impl ProcessTable`**
  `pub fn new() -> Self`

  `pub fn add_thread(&mut self, thread: ThreadRef)`

  `pub fn dump(&self)`

  `pub fn take_zombie(&mut self, parent_tid: Tid, target: &WaitPid) -> Option<(Tid, i32)>`

  `pub fn take_stopped(&mut self, parent_tid: Tid, target: &WaitPid) -> Option<(Tid, i32)>`

  `pub fn get_pgid_of(&self, tid: Tid) -> Option<Tid>`

  `pub fn get_sid_of(&self, tid: Tid) -> Option<Tid>`

  `pub fn set_pgid_of(&mut self, tid: Tid, pgid: Tid) -> bool`

  `pub fn is_child_of(&self, parent_tid: Tid, child_tid: Tid) -> bool`

  `pub fn has_any_child_of(&self, parent_tid: Tid) -> bool`

  `pub fn register_wait_waker(&mut self, waker: Waker)`

  `pub fn get_thread(&self, tid: Tid) -> Option<&ThreadRef>`

  `pub fn wake_wait_wakers(&mut self)`

  `pub fn send_signal(&mut self, tid: Tid, sig: u32)`

  `pub fn send_signal_to_pgid(&mut self, pgid: Tid, sig: u32)`

  `pub fn send_signal_to_process(&mut self, tid: Tid, sig: u32)`

  `pub fn kill_process_of(&mut self, tid: Tid, exit_status: super::signal::ExitStatus)`

  `pub fn kill(&mut self, tid: Tid, exit_status: super::signal::ExitStatus)`


---

## kernel/src/processes/scheduler.rs

**Language:** Rust | **Size:** 15.8 KB | **Lines:** 406

**Imports:**
- `super::{
    process::{POWERSAVE_TID, ProcessRef},
    process_table::{self},
    thread::{ThreadRef, ThreadState},
}`
- `crate::{
    cpu::Cpu,
    debug, info,
    memory::VirtAddr,
    processes::{
        thread::{SyscallTask, Thread},
        timer,
        waker::ThreadWaker,
    },
    test::qemu_exit,
}`
- `alloc::sync::Arc`
- `common::syscalls::trap_frame::Register`
- `core::task::{Context, Poll}`
- `headers::errno::Errno`

**Declarations:**

`enum ProcessMode`
> Variants: `KernelSyscallTask`, `Userspace`

**`impl CpuScheduler`**
  `pub fn new() -> Self`

  `pub fn get_current_thread(&self) -> &ThreadRef`

  `pub fn get_current_process(&self) -> ProcessRef`

  `pub fn is_current_process_energy_saver(&self) -> bool`

  `pub fn schedule(&mut self)`

  `pub fn run_syscall_task(&mut self, mut task: SyscallTask) -> bool`

  `pub fn kill_current_thread(&mut self, exit_status: super::signal::ExitStatus)`

  `pub fn kill_current_process(&mut self, exit_status: super::signal::ExitStatus)`

  `pub fn stop_current_process(&mut self, stop_sig: u32)`

  `pub fn send_tty_signal(&mut self, sig: u32, fg_pgid: common::pid::Tid)`

  `fn queue_current_process_back(&mut self)`

  `fn prepare_next_process(&mut self) -> ProcessMode`

  `pub fn set_cpu_reg_for_current_thread(&self) -> bool`

  `fn swap_current_with_powersave(&mut self) -> ThreadRef`


---

## kernel/src/processes/signal.rs

**Language:** Rust | **Size:** 8.3 KB | **Lines:** 268

**Imports:**
- `crate::{
    debug,
    klibc::util::ByteInterpretable,
    memory::VirtAddr,
    processes::{thread::Thread, userspace_ptr::UserspacePtr},
}`
- `common::syscalls::trap_frame::Register`
- `headers::syscall_types::{
    SA_NODEFER, SA_RESETHAND, SIGABRT, SIGALRM, SIGBUS, SIGCHLD, SIGCONT, SIGFPE, SIGHUP, SIGILL,
    SIGINT, SIGIO, SIGKILL, SIGPIPE, SIGPROF, SIGPWR, SIGQUIT, SIGSEGV, SIGSTKFLT, SIGSTOP, SIGSYS,
    SIGTERM, SIGTRAP, SIGTSTP, SIGTTIN, SIGTTOU, SIGURG, SIGUSR1, SIGUSR2, SIGVTALRM, SIGWINCH,
    SIGXCPU, SIGXFSZ, sigset_t,
}`
- `pub use crate::asm::signal_trampoline_phys_addr as trampoline_phys_addr`

**Declarations:**

**`impl ExitStatus`**
  `pub fn to_wstatus(self) -> i32`


**`impl PendingSignals`**
  `pub const fn new() -> Self`

  `pub fn raise(&mut self, sig: u32)`

  `pub fn clear(&mut self, sig: u32)`

  `pub fn first_unblocked(&self, mask: u64) -> Option<u32>`


`struct SignalFrame`
> Fields: `saved_regs: [usize; 32]`, `saved_fregs: [usize; 32]`, `saved_pc: usize`, `saved_sigmask: u64`

`const SIGNAL_FRAME_SIZE: usize = core::mem::size_of::<SignalFrame>()`

**`impl ByteInterpretable for SignalFrame`**

`fn setup_signal_frame( thread: &mut Thread, sig: u32, handler: unsafe extern "C" fn(core::ffi::c_int), action: &headers::syscall_types::sigaction, ) -> bool`

`mod tests`

---

## kernel/src/processes/task.rs

**Language:** Rust | **Size:** 806 B | **Lines:** 32

**Imports:**
- `core::{
    any::type_name,
    pin::Pin,
    task::{Context, Poll},
}`
- `alloc::boxed::Box`

**Declarations:**

**`impl<Output> core::fmt::Debug for Task<Output>`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl<Output> Task<Output>`**
  `pub fn new(future: impl Future<Output = Output> + Send + 'static) -> Self`

  `pub fn poll(&mut self, cx: &mut Context<'_>) -> Poll<Output>`


---

## kernel/src/processes/thread.rs

**Language:** Rust | **Size:** 14.2 KB | **Lines:** 519

**Imports:**
- `super::process::ProcessRef`
- `crate::{
    debug,
    klibc::elf::ElfFile,
    memory::{PinnedHeapPages, VirtAddr, page_tables::RootPageTableHolder},
    processes::{
        brk::Brk,
        loader::{self, LoadedElf},
        process::{POWERSAVE_TID, Process},
        task::Task,
        userspace_ptr::{ContainsUserspacePtr, UserspacePtr},
    },
}`
- `alloc::{
    collections::BTreeMap,
    string::String,
    sync::{Arc, Weak},
}`
- `common::{
    errors::LoaderError,
    pid::Tid,
    syscalls::trap_frame::{Register, TrapFrame},
}`
- `core::{
    ffi::{c_int, c_uint},
    fmt::Debug,
    ptr::null_mut,
    sync::atomic::{AtomicU64, Ordering},
}`
- `headers::{
    errno::Errno,
    syscall_types::{_NSIG, sigaction, sigaltstack, sigset_t, stack_t},
}`
- `sys::klibc::send_sync::UnsafeSendSync`
- `crate::klibc::Spinlock`

**Declarations:**

`struct SignalState`
> Fields: `sigaltstack: ContainsUserspacePtr<stack_t>`, `sigmask: sigset_t`, `sigaction: [sigaction; _NSIG as usize]`, `pending: super::signal::PendingSignals`

**`impl SignalState`**
  `fn new() -> Self`


**`impl core::fmt::Display for Thread`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl Thread`**
  `pub fn create_powersave_thread() -> Arc<Spinlock<Self>>`

  `pub fn from_elf( elf_file: &ElfFile, name: &str, args: &[&str], env: &[&str], parent_tid: Tid, ) -> Result<Arc<Spinlock<Self>>, LoaderError>`

  `pub fn new_process( name: impl Into<String>, tid: Tid, register_state: TrapFrame, page_table: RootPageTableHolder, program_counter: VirtAddr, allocated_pages: BTreeMap<VirtAddr, PinnedHeapPages>, in_kernel_mode: bool, brk: Brk, parent_tid: Tid, pgid: Tid, sid: Tid, ) -> ThreadRef`

  `pub fn new( tid: Tid, process_name: Arc<String>, register_state: TrapFrame, program_counter: VirtAddr, in_kernel_mode: bool, process: ProcessRef, parent_tid: Tid, ) -> ThreadRef`

  `pub fn get_name(&self) -> &str`

  `pub fn process_name(&self) -> &str`

  `pub fn thread_name(&self) -> Option<&str>`

  `pub fn set_thread_name(&mut self, name: String)`

  `pub fn set_syscall_task_and_suspend(&mut self, task: SyscallTask)`

  `pub fn wake_up(&mut self) -> bool`

  `pub fn suspend_unless_wakeup_pending(&mut self)`

  `fn suspend(&mut self)`

  `pub fn get_tid(&self) -> Tid`

  `pub fn parent_tid(&self) -> Tid`

  `pub fn set_parent_tid(&mut self, parent_tid: Tid)`

  `pub fn set_sigaction(&mut self, sig: c_uint, sigaction: sigaction) -> Result<sigaction, Errno>`

  `pub fn get_sigaction(&self, sig: c_uint) -> Result<sigaction, Errno>`

  `pub fn get_sigset(&self) -> sigset_t`

  `pub fn set_sigset(&mut self, sigmask: sigset_t) -> sigset_t`

  `pub fn get_sigaltstack(&self) -> sigaltstack`

  `pub fn set_sigaltstack(&mut self, sigaltstack: &sigaltstack)`

  `pub fn clear_wakeup_pending(&mut self)`

  `pub fn take_syscall_task(&mut self) -> Option<SyscallTask>`

  `pub fn store_syscall_task(&mut self, task: SyscallTask)`

  `pub fn set_clear_child_tid(&mut self, clear_child_tid: UserspacePtr<*mut c_int>)`

  `pub fn get_clear_child_tid(&self) -> &Option<UserspacePtr<*mut c_int>>`

  `pub fn get_register_state(&self) -> &TrapFrame`

  `pub fn get_register_state_mut(&mut self) -> &mut TrapFrame`

  `pub fn set_register_state(&mut self, register_state: TrapFrame)`

  `pub fn get_program_counter(&self) -> VirtAddr`

  `pub fn set_program_counter(&mut self, program_counter: VirtAddr)`

  `pub fn get_state(&self) -> ThreadState`

  `pub fn set_state(&mut self, state: ThreadState)`

  `pub fn get_in_kernel_mode(&self) -> bool`

  `pub fn process(&self) -> ProcessRef`

  `pub fn set_process(&mut self, new_process: ProcessRef, name: Arc<String>)`

  `pub fn registers_replaced(&self) -> bool`

  `pub fn set_registers_replaced(&mut self, value: bool)`

  `pub fn clear_pending_stop_signals(&mut self)`

  `pub fn raise_signal(&mut self, sig: u32)`

  `pub fn has_pending_unblocked_signal(&self) -> bool`

  `pub fn peek_first_unblocked_signal(&self) -> Option<u32>`

  `pub fn take_next_pending_signal(&mut self) -> Option<u32>`

  `pub fn get_sigaction_raw(&self, sig: u32) -> &sigaction`

  `pub fn get_sigmask(&self) -> u64`

  `pub fn set_sigmask_raw(&mut self, mask: u64)`


---

## kernel/src/processes/timer.rs

**Language:** Rust | **Size:** 3.1 KB | **Lines:** 110

**Imports:**
- `crate::{
    debug, device_tree,
    klibc::{
        Spinlock, big_endian::BigEndian, btreemap::SplitOffLowerThan,
        runtime_initialized::RuntimeInitializedData,
    },
}`
- `alloc::collections::BTreeMap`
- `core::{
    pin::Pin,
    task::{Context, Poll, Waker},
}`
- `headers::{errno::Errno, syscall_types::timespec}`
- `pub use arch::timer::{CLINT_BASE, CLINT_SIZE}`

**Declarations:**

`static TIMEBASE_FREQ: RuntimeInitializedData<u64> = RuntimeInitializedData::new()`

`type WakeupClockTime = u64`

`static WAKEUP_QUEUE: Spinlock<BTreeMap<WakeupClockTime, alloc::vec::Vec<Waker>>> = Spinlock::new(BTreeMap::new())`

**`impl Sleep`**
  `fn new(wakeup_time: u64) -> Self`


**`impl Future for Sleep`**
  `fn poll(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output>`


---

## kernel/src/processes/userspace_ptr.rs

**Language:** Rust | **Size:** 943 B | **Lines:** 40

**Imports:**
- `common::pointer::Pointer`
- `headers::errno::Errno`
- `crate::{klibc::SpinlockGuard, processes::process::Process}`
- `sys::klibc::send_sync::UnsafeSendSync`

**Declarations:**

**`impl<PTR: Pointer> UserspacePtr<PTR>`**
  `pub fn new(ptr: PTR) -> Self`

  `pub fn get(&self) -> PTR`


**`impl<T> UserspacePtr<*mut T>`**
  `pub fn write_with_process_lock( &self, process_lock: &mut SpinlockGuard<'_, Process>, value: T, ) -> Result<(), Errno>`


**`impl<T: core::fmt::Debug> core::fmt::Debug for ContainsUserspacePtr<T>`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


---

## kernel/src/processes/wait_child.rs

**Language:** Rust | **Size:** 1.4 KB | **Lines:** 62

**Imports:**
- `common::pid::Tid`
- `core::{
    future::Future,
    pin::Pin,
    task::{Context, Poll},
}`
- `headers::errno::Errno`
- `super::process_table`

**Declarations:**

**`impl WaitChild`**
  `pub fn new(parent_main_tid: Tid, target: WaitPid, wnohang: bool, wuntraced: bool) -> Self`


**`impl Future for WaitChild`**
  `fn poll(self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<Self::Output>`


---

## kernel/src/processes/waker.rs

**Language:** Rust | **Size:** 628 B | **Lines:** 26

**Imports:**
- `alloc::{sync::Arc, task::Wake}`
- `core::task::Waker`
- `crate::processes::{process_table, thread::ThreadWeakRef}`

**Declarations:**

**`impl ThreadWaker`**
  `pub fn new_waker(thread: ThreadWeakRef) -> Waker`


**`impl Wake for ThreadWaker`**
  `fn wake(self: Arc<Self>)`


---

## kernel/src/syscalls/exec_ops.rs

**Language:** Rust | **Size:** 4.7 KB | **Lines:** 136

**Imports:**
- `alloc::{string::String, sync::Arc, vec::Vec}`
- `headers::errno::Errno`
- `crate::{
    autogenerated::userspace_programs::PROGRAMS,
    klibc::{consumable_buffer::ConsumableBuffer, elf::ElfFile},
    processes::{loader, process::Process, userspace_ptr::UserspacePtr},
}`
- `common::syscalls::trap_frame::{Register, TrapFrame}`
- `super::linux::{LinuxSyscallHandler, LinuxSyscalls}`

**Declarations:**

**`impl LinuxSyscallHandler`**
  `fn read_string_array(&self, array_ptr: usize) -> Result<Vec<Vec<u8>>, Errno>`

  `pub(super) fn do_execve( &mut self, filename: usize, argv: usize, envp: usize, ) -> Result<isize, Errno>`


---

## kernel/src/syscalls/fs_ops.rs

**Language:** Rust | **Size:** 17.0 KB | **Lines:** 529

**Imports:**
- `alloc::{string::String, vec}`
- `core::ffi::{c_int, c_uint}`
- `headers::{
    errno::Errno,
    syscall_types::{O_CREAT, O_DIRECTORY, O_EXCL, O_TRUNC},
}`
- `crate::{
    fs, klibc::util::ByteInterpretable, processes::fd_table::FileDescriptor,
    syscalls::linux_validator::LinuxUserspaceArg,
}`
- `super::linux::LinuxSyscallHandler`

**Declarations:**

**`impl LinuxSyscallHandler`**
  `pub(super) fn do_openat( &self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, flags: c_int, _mode: c_uint, ) -> Result<isize, Errno>`

  `pub(super) fn do_fstat( &self, fd: c_int, statbuf: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `fn resolve_stat_node( &self, dirfd: c_int, pathname: &LinuxUserspaceArg<*const u8>, flags: c_int, ) -> Result<fs::vfs::VfsNodeRef, Errno>`

  `pub(super) fn do_newfstatat( &self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, statbuf: LinuxUserspaceArg<*mut u8>, flags: c_int, ) -> Result<isize, Errno>`

  `pub(super) fn do_statx( &self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, flags: c_int, _mask: c_uint, statxbuf: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `pub(super) fn do_getdents64( &self, fd: c_int, dirp: LinuxUserspaceArg<*mut u8>, count: usize, ) -> Result<isize, Errno>`

  `pub(super) fn do_faccessat( &self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, _mode: c_int, ) -> Result<isize, Errno>`

  `pub(super) fn do_mkdirat( &self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, _mode: c_uint, ) -> Result<isize, Errno>`

  `pub(super) fn do_unlinkat( &self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, flags: c_int, ) -> Result<isize, Errno>`

  `pub(super) fn do_getcwd( &self, buf: LinuxUserspaceArg<*mut u8>, size: usize, ) -> Result<isize, Errno>`

  `fn make_statfs_reply() -> headers::fs::statfs`

  `pub(super) fn do_statfs( &self, pathname: LinuxUserspaceArg<*const u8>, buf: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `pub(super) fn do_fstatfs( &self, fd: c_int, buf: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `pub(super) fn do_truncate( &self, pathname: LinuxUserspaceArg<*const u8>, length: isize, ) -> Result<isize, Errno>`

  `pub(super) fn do_ftruncate(&self, fd: c_int, length: isize) -> Result<isize, Errno>`

  `pub(super) fn do_fchmod(&self, fd: c_int, mode: c_uint) -> Result<isize, Errno>`

  `pub(super) fn do_fchmodat( &self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, mode: c_uint, ) -> Result<isize, Errno>`

  `pub(super) fn do_fchown(&self, fd: c_int, uid: c_uint, gid: c_uint) -> Result<isize, Errno>`

  `pub(super) fn do_fchownat( &self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, uid: c_uint, gid: c_uint, ) -> Result<isize, Errno>`

  `fn resolve_path_from_dirfd( &self, dirfd: c_int, pathname: &LinuxUserspaceArg<*const u8>, ) -> Result<fs::vfs::VfsNodeRef, Errno>`

  `pub(super) fn do_readlinkat( &self, _dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, buf: LinuxUserspaceArg<*mut u8>, bufsiz: usize, ) -> Result<isize, Errno>`

  `pub(super) fn do_symlinkat( &self, target: LinuxUserspaceArg<*const u8>, linkpath: LinuxUserspaceArg<*const u8>, ) -> Result<isize, Errno>`

  `pub(super) fn do_linkat( &self, oldpath: LinuxUserspaceArg<*const u8>, newpath: LinuxUserspaceArg<*const u8>, ) -> Result<isize, Errno>`

  `pub(super) fn do_renameat2( &self, oldpath: LinuxUserspaceArg<*const u8>, newpath: LinuxUserspaceArg<*const u8>, flags: c_uint, ) -> Result<isize, Errno>`


---

## kernel/src/syscalls/helpers.rs

**Language:** Rust | **Size:** 1.7 KB | **Lines:** 50

**Imports:**
- `alloc::string::String`
- `headers::{errno::Errno, syscall_types::timespec}`
- `crate::{
    fs, klibc::consumable_buffer::ConsumableBuffer, syscalls::linux_validator::LinuxUserspaceArg,
}`
- `super::linux::LinuxSyscallHandler`

**Declarations:**

**`impl LinuxSyscallHandler`**
  `pub(super) fn resolve_dirfd_node(&self, dirfd: i32) -> Result<fs::vfs::VfsNodeRef, Errno>`

  `pub(super) fn read_path(&self, arg: &LinuxUserspaceArg<*const u8>) -> Result<String, Errno>`

  `pub(super) fn make_absolute(&self, path: &str) -> String`

  `pub(super) fn read_cstring(&self, arg: &LinuxUserspaceArg<*const u8>) -> Result<String, Errno>`

  `pub(super) fn validate_poll_timeout(to: Option<timespec>)`


---

## kernel/src/syscalls/id_ops.rs

**Language:** Rust | **Size:** 9.0 KB | **Lines:** 298

**Imports:**
- `alloc::{string::String, vec::Vec}`
- `core::ffi::{c_int, c_uint, c_ulong}`
- `headers::{
    errno::Errno,
    syscall_types::{FUTEX_PRIVATE_FLAG, FUTEX_WAIT, FUTEX_WAKE, PR_GET_NAME, PR_SET_NAME},
}`
- `crate::processes::{
    futex::{self, FutexWait},
    process_table,
}`
- `common::pid::Tid`
- `super::{linux::LinuxSyscallHandler, linux_validator::LinuxUserspaceArg}`

**Declarations:**

**`impl LinuxSyscallHandler`**
  `pub(super) fn do_getpgid(&self, pid: c_int) -> Result<isize, Errno>`

  `pub(super) fn do_getsid(&self, pid: c_int) -> Result<isize, Errno>`

  `pub(super) fn do_setpgid(&self, pid: c_int, pgid: c_int) -> Result<isize, Errno>`

  `pub(super) fn do_setuid(&self, uid: c_uint) -> Result<isize, Errno>`

  `pub(super) fn do_setgid(&self, gid: c_uint) -> Result<isize, Errno>`

  `pub(super) fn do_setreuid(&self, ruid: c_uint, euid: c_uint) -> Result<isize, Errno>`

  `pub(super) fn do_setregid(&self, rgid: c_uint, egid: c_uint) -> Result<isize, Errno>`

  `pub(super) fn do_setresuid( &self, ruid: c_uint, euid: c_uint, suid: c_uint, ) -> Result<isize, Errno>`

  `pub(super) fn do_setresgid( &self, rgid: c_uint, egid: c_uint, sgid: c_uint, ) -> Result<isize, Errno>`

  `pub(super) fn do_getresuid( &self, ruid: LinuxUserspaceArg<*mut u8>, euid: LinuxUserspaceArg<*mut u8>, suid: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `pub(super) fn do_getresgid( &self, rgid: LinuxUserspaceArg<*mut u8>, egid: LinuxUserspaceArg<*mut u8>, sgid: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `pub(super) fn do_getgroups( &self, size: c_int, list: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `pub(super) fn do_setgroups( &self, size: c_int, list: LinuxUserspaceArg<*const u8>, ) -> Result<isize, Errno>`

  `pub(super) fn do_prctl( &self, option: c_int, arg2: c_ulong, _arg3: c_ulong, _arg4: c_ulong, _arg5: c_ulong, ) -> Result<isize, Errno>`

  `pub(super) async fn do_futex( &self, uaddr: usize, op: c_int, val: c_uint, ) -> Result<isize, Errno>`


---

## kernel/src/syscalls/io_ops.rs

**Language:** Rust | **Size:** 9.3 KB | **Lines:** 288

**Imports:**
- `alloc::vec::Vec`
- `core::ffi::{c_int, c_ulong}`
- `headers::{
    errno::Errno,
    syscall_types::{F_GETFL, F_SETFL, O_CLOEXEC, iovec},
}`
- `crate::{
    io::pipe,
    klibc::util::UsizeExt,
    processes::fd_table::{FdFlags, FileDescriptor},
    syscalls::linux_validator::LinuxUserspaceArg,
}`
- `super::linux::{LinuxSyscallHandler, LinuxSyscalls}`

**Declarations:**

**`impl LinuxSyscallHandler`**
  `pub(super) async fn do_read( &mut self, fd: c_int, buf: LinuxUserspaceArg<*mut u8>, count: usize, ) -> Result<isize, Errno>`

  `pub(super) async fn do_write( &self, fd: c_int, buf: LinuxUserspaceArg<*const u8>, count: usize, ) -> Result<isize, Errno>`

  `pub(super) async fn do_readv( &mut self, fd: c_int, iov: LinuxUserspaceArg<*const iovec>, iovcnt: c_int, ) -> Result<isize, Errno>`

  `pub(super) async fn do_writev( &self, fd: c_int, iov: LinuxUserspaceArg<*const iovec>, iovcnt: c_int, ) -> Result<isize, Errno>`

  `pub(super) async fn do_pread64( &mut self, fd: c_int, buf: LinuxUserspaceArg<*mut u8>, count: usize, offset: isize, ) -> Result<isize, Errno>`

  `pub(super) async fn do_pwrite64( &mut self, fd: c_int, buf: LinuxUserspaceArg<*const u8>, count: usize, offset: isize, ) -> Result<isize, Errno>`

  `pub(super) async fn do_sendfile( &mut self, out_fd: c_int, in_fd: c_int, offset: LinuxUserspaceArg<Option<*mut isize>>, count: usize, ) -> Result<isize, Errno>`

  `pub(super) fn do_pipe2(&self, fds: LinuxUserspaceArg<*mut c_int>) -> Result<isize, Errno>`

  `pub(super) fn do_fcntl(&self, fd: c_int, cmd: c_int, arg: c_ulong) -> Result<isize, Errno>`


---

## kernel/src/syscalls/ioctl_ops.rs

**Language:** Rust | **Size:** 5.3 KB | **Lines:** 132

**Imports:**
- `core::ffi::{c_int, c_uint}`
- `headers::{
    errno::Errno,
    syscall_types::{
        FIONBIO, TCGETS, TCSETS, TCSETSF, TCSETSW, TIOCGPGRP, TIOCGWINSZ, TIOCSPGRP, termios,
    },
}`
- `crate::{
    autogenerated::userspace_programs::PROGRAMS,
    net, print, println,
    processes::{
        fd_table::{FdFlags, FileDescriptor},
        process_table,
    },
    syscalls::linux_validator::LinuxUserspaceArg,
}`
- `common::{
    ioctl::{ARPHRD_ETHER, Ifreq, SIOCGIFHWADDR, SIOCSIFADDR, SOLAYA_LIST_PROGRAMS, SOLAYA_PANIC},
    pid::Tid,
}`
- `super::linux::{LinuxSyscallHandler, LinuxSyscalls}`

**Declarations:**

**`impl LinuxSyscallHandler`**
  `pub(super) fn do_ioctl(&self, fd: c_int, op: c_uint, arg: usize) -> Result<isize, Errno>`


---

## kernel/src/syscalls/linux.rs

**Language:** Rust | **Size:** 32.9 KB | **Lines:** 1028

**Imports:**
- `crate::{
    cpu::Cpu,
    debug, fs,
    klibc::util::{ByteInterpretable, UsizeExt},
    memory::{PAGE_SIZE, VirtAddr},
    processes::{
        fd_table::{FdFlags, FileDescriptor},
        process::ProcessRef,
        process_table,
        thread::ThreadRef,
    },
    syscalls::macros::linux_syscalls,
}`
- `common::{
    pid::Tid,
    syscalls::trap_frame::{Register, TrapFrame},
}`
- `core::ffi::{c_int, c_uint, c_ulong}`
- `headers::{
    errno::Errno,
    socket::sockaddr_in,
    syscall_types::{CLONE_THREAD, iovec, pollfd, sigaction, sigset_t, stack_t, timespec},
}`

**Declarations:**

**`impl ByteInterpretable for sockaddr_in`**

**`impl ByteInterpretable for headers::fs::stat`**

**`impl ByteInterpretable for headers::fs::statfs`**

**`impl ByteInterpretable for headers::fs::statx`**

**`impl ByteInterpretable for headers::syscall_types::termios`**

**`impl ByteInterpretable for headers::sysinfo_types::utsname`**

**`impl ByteInterpretable for headers::sysinfo_types::sysinfo`**

**`impl ByteInterpretable for headers::sysinfo_types::rusage`**

**`impl ByteInterpretable for headers::sysinfo_types::rlimit`**

**`impl LinuxSyscalls for LinuxSyscallHandler`**
  `async fn read( &mut self, fd: c_int, buf: LinuxUserspaceArg<*mut u8>, count: usize, ) -> Result<isize, Errno>`

  `async fn write( &mut self, fd: c_int, buf: LinuxUserspaceArg<*const u8>, count: usize, ) -> Result<isize, Errno>`

  `async fn readv( &mut self, fd: c_int, iov: LinuxUserspaceArg<*const iovec>, iovcnt: c_int, ) -> Result<isize, Errno>`

  `async fn writev( &mut self, fd: c_int, iov: LinuxUserspaceArg<*const iovec>, iovcnt: c_int, ) -> Result<isize, Errno>`

  `async fn pread64( &mut self, fd: c_int, buf: LinuxUserspaceArg<*mut u8>, count: usize, offset: isize, ) -> Result<isize, Errno>`

  `async fn pwrite64( &mut self, fd: c_int, buf: LinuxUserspaceArg<*const u8>, count: usize, offset: isize, ) -> Result<isize, Errno>`

  `async fn close(&mut self, fd: c_int) -> Result<isize, Errno>`

  `async fn dup(&mut self, oldfd: c_int) -> Result<isize, Errno>`

  `async fn dup3(&mut self, oldfd: c_int, newfd: c_int, flags: c_int) -> Result<isize, Errno>`

  `async fn pipe2( &mut self, fds: LinuxUserspaceArg<*mut c_int>, _flags: c_int, ) -> Result<isize, Errno>`

  `async fn fcntl(&mut self, fd: c_int, cmd: c_int, arg: c_ulong) -> Result<isize, Errno>`

  `async fn lseek(&mut self, fd: c_int, offset: isize, whence: c_int) -> Result<isize, Errno>`

  `async fn ioctl(&mut self, fd: c_int, op: c_uint, arg: usize) -> Result<isize, Errno>`

  `async fn openat( &mut self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, flags: c_int, mode: c_uint, ) -> Result<isize, Errno>`

  `async fn fstat( &mut self, fd: c_int, statbuf: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `async fn fstatfs( &mut self, fd: c_int, buf: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `async fn newfstatat( &mut self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, statbuf: LinuxUserspaceArg<*mut u8>, flags: c_int, ) -> Result<isize, Errno>`

  `async fn statfs( &mut self, pathname: LinuxUserspaceArg<*const u8>, buf: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `async fn statx( &mut self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, flags: c_int, mask: c_uint, statxbuf: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `async fn getdents64( &mut self, fd: c_int, dirp: LinuxUserspaceArg<*mut u8>, count: usize, ) -> Result<isize, Errno>`

  `async fn faccessat( &mut self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, mode: c_int, ) -> Result<isize, Errno>`

  `async fn faccessat2( &mut self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, mode: c_int, _flags: c_int, ) -> Result<isize, Errno>`

  `async fn fchmod(&mut self, fd: c_int, mode: c_uint) -> Result<isize, Errno>`

  `async fn fchmodat( &mut self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, mode: c_uint, ) -> Result<isize, Errno>`

  `async fn fchown(&mut self, fd: c_int, uid: c_uint, gid: c_uint) -> Result<isize, Errno>`

  `async fn fchownat( &mut self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, uid: c_uint, gid: c_uint, _flags: c_int, ) -> Result<isize, Errno>`

  `async fn ftruncate(&mut self, fd: c_int, length: isize) -> Result<isize, Errno>`

  `async fn mkdirat( &mut self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, mode: c_uint, ) -> Result<isize, Errno>`

  `async fn unlinkat( &mut self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, flags: c_int, ) -> Result<isize, Errno>`

  `async fn chdir(&mut self, pathname: LinuxUserspaceArg<*const u8>) -> Result<isize, Errno>`

  `async fn getcwd( &mut self, buf: LinuxUserspaceArg<*mut u8>, size: usize, ) -> Result<isize, Errno>`

  `async fn umask(&mut self, mask: c_uint) -> Result<isize, Errno>`

  `async fn brk(&mut self, brk: c_ulong) -> Result<isize, Errno>`

  `async fn mmap( &mut self, addr: usize, length: usize, prot: c_uint, flags: c_uint, fd: c_int, offset: isize, ) -> Result<isize, Errno>`

  `async fn mprotect(&mut self, addr: usize, len: usize, prot: c_int) -> Result<isize, Errno>`

  `async fn munmap(&mut self, addr: usize, length: usize) -> Result<isize, Errno>`

  `async fn exit(&mut self, status: c_int) -> Result<isize, Errno>`

  `async fn exit_group(&mut self, status: c_int) -> Result<isize, Errno>`

  `async fn wait4( &mut self, pid: c_int, status: LinuxUserspaceArg<Option<*mut c_int>>, options: c_int, _rusage: usize, ) -> Result<isize, Errno>`

  `async fn clone( &mut self, flags: c_ulong, stack: usize, ptid: LinuxUserspaceArg<Option<*mut c_int>>, tls: c_ulong, ctid: LinuxUserspaceArg<Option<*mut c_int>>, ) -> Result<isize, Errno>`

  `async fn execve(&mut self, filename: usize, argv: usize, envp: usize) -> Result<isize, Errno>`

  `async fn rt_sigaction( &mut self, sig: c_uint, act: LinuxUserspaceArg<Option<*const sigaction>>, oact: LinuxUserspaceArg<Option<*mut sigaction>>, sigsetsize: usize, ) -> Result<isize, Errno>`

  `async fn rt_sigprocmask( &mut self, how: c_uint, set: LinuxUserspaceArg<Option<*const sigset_t>>, oldset: LinuxUserspaceArg<Option<*mut sigset_t>>, sigsetsize: usize, ) -> Result<isize, Errno>`

  `async fn rt_sigreturn(&mut self) -> Result<isize, Errno>`

  `async fn sigaltstack( &mut self, uss: LinuxUserspaceArg<Option<*const stack_t>>, uoss: LinuxUserspaceArg<Option<*mut stack_t>>, ) -> Result<isize, Errno>`

  `async fn kill(&mut self, pid: c_int, sig: c_int) -> Result<isize, Errno>`

  `async fn tgkill(&mut self, _tgid: c_int, tid: c_int, sig: c_int) -> Result<isize, Errno>`

  `async fn tkill(&mut self, tid: c_int, sig: c_int) -> Result<isize, Errno>`

  `async fn truncate( &mut self, pathname: LinuxUserspaceArg<*const u8>, length: isize, ) -> Result<isize, Errno>`

  `async fn socket(&mut self, domain: c_int, typ: c_int, protocol: c_int) -> Result<isize, Errno>`

  `async fn bind( &mut self, fd: c_int, addr: LinuxUserspaceArg<*const u8>, addrlen: c_uint, ) -> Result<isize, Errno>`

  `async fn sendfile( &mut self, out_fd: c_int, in_fd: c_int, offset: LinuxUserspaceArg<Option<*mut isize>>, count: usize, ) -> Result<isize, Errno>`

  `async fn sendto( &mut self, fd: c_int, buf: LinuxUserspaceArg<*const u8>, len: usize, flags: c_int, dest_addr: LinuxUserspaceArg<*const u8>, addrlen: c_uint, ) -> Result<isize, Errno>`

  `async fn recvfrom( &mut self, fd: c_int, buf: LinuxUserspaceArg<*mut u8>, len: usize, flags: c_int, src_addr: LinuxUserspaceArg<Option<*mut u8>>, addrlen: LinuxUserspaceArg<Option<*mut c_uint>>, ) -> Result<isize, Errno>`

  `async fn connect( &mut self, fd: c_int, addr: LinuxUserspaceArg<*const u8>, addrlen: c_uint, ) -> Result<isize, Errno>`

  `async fn listen(&mut self, fd: c_int, _backlog: c_int) -> Result<isize, Errno>`

  `async fn accept4( &mut self, fd: c_int, addr: LinuxUserspaceArg<Option<*mut u8>>, addrlen: LinuxUserspaceArg<Option<*mut c_uint>>, _flags: c_int, ) -> Result<isize, Errno>`

  `async fn setsockopt( &mut self, _fd: c_int, _level: c_int, _optname: c_int, _optval: LinuxUserspaceArg<*const u8>, _optlen: c_uint, ) -> Result<isize, Errno>`

  `async fn getsockname( &mut self, fd: c_int, addr: LinuxUserspaceArg<*mut u8>, addrlen: LinuxUserspaceArg<*mut c_uint>, ) -> Result<isize, Errno>`

  `async fn getpeername( &mut self, fd: c_int, addr: LinuxUserspaceArg<*mut u8>, addrlen: LinuxUserspaceArg<*mut c_uint>, ) -> Result<isize, Errno>`

  `async fn shutdown(&mut self, fd: c_int, _how: c_int) -> Result<isize, Errno>`

  `async fn nanosleep( &mut self, duration: LinuxUserspaceArg<*const timespec>, _rem: LinuxUserspaceArg<Option<*const timespec>>, ) -> Result<isize, Errno>`

  `async fn clock_nanosleep( &mut self, clockid: c_int, flags: c_int, request: LinuxUserspaceArg<*const timespec>, _remain: LinuxUserspaceArg<Option<*mut timespec>>, ) -> Result<isize, Errno>`

  `async fn clock_gettime( &mut self, _clockid: c_int, tp: LinuxUserspaceArg<*mut timespec>, ) -> Result<isize, Errno>`

  `async fn ppoll( &mut self, fds: LinuxUserspaceArg<*mut pollfd>, n: c_uint, to: LinuxUserspaceArg<Option<*const timespec>>, mask: LinuxUserspaceArg<Option<*const sigset_t>>, ) -> Result<isize, Errno>`

  `async fn getpid(&mut self) -> Result<isize, Errno>`

  `async fn getppid(&mut self) -> Result<isize, Errno>`

  `async fn gettid(&mut self) -> Result<isize, Errno>`

  `async fn getpgid(&mut self, pid: c_int) -> Result<isize, Errno>`

  `async fn getsid(&mut self, pid: c_int) -> Result<isize, Errno>`

  `async fn setpgid(&mut self, pid: c_int, pgid: c_int) -> Result<isize, Errno>`

  `async fn setrlimit( &mut self, _resource: c_uint, _rlim: LinuxUserspaceArg<*const u8>, ) -> Result<isize, Errno>`

  `async fn setsid(&mut self) -> Result<isize, Errno>`

  `async fn set_tid_address( &mut self, tidptr: LinuxUserspaceArg<*mut c_int>, ) -> Result<isize, Errno>`

  `async fn futex( &mut self, uaddr: usize, op: c_int, val: c_uint, _timeout: usize, _uaddr2: usize, _val3: c_uint, ) -> Result<isize, Errno>`

  `async fn uname(&mut self, buf: LinuxUserspaceArg<*mut u8>) -> Result<isize, Errno>`

  `async fn sysinfo(&mut self, info: LinuxUserspaceArg<*mut u8>) -> Result<isize, Errno>`

  `async fn getrlimit( &mut self, resource: c_uint, rlim: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `async fn getrusage( &mut self, who: c_int, usage: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `async fn getrandom( &mut self, buf: LinuxUserspaceArg<*mut u8>, buflen: usize, _flags: c_uint, ) -> Result<isize, Errno>`

  `fn get_process(&self) -> ProcessRef`

  `async fn sched_getaffinity( &mut self, pid: c_int, cpusetsize: usize, mask: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `async fn set_robust_list(&mut self, _head: usize, _len: usize) -> Result<isize, Errno>`

  `async fn madvise( &mut self, _addr: usize, _length: usize, _advice: c_int, ) -> Result<isize, Errno>`

  `async fn fadvise64( &mut self, _fd: c_int, _offset: isize, _len: isize, _advice: c_int, ) -> Result<isize, Errno>`

  `async fn listxattr( &mut self, _pathname: LinuxUserspaceArg<*const u8>, _list: LinuxUserspaceArg<*mut u8>, _size: usize, ) -> Result<isize, Errno>`

  `async fn llistxattr( &mut self, _pathname: LinuxUserspaceArg<*const u8>, _list: LinuxUserspaceArg<*mut u8>, _size: usize, ) -> Result<isize, Errno>`

  `async fn utimensat( &mut self, _dirfd: c_int, _pathname: LinuxUserspaceArg<*const u8>, _times: usize, _flags: c_int, ) -> Result<isize, Errno>`

  `async fn prctl( &mut self, option: c_int, arg2: c_ulong, arg3: c_ulong, arg4: c_ulong, arg5: c_ulong, ) -> Result<isize, Errno>`

  `async fn prlimit64( &mut self, pid: c_int, resource: c_uint, _new_limit: LinuxUserspaceArg<Option<*const u8>>, old_limit: LinuxUserspaceArg<Option<*mut u8>>, ) -> Result<isize, Errno>`

  `async fn readlinkat( &mut self, dirfd: c_int, pathname: LinuxUserspaceArg<*const u8>, buf: LinuxUserspaceArg<*mut u8>, bufsiz: usize, ) -> Result<isize, Errno>`

  `async fn symlinkat( &mut self, target: LinuxUserspaceArg<*const u8>, _newdirfd: c_int, linkpath: LinuxUserspaceArg<*const u8>, ) -> Result<isize, Errno>`

  `async fn linkat( &mut self, _olddirfd: c_int, oldpath: LinuxUserspaceArg<*const u8>, _newdirfd: c_int, newpath: LinuxUserspaceArg<*const u8>, _flags: c_int, ) -> Result<isize, Errno>`

  `async fn renameat2( &mut self, _olddirfd: c_int, oldpath: LinuxUserspaceArg<*const u8>, _newdirfd: c_int, newpath: LinuxUserspaceArg<*const u8>, flags: c_uint, ) -> Result<isize, Errno>`

  `async fn getuid(&mut self) -> Result<isize, Errno>`

  `async fn geteuid(&mut self) -> Result<isize, Errno>`

  `async fn getgid(&mut self) -> Result<isize, Errno>`

  `async fn getegid(&mut self) -> Result<isize, Errno>`

  `async fn setuid(&mut self, uid: c_uint) -> Result<isize, Errno>`

  `async fn setgid(&mut self, gid: c_uint) -> Result<isize, Errno>`

  `async fn setreuid(&mut self, ruid: c_uint, euid: c_uint) -> Result<isize, Errno>`

  `async fn setregid(&mut self, rgid: c_uint, egid: c_uint) -> Result<isize, Errno>`

  `async fn setresuid( &mut self, ruid: c_uint, euid: c_uint, suid: c_uint, ) -> Result<isize, Errno>`

  `async fn setresgid( &mut self, rgid: c_uint, egid: c_uint, sgid: c_uint, ) -> Result<isize, Errno>`

  `async fn getresuid( &mut self, ruid: LinuxUserspaceArg<*mut u8>, euid: LinuxUserspaceArg<*mut u8>, suid: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `async fn getresgid( &mut self, rgid: LinuxUserspaceArg<*mut u8>, egid: LinuxUserspaceArg<*mut u8>, sgid: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `async fn getgroups( &mut self, size: c_int, list: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `async fn setgroups( &mut self, size: c_int, list: LinuxUserspaceArg<*const u8>, ) -> Result<isize, Errno>`

  `async fn splice( &mut self, _fd_in: c_int, _off_in: usize, _fd_out: c_int, _off_out: usize, _len: usize, _flags: c_uint, ) -> Result<isize, Errno>`


**`impl LinuxSyscallHandler`**
  `pub fn new() -> Self`


---

## kernel/src/syscalls/linux_validator.rs

**Language:** Rust | **Size:** 2.7 KB | **Lines:** 98

**Imports:**
- `crate::processes::{process::ProcessRef, userspace_ptr::UserspacePtr}`
- `alloc::vec::Vec`
- `common::pointer::Pointer`
- `core::marker::PhantomData`
- `headers::errno::Errno`

**Declarations:**

**`impl<T> LinuxUserspaceArg<T>`**
  `pub fn new(arg: usize, process: ProcessRef) -> Self`

  `pub fn arg_nonzero(&self) -> bool`

  `pub fn raw_arg(&self) -> usize`


**`impl<T: Copy> LinuxUserspaceArg<*const T>`**
  `pub fn validate_ptr(&self) -> Result<T, Errno>`


**`impl<T: Copy> LinuxUserspaceArg<Option<*const T>>`**
  `pub fn validate_ptr(&self) -> Result<Option<T>, Errno>`


**`impl<T: Copy> LinuxUserspaceArg<*const T>`**
  `pub fn validate_slice(&self, len: usize) -> Result<Vec<T>, Errno>`


**`impl<T: Copy> LinuxUserspaceArg<*mut T>`**
  `pub fn validate_slice(&self, len: usize) -> Result<Vec<T>, Errno>`

  `pub fn write_slice(&self, values: &[T]) -> Result<(), Errno>`


**`impl<T: Clone> LinuxUserspaceArg<Option<*mut T>>`**
  `pub fn write_if_not_none(&self, value: T) -> Result<Option<()>, Errno>`


**`impl<PTR: Pointer> From<&LinuxUserspaceArg<PTR>> for UserspacePtr<PTR>`**
  `fn from(value: &LinuxUserspaceArg<PTR>) -> Self`


**`impl<PTR: Pointer> From<&LinuxUserspaceArg<Option<PTR>>> for UserspacePtr<PTR>`**
  `fn from(value: &LinuxUserspaceArg<Option<PTR>>) -> Self`


**`impl<T> From<&LinuxUserspaceArg<*mut T>> for UserspacePtr<*const T>`**
  `fn from(value: &LinuxUserspaceArg<*mut T>) -> Self`


---

## kernel/src/syscalls/macros.rs

**Language:** Rust | **Size:** 4.1 KB | **Lines:** 126

**Imports:**
- `core::ffi::{c_int, c_uint, c_ulong}`
- `pub(super) use linux_syscalls`
- `crate::{processes::process::ProcessRef, syscalls::linux_validator::LinuxUserspaceArg}`

**Declarations:**

**`impl NeedsUserSpaceWrapper for c_int`**
  `fn wrap_arg( value: usize, _process: ProcessRef, ) -> Result<Self::Wrapped, headers::errno::Errno>`


**`impl NeedsUserSpaceWrapper for c_uint`**
  `fn wrap_arg( value: usize, _process: ProcessRef, ) -> Result<Self::Wrapped, headers::errno::Errno>`


**`impl NeedsUserSpaceWrapper for c_ulong`**
  `fn wrap_arg( value: usize, _process: ProcessRef, ) -> Result<Self::Wrapped, headers::errno::Errno>`


**`impl NeedsUserSpaceWrapper for usize`**
  `fn wrap_arg( value: usize, _process: ProcessRef, ) -> Result<Self::Wrapped, headers::errno::Errno>`


**`impl NeedsUserSpaceWrapper for isize`**
  `fn wrap_arg( value: usize, _process: ProcessRef, ) -> Result<Self::Wrapped, headers::errno::Errno>`


---

## kernel/src/syscalls/mm_ops.rs

**Language:** Rust | **Size:** 3.2 KB | **Lines:** 90

**Imports:**
- `core::ffi::c_uint`
- `headers::{
    errno::Errno,
    syscall_types::{MAP_ANONYMOUS, MAP_FIXED, MAP_PRIVATE},
}`
- `crate::memory::{PAGE_SIZE, Pages, VirtAddr, page_tables::XWRMode}`
- `super::linux::LinuxSyscallHandler`

**Declarations:**

**`impl LinuxSyscallHandler`**
  `pub(super) fn do_mmap( &self, addr: usize, length: usize, prot: c_uint, flags: c_uint, fd: i32, offset: isize, ) -> Result<isize, Errno>`

  `pub(super) fn do_mprotect(&self, addr: usize, len: usize, prot: i32) -> Result<isize, Errno>`


---

## kernel/src/syscalls/mod.rs

**Language:** Rust | **Size:** 257 B | **Lines:** 17

**Declarations:**

`mod exec_ops`

`mod fs_ops`

`mod helpers`

`mod id_ops`

`mod io_ops`

`mod ioctl_ops`

`mod macros`

`mod mm_ops`

`mod net_ops`

`mod process_ops`

`mod signal_ops`

`mod sysinfo_ops`

`mod time_ops`

---

## kernel/src/syscalls/net_ops.rs

**Language:** Rust | **Size:** 14.1 KB | **Lines:** 419

**Imports:**
- `core::ffi::{c_int, c_uint}`
- `headers::{
    errno::Errno,
    socket::{AF_INET, SOCK_CLOEXEC, SOCK_DGRAM, SOCK_STREAM, sockaddr_in},
}`
- `crate::{
    klibc::util::ByteInterpretable,
    net::{
        self, arp,
        sockets::{Port, SharedAssignedSocket},
        tcp_connection,
        udp::UdpHeader,
    },
    processes::fd_table::FileDescriptor,
    syscalls::linux_validator::LinuxUserspaceArg,
}`
- `super::linux::{LinuxSyscallHandler, LinuxSyscalls}`

**Declarations:**

**`impl LinuxSyscallHandler`**
  `pub(super) fn do_socket( &self, domain: c_int, typ: c_int, _protocol: c_int, ) -> Result<isize, Errno>`

  `pub(super) fn do_bind( &self, fd: c_int, addr: LinuxUserspaceArg<*const u8>, addrlen: c_uint, ) -> Result<isize, Errno>`

  `pub(super) fn do_sendto( &self, fd: c_int, buf: LinuxUserspaceArg<*const u8>, len: usize, _flags: c_int, dest_addr: LinuxUserspaceArg<*const u8>, addrlen: c_uint, ) -> Result<isize, Errno>`

  `pub(super) async fn do_recvfrom( &self, fd: c_int, buf: LinuxUserspaceArg<*mut u8>, len: usize, _flags: c_int, src_addr: LinuxUserspaceArg<Option<*mut u8>>, addrlen: LinuxUserspaceArg<Option<*mut c_uint>>, ) -> Result<isize, Errno>`

  `pub(super) async fn do_connect( &self, fd: c_int, addr: LinuxUserspaceArg<*const u8>, addrlen: c_uint, ) -> Result<isize, Errno>`

  `pub(super) fn do_listen(&self, fd: c_int) -> Result<isize, Errno>`

  `pub(super) async fn do_accept( &self, fd: c_int, addr: LinuxUserspaceArg<Option<*mut u8>>, addrlen: LinuxUserspaceArg<Option<*mut c_uint>>, ) -> Result<isize, Errno>`

  `pub(super) fn do_getsockname( &self, fd: c_int, addr: LinuxUserspaceArg<*mut u8>, addrlen: LinuxUserspaceArg<*mut c_uint>, ) -> Result<isize, Errno>`

  `pub(super) fn do_getpeername( &self, fd: c_int, addr: LinuxUserspaceArg<*mut u8>, addrlen: LinuxUserspaceArg<*mut c_uint>, ) -> Result<isize, Errno>`

  `pub(super) fn do_shutdown(&self, fd: c_int) -> Result<isize, Errno>`


---

## kernel/src/syscalls/process_ops.rs

**Language:** Rust | **Size:** 5.9 KB | **Lines:** 186

**Imports:**
- `alloc::{collections::BTreeMap, string::String, sync::Arc}`
- `core::ffi::{c_int, c_ulong}`
- `headers::{
    errno::Errno,
    syscall_types::{CLONE_CHILD_CLEARTID, CLONE_PARENT_SETTID, CLONE_SETTLS},
}`
- `crate::{
    cpu::Cpu,
    klibc::Spinlock,
    memory::VirtAddr,
    processes::{
        process::Process,
        process_table,
        thread::{Thread, get_next_tid},
        wait_child::{WaitChild, WaitPid},
    },
    syscalls::linux_validator::LinuxUserspaceArg,
}`
- `common::{pid::Tid, syscalls::trap_frame::Register}`
- `super::linux::LinuxSyscallHandler`

**Declarations:**

**`impl LinuxSyscallHandler`**
  `pub(super) async fn clone_fork(&mut self, stack: usize) -> Result<isize, Errno>`

  `pub(super) fn clone_thread( &mut self, flags: c_ulong, stack: usize, ptid: LinuxUserspaceArg<Option<*mut c_int>>, tls: c_ulong, ctid: LinuxUserspaceArg<Option<*mut c_int>>, ) -> Result<isize, Errno>`

  `pub(super) async fn do_wait4( &self, pid: c_int, status: LinuxUserspaceArg<Option<*mut c_int>>, options: c_int, ) -> Result<isize, Errno>`


---

## kernel/src/syscalls/signal_ops.rs

**Language:** Rust | **Size:** 3.7 KB | **Lines:** 116

**Imports:**
- `core::ffi::{c_int, c_uint}`
- `headers::{
    errno::Errno,
    syscall_types::{
        _NSIG, SIG_BLOCK, SIG_SETMASK, SIG_UNBLOCK, SIGKILL, SIGSTOP, sigaction, sigset_t, stack_t,
    },
}`
- `crate::{processes::process_table, syscalls::linux_validator::LinuxUserspaceArg}`
- `common::pid::Tid`
- `super::linux::LinuxSyscallHandler`

**Declarations:**

**`impl LinuxSyscallHandler`**
  `pub(super) fn do_rt_sigaction( &self, sig: c_uint, act: LinuxUserspaceArg<Option<*const sigaction>>, oact: LinuxUserspaceArg<Option<*mut sigaction>>, sigsetsize: usize, ) -> Result<isize, Errno>`

  `pub(super) fn do_rt_sigprocmask( &self, how: c_uint, set: LinuxUserspaceArg<Option<*const sigset_t>>, oldset: LinuxUserspaceArg<Option<*mut sigset_t>>, sigsetsize: usize, ) -> Result<isize, Errno>`

  `pub(super) fn do_sigaltstack( &self, uss: LinuxUserspaceArg<Option<*const stack_t>>, uoss: LinuxUserspaceArg<Option<*mut stack_t>>, ) -> Result<isize, Errno>`

  `pub(super) fn do_kill(&self, pid: c_int, sig: c_int) -> Result<isize, Errno>`


---

## kernel/src/syscalls/sysinfo_ops.rs

**Language:** Rust | **Size:** 5.1 KB | **Lines:** 176

**Imports:**
- `alloc::vec`
- `core::{
    ffi::{c_int, c_uint},
    sync::atomic::{AtomicU64, Ordering},
}`
- `headers::errno::Errno`
- `crate::{
    cpu::Cpu,
    drivers::virtio::rng,
    klibc::util::ByteInterpretable,
    memory::{self, PAGE_SIZE},
    processes::{process_table, timer},
    syscalls::linux_validator::LinuxUserspaceArg,
}`
- `super::linux::LinuxSyscallHandler`

**Declarations:**

`trait CStrField`
  `fn copy_from(&mut self, src: &str)`


**`impl CStrField for [core::ffi::c_char]`**
  `fn copy_from(&mut self, src: &str)`


**`impl LinuxSyscallHandler`**
  `pub(super) fn do_uname(&self, buf: LinuxUserspaceArg<*mut u8>) -> Result<isize, Errno>`

  `pub(super) fn do_sysinfo(&self, info: LinuxUserspaceArg<*mut u8>) -> Result<isize, Errno>`

  `pub(super) fn do_getrusage( &self, _who: c_int, usage: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `pub(super) fn do_getrlimit( &self, resource: c_uint, rlim: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `pub(super) fn do_prlimit64( &self, pid: c_int, resource: c_uint, old_limit: LinuxUserspaceArg<Option<*mut u8>>, ) -> Result<isize, Errno>`

  `pub(super) fn do_sched_getaffinity( &self, _pid: c_int, cpusetsize: usize, mask: LinuxUserspaceArg<*mut u8>, ) -> Result<isize, Errno>`

  `pub(super) fn do_getrandom( &self, buf: LinuxUserspaceArg<*mut u8>, buflen: usize, ) -> Result<isize, Errno>`


`fn get_default_rlimit(resource: u32) -> (u64, u64)`

`static PRNG_STATE: AtomicU64 = AtomicU64::new(0)`

`fn xorshift_fill(buf: &mut [u8])`

---

## kernel/src/syscalls/time_ops.rs

**Language:** Rust | **Size:** 2.2 KB | **Lines:** 72

**Imports:**
- `core::ffi::{c_int, c_uint}`
- `headers::{
    errno::Errno,
    syscall_types::{CLOCK_MONOTONIC, CLOCK_REALTIME, pollfd, sigset_t, timespec},
}`
- `crate::{processes::timer, syscalls::linux_validator::LinuxUserspaceArg}`
- `super::linux::LinuxSyscallHandler`

**Declarations:**

**`impl LinuxSyscallHandler`**
  `pub(super) async fn do_nanosleep( &self, duration: LinuxUserspaceArg<*const timespec>, ) -> Result<isize, Errno>`

  `pub(super) async fn do_clock_nanosleep( &self, clockid: c_int, flags: c_int, request: LinuxUserspaceArg<*const timespec>, ) -> Result<isize, Errno>`

  `pub(super) fn do_ppoll( &self, fds: LinuxUserspaceArg<*mut pollfd>, n: c_uint, to: LinuxUserspaceArg<Option<*const timespec>>, mask: LinuxUserspaceArg<Option<*const sigset_t>>, ) -> Result<isize, Errno>`


---

## kernel/src/syscalls/trace_config.rs

**Language:** Rust | **Size:** 50 B | **Lines:** 1

**Declarations:**

---

## kernel/src/syscalls/tracer.rs

**Language:** Rust | **Size:** 7.9 KB | **Lines:** 279

**Imports:**
- `crate::{
    autogenerated::userspace_programs::PROGRAMS,
    cpu::Cpu,
    debugging::{
        eh_frame_parser::EhFrameParser,
        unwinder::{RegisterRule, Unwinder},
    },
    klibc::{elf::ElfFile, util::UsizeExt},
    println,
    processes::userspace_ptr::UserspacePtr,
    syscalls::{
        linux::{LinuxSyscallHandler, LinuxSyscalls, SYSCALL_METADATA},
        trace_config::TRACED_PROCESSES,
    },
}`
- `alloc::vec::Vec`
- `common::syscalls::trap_frame::{Register, TrapFrame}`
- `core::ffi::{c_int, c_uint, c_ulong}`
- `headers::errno::Errno`

**Declarations:**

**`impl SyscallArgFormat for c_int`**

**`impl SyscallArgFormat for c_uint`**

**`impl SyscallArgFormat for c_ulong`**

**`impl SyscallArgFormat for usize`**

**`impl SyscallArgFormat for isize`**

**`impl<T> SyscallArgFormat for *const T`**

**`impl<T> SyscallArgFormat for *mut T`**

**`impl<T> SyscallArgFormat for Option<*const T>`**

**`impl<T> SyscallArgFormat for Option<*mut T>`**

`fn should_trace() -> bool`

`fn find_metadata(nr: usize) -> Option<&'static SyscallMetadata>`

`fn format_arg(raw: usize, fmt: ArgFormat) -> alloc::string::String`

`fn log_enter(trap_frame: &TrapFrame, tid: common::pid::Tid)`

`fn log_exit(trap_frame: &TrapFrame, tid: common::pid::Tid, result: &Result<isize, Errno>)`

`fn read_userspace_usize(addr: usize) -> Option<usize>`

`fn print_userspace_backtrace(trap_frame: &TrapFrame, process_name: &str)`

`fn print_frame(num: u32, addr: usize, elf: &Option<ElfFile<'_>>)`

---

## kernel/src/test/array_vec.rs

**Language:** Rust | **Size:** 2.3 KB | **Lines:** 87

**Declarations:**

`mod tests`

---

## kernel/src/test/leb128.rs

**Language:** Rust | **Size:** 1.8 KB | **Lines:** 61

**Declarations:**

`mod tests`

---

## kernel/src/test/mod.rs

**Language:** Rust | **Size:** 1.4 KB | **Lines:** 51

**Imports:**
- `crate::{print, println}`

**Declarations:**

`mod array_vec`

`mod leb128`

**`impl<T> Testable for T where T: Fn(),`**
  `fn run(&self)`


---

## kernel/src/test/qemu_exit.rs

**Language:** Rust | **Size:** 803 B | **Lines:** 35

**Imports:**
- `crate::{
    io::TEST_DEVICE_ADDRESS,
    klibc::{MMIO, Spinlock},
}`

**Declarations:**

`const EXIT_SUCCESS_CODE: u32 = 0x5555`

`const EXIT_FAILURE_CODE: u32 = 0x3333`

`const EXIT_RESET_CODE: u32 = 0x7777`

`static TEST_DEVICE: Spinlock<MMIO<u32>> = Spinlock::new(MMIO::new(TEST_DEVICE_ADDRESS))`

---

## mcp-server/Cargo.toml

**Language:** TOML | **Size:** 444 B | **Lines:** 15

**Imports:**
- `anyhow`
- `tokio`
- `rmcp`
- `schemars`
- `serde`
- `serde_json`
- `tracing-subscriber`
- `qemu-infra`

**Declarations:**

---

## mcp-server/src/main.rs

**Language:** Rust | **Size:** 526 B | **Lines:** 23

**Imports:**
- `anyhow::Result`
- `rmcp::{ServiceExt, transport::stdio}`
- `server::QemuMcpServer`

**Declarations:**

`mod server`

`async fn main() -> Result<()>`

---

## mcp-server/src/server.rs

**Language:** Rust | **Size:** 15.8 KB | **Lines:** 450

**Imports:**
- `std::{sync::Arc, time::Duration}`
- `qemu_infra::qemu::{QemuInstance, QemuOptions, project_root}`
- `rmcp::{
    ErrorData as McpError, RoleServer, ServerHandler,
    handler::server::{router::tool::ToolRouter, tool::ToolCallContext, wrapper::Parameters},
    model::*,
    schemars,
    service::RequestContext,
    tool, tool_router,
}`
- `tokio::{io::AsyncWriteExt, process::Command, sync::Mutex}`

**Declarations:**

**`impl QemuMcpServer`**
  `pub fn new() -> Self`


`fn mcp_err(msg: impl Into<String>) -> McpError`

`fn text_result(text: impl Into<String>) -> Result<CallToolResult, McpError>`

`fn require_running(guard: &mut Option<QemuInstance>) -> Result<&mut QemuInstance, McpError>`

`fn format_command_output(label: &str, success_word: &str, output: &std::process::Output) -> String`

`async fn lock_qemu( qemu: &Mutex<Option<QemuInstance>>, ) -> Result<tokio::sync::MutexGuard<'_, Option<QemuInstance>>, McpError>`

`async fn run_command(mut cmd: Command) -> Result<std::process::Output, std::io::Error>`

**`impl QemuMcpServer`**
  `async fn boot_qemu( &self, Parameters(params): Parameters<BootParams>, ) -> Result<CallToolResult, McpError>`

  `async fn shutdown_qemu(&self) -> Result<CallToolResult, McpError>`

  `async fn get_status(&self) -> Result<CallToolResult, McpError>`

  `async fn send_command( &self, Parameters(params): Parameters<SendCommandParams>, ) -> Result<CallToolResult, McpError>`

  `async fn send_input( &self, Parameters(params): Parameters<SendInputParams>, ) -> Result<CallToolResult, McpError>`

  `async fn send_ctrl_c(&self) -> Result<CallToolResult, McpError>`

  `async fn screenshot( &self, Parameters(params): Parameters<ScreenshotParams>, ) -> Result<CallToolResult, McpError>`

  `async fn read_output(&self) -> Result<CallToolResult, McpError>`

  `async fn build_kernel( &self, Parameters(params): Parameters<BuildKernelParams>, ) -> Result<CallToolResult, McpError>`

  `async fn run_system_tests( &self, Parameters(params): Parameters<RunSystemTestsParams>, ) -> Result<CallToolResult, McpError>`


**`impl ServerHandler for QemuMcpServer`**
  `fn get_info(&self) -> ServerInfo`

  `async fn call_tool( &self, request: CallToolRequestParams, context: RequestContext<RoleServer>, ) -> Result<CallToolResult, McpError>`

  `async fn list_tools( &self, _request: Option<PaginatedRequestParams>, _context: RequestContext<RoleServer>, ) -> Result<ListToolsResult, McpError>`

  `fn get_tool(&self, name: &str) -> Option<Tool>`


---

## plans/00-overview.md

**Language:** Markdown | **Size:** 6.5 KB | **Lines:** 127

**Declarations:**

---

## plans/01-roadmap.md

**Language:** Markdown | **Size:** 32.0 KB | **Lines:** 708

**Declarations:**

---

## plans/02-testing-strategy.md

**Language:** Markdown | **Size:** 29.8 KB | **Lines:** 793

**Declarations:**

---

## plans/03-driver-framework.md

**Language:** Markdown | **Size:** 30.5 KB | **Lines:** 713

**Declarations:**

---

## plans/04-build-system.md

**Language:** Markdown | **Size:** 29.6 KB | **Lines:** 568

**Declarations:**

---

## plans/05-organization.md

**Language:** Markdown | **Size:** 25.7 KB | **Lines:** 521

**Declarations:**

---

## qemu-infra/Cargo.toml

**Language:** TOML | **Size:** 261 B | **Lines:** 10

**Imports:**
- `anyhow`
- `image`
- `tokio`

**Declarations:**

---

## qemu-infra/src/lib.rs

**Language:** Rust | **Size:** 129 B | **Lines:** 7

**Declarations:**

`pub(crate) mod searchable_buffer`

---

## qemu-infra/src/ppm.rs

**Language:** Rust | **Size:** 3.8 KB | **Lines:** 131

**Imports:**
- `std::path::Path`
- `anyhow::anyhow`
- `image::{ImageBuffer, Rgb}`

**Declarations:**

**`impl PpmImage`**
  `pub fn from_file(path: &Path) -> anyhow::Result<Self>`

  `pub fn parse(data: &[u8]) -> anyhow::Result<Self>`

  `pub fn pixel(&self, x: u32, y: u32) -> (u8, u8, u8)`

  `pub fn has_non_black_pixels(&self) -> bool`

  `pub fn to_png_bytes(&self) -> anyhow::Result<Vec<u8>>`


`fn read_token(data: &[u8], pos: &mut usize) -> anyhow::Result<String>`

`mod tests`

---

## qemu-infra/src/qemu.rs

**Language:** Rust | **Size:** 8.5 KB | **Lines:** 292

**Imports:**
- `std::{
    net::TcpListener,
    path::{Path, PathBuf},
    process::{ExitStatus, Stdio},
    time::Duration,
}`
- `anyhow::anyhow`
- `tokio::{
    io::AsyncWriteExt,
    process::{Child, ChildStdin, ChildStdout, Command},
}`
- `crate::{PROMPT, qmp::QmpClient, read_asserter::ReadAsserter}`

**Declarations:**

`fn find_available_port() -> anyhow::Result<u16>`

**`impl Default for QemuOptions`**
  `fn default() -> Self`


**`impl QemuOptions`**
  `pub fn add_network_card(mut self, value: bool) -> Self`

  `pub fn use_smp(mut self, value: bool) -> Self`

  `pub fn enable_gdb(mut self, value: bool) -> Self`

  `pub fn block_device(mut self, path: PathBuf) -> Self`

  `pub fn framebuffer(mut self, value: bool) -> Self`

  `pub fn headless(mut self, value: bool) -> Self`

  `pub fn qmp_socket(mut self, path: PathBuf) -> Self`

  `fn has_block_device(&self) -> bool`

  `fn apply(self, command: &mut Command) -> Option<u16>`


**`impl QemuInstance`**
  `pub async fn start() -> anyhow::Result<Self>`

  `pub async fn start_with(mut options: QemuOptions) -> anyhow::Result<Self>`

  `pub fn stdout(&mut self) -> &mut ReadAsserter<ChildStdout>`

  `pub fn stdin(&mut self) -> &mut ChildStdin`

  `pub fn network_port(&self) -> Option<u16>`

  `pub fn gdb_port(&self) -> Option<u16>`

  `pub fn qmp_socket(&self) -> Option<&Path>`

  `pub async fn screendump(&self) -> anyhow::Result<crate::ppm::PpmImage>`

  `pub async fn ctrl_c_and_assert_prompt(&mut self) -> anyhow::Result<String>`

  `pub async fn wait_for_qemu_to_exit(mut self) -> anyhow::Result<ExitStatus>`

  `pub async fn run_prog(&mut self, prog_name: &str) -> anyhow::Result<String>`

  `pub async fn run_prog_waiting_for( &mut self, prog_name: &str, wait_for: &str, ) -> anyhow::Result<String>`

  `pub async fn write_and_wait_for(&mut self, text: &str, wait: &str) -> anyhow::Result<()>`


---

## qemu-infra/src/qmp.rs

**Language:** Rust | **Size:** 2.2 KB | **Lines:** 75

**Imports:**
- `std::path::Path`
- `anyhow::anyhow`
- `tokio::{
    io::{AsyncBufReadExt, AsyncWriteExt, BufReader},
    net::UnixStream,
}`

**Declarations:**

**`impl QmpClient`**
  `pub async fn connect(socket_path: &Path) -> anyhow::Result<Self>`

  `pub async fn screendump(&mut self, output_path: &Path) -> anyhow::Result<()>`

  `async fn send_command(&mut self, cmd: &str) -> anyhow::Result<()>`

  `async fn read_line(&mut self) -> anyhow::Result<String>`


---

## qemu-infra/src/read_asserter.rs

**Language:** Rust | **Size:** 4.0 KB | **Lines:** 131

**Imports:**
- `std::{fmt, time::Duration}`
- `tokio::io::{AsyncRead, AsyncReadExt, AsyncWriteExt}`
- `crate::searchable_buffer::SearchableBuffer`

**Declarations:**

`const DEFAULT_BUFFER_SIZE: usize = 1024`

**`impl fmt::Display for ReadError`**
  `fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result`


**`impl std::error::Error for ReadError`**

**`impl<Reader: AsyncRead + Unpin> ReadAsserter<Reader>`**
  `pub fn new(reader: Reader) -> Self`

  `pub fn with_timeout(mut self, timeout: Duration) -> Self`

  `pub async fn assert_read_until(&mut self, needle: &str) -> Result<Vec<u8>, ReadError>`

  `async fn read_until_inner(&mut self, needle: &str) -> Result<Vec<u8>, ReadError>`

  `pub async fn read_available(&mut self) -> Vec<u8>`

  `async fn print_to_stderr(&mut self, data: &[u8])`


---

## qemu-infra/src/searchable_buffer.rs

**Language:** Rust | **Size:** 2.0 KB | **Lines:** 82

**Declarations:**

**`impl SearchableBuffer`**
  `pub fn new(buffer: Vec<u8>) -> Self`

  `pub fn find_and_remove(&mut self, needle: &str) -> Option<Vec<u8>>`

  `pub fn append(&mut self, data: &[u8])`

  `pub fn peek(&self) -> &[u8]`

  `pub fn drain(&mut self) -> Vec<u8>`

  `fn find_end_index_in_buffer(&self, needle: &str) -> Option<usize>`

  `fn shift_to_front(&mut self, index: usize) -> Vec<u8>`


`mod tests`

---

## qemu_wrapper.sh

**Language:** Shell | **Size:** 4.2 KB | **Lines:** 140

---

## rustfmt.toml

**Language:** TOML | **Size:** 43 B | **Lines:** 2

**Declarations:**

`edition="2024"`

`imports_granularity="Crate"`

---

## sys/Cargo.toml

**Language:** TOML | **Size:** 337 B | **Lines:** 17

**Imports:**
- `arch`
- `common`
- `headers`

**Declarations:**

`cargo-features = ["per-package-target"]`

---

## sys/build.rs

**Language:** Rust | **Size:** 64 B | **Lines:** 3

**Declarations:**

`fn main()`

---

## sys/src/asm/mod.rs

**Language:** Rust | **Size:** 780 B | **Lines:** 27

**Imports:**
- `crate::cpu`
- `core::arch::global_asm`

---

## sys/src/cpu.rs

**Language:** Rust | **Size:** 3.4 KB | **Lines:** 96

**Imports:**
- `pub use arch::CpuId`
- `common::syscalls::trap_frame::TrapFrame`
- `core::mem::offset_of`
- `crate::klibc::runtime_initialized::RuntimeInitializedData`

**Declarations:**

---

## sys/src/io/mod.rs

**Language:** Rust | **Size:** 14 B | **Lines:** 1

**Declarations:**

---

## sys/src/io/uart.rs

**Language:** Rust | **Size:** 1.9 KB | **Lines:** 83

**Imports:**
- `core::fmt::Write`
- `crate::{
    klibc::{MMIO, Spinlock, send_sync::UnsafeSendSync},
    mmio_struct,
}`

**Declarations:**

`const LCR_WORD_LEN_8BIT: u8 = 0b11`

`const LCR_DLAB: u8 = 1 << 7`

`const FCR_ENABLE: u8 = 1`

`const IER_RX_AVAILABLE: u8 = 1`

`const LSR_DATA_READY: u8 = 1`

`const BAUD_DIVISOR: u16 = 592`

**`impl Uart`**
  `const fn new(uart_base_address: usize) -> Self`

  `pub fn init(&mut self)`

  `pub fn write_byte(&mut self, character: u8)`

  `pub fn read(&self) -> Option<u8>`


**`impl Write for Uart`**
  `fn write_str(&mut self, s: &str) -> core::fmt::Result`


---

## sys/src/klibc/array_vec.rs

**Language:** Rust | **Size:** 3.1 KB | **Lines:** 116

**Imports:**
- `core::ops::{Deref, DerefMut}`

**Declarations:**

**`impl<T, const LENGTH: usize> ArrayVec<T, LENGTH>`**
  `pub const fn new() -> Self`

  `pub fn push(&mut self, element: T) -> Result<(), ArrayVecError<T>>`

  `pub fn pop(&mut self) -> Option<T>`

  `pub fn iter(&self) -> ArrayVecIter<'_, T, LENGTH>`

  `pub fn len(&self) -> usize`

  `pub fn is_empty(&self) -> bool`


**`impl<T, const LENGTH: usize> Default for ArrayVec<T, LENGTH>`**
  `fn default() -> Self`


**`impl<T, const LENGTH: usize> Drop for ArrayVec<T, LENGTH>`**
  `fn drop(&mut self)`


**`impl<'a, T, const LENGTH: usize> Iterator for ArrayVecIter<'a, T, LENGTH>`**
  `fn next(&mut self) -> Option<Self::Item>`


**`impl<'a, T, const LENGTH: usize> IntoIterator for &'a ArrayVec<T, LENGTH>`**
  `fn into_iter(self) -> Self::IntoIter`


**`impl<T, const LENGTH: usize> Deref for ArrayVec<T, LENGTH>`**
  `fn deref(&self) -> &Self::Target`


**`impl<T, const LENGTH: usize> DerefMut for ArrayVec<T, LENGTH>`**
  `fn deref_mut(&mut self) -> &mut Self::Target`


---

## sys/src/klibc/deconstructed_vec.rs

**Language:** Rust | **Size:** 1.4 KB | **Lines:** 49

**Imports:**
- `alloc::vec::Vec`

**Declarations:**

**`unsafe impl Send for DeconstructedVec`**

**`impl DeconstructedVec`**
  `pub fn from_vec(vec: Vec<u8>) -> Self`

  `pub fn into_vec_with_len(self, length: usize) -> Vec<u8>`

  `pub fn length(&self) -> usize`


**`impl Drop for DeconstructedVec`**
  `fn drop(&mut self)`


---

## sys/src/klibc/mmio.rs

**Language:** Rust | **Size:** 5.9 KB | **Lines:** 189

**Imports:**
- `core::ops::{BitAnd, BitAndAssign, BitOr, BitOrAssign}`
- `common::numbers::Number`

**Declarations:**

**`impl<T> MMIO<T>`**
  `pub const fn new(addr: usize) -> Self`

  `pub const unsafe fn add(&self, count: usize) -> Self`

  `pub fn add_within_region(&self, count: usize, region_elements: usize) -> Self`

  `pub const fn new_type<U>(&self) -> MMIO<U>`

  `pub const fn new_type_with_offset<U>(&self, offset: usize) -> MMIO<U>`


**`impl<T: Copy> MMIO<T>`**
  `pub fn read(&self) -> T`

  `pub fn write(&mut self, value: T)`


**`impl<T: Copy, const LENGTH: usize> MMIO<[T; LENGTH]>`**
  `pub fn read_index(&self, index: usize) -> T`

  `pub fn write_index(&mut self, index: usize, value: T)`

  `fn get_index(&self, index: usize) -> MMIO<T>`


**`impl<T: Number + BitOr<T, Output = T>> BitOrAssign<T> for MMIO<T>`**
  `fn bitor_assign(&mut self, rhs: T)`


**`impl<T: Number + BitAnd<T, Output = T>> BitAndAssign<T> for MMIO<T>`**
  `fn bitand_assign(&mut self, rhs: T)`


**`unsafe impl<T> Send for MMIO<T>`**

**`unsafe impl<T> Sync for MMIO<T>`**

**`impl<T> core::fmt::Pointer for MMIO<T>`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl<T: core::fmt::Debug + Copy> core::fmt::Debug for MMIO<T>`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


---

## sys/src/klibc/mod.rs

**Language:** Rust | **Size:** 244 B | **Lines:** 12

**Imports:**
- `pub use mmio::MMIO`
- `pub use spinlock::{Spinlock, SpinlockGuard}`

**Declarations:**

---

## sys/src/klibc/runtime_initialized.rs

**Language:** Rust | **Size:** 2.3 KB | **Lines:** 73

**Imports:**
- `core::{cell::UnsafeCell, mem::MaybeUninit, ops::Deref, sync::atomic::AtomicBool}`

**Declarations:**

**`unsafe impl<T: Sync> Sync for RuntimeInitializedData<T>`**

**`impl<T> RuntimeInitializedData<T>`**
  `pub const fn new() -> Self`

  `pub fn initialize(&self, value: T)`

  `pub fn is_initialized(&self) -> bool`


**`impl<T> Deref for RuntimeInitializedData<T>`**
  `fn deref(&self) -> &Self::Target`


`mod tests`

---

## sys/src/klibc/send_sync.rs

**Language:** Rust | **Size:** 1.0 KB | **Lines:** 38

**Imports:**
- `core::ops::{Deref, DerefMut}`

**Declarations:**

**`impl<T: core::fmt::Debug> core::fmt::Debug for UnsafeSendSync<T>`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl<T: Clone> Clone for UnsafeSendSync<T>`**
  `fn clone(&self) -> Self`


**`impl<T: Copy> Copy for UnsafeSendSync<T>`**

**`unsafe impl<T> Send for UnsafeSendSync<T>`**

**`unsafe impl<T> Sync for UnsafeSendSync<T>`**

**`impl<T> Deref for UnsafeSendSync<T>`**
  `fn deref(&self) -> &T`


**`impl<T> DerefMut for UnsafeSendSync<T>`**
  `fn deref_mut(&mut self) -> &mut T`


---

## sys/src/klibc/sizes.rs

**Language:** Rust | **Size:** 270 B | **Lines:** 14

**Declarations:**

---

## sys/src/klibc/spinlock.rs

**Language:** Rust | **Size:** 6.5 KB | **Lines:** 227

**Imports:**
- `core::{
    cell::UnsafeCell,
    fmt::Debug,
    ops::{Deref, DerefMut},
    sync::atomic::{AtomicBool, AtomicUsize, Ordering},
}`

**Declarations:**

`const NO_OWNER: usize = usize::MAX`

**`impl<T> Spinlock<T>`**
  `pub const fn new(data: T) -> Self`

  `pub fn with_lock<'a, R>(&'a self, f: impl FnOnce(SpinlockGuard<'a, T>) -> R) -> R`

  `pub fn try_with_lock<'a, R>(&'a self, f: impl FnOnce(SpinlockGuard<'a, T>) -> R) -> Option<R>`

  `pub fn lock(&self) -> SpinlockGuard<'_, T>`

  `fn detect_same_cpu_deadlock(&self)`

  `fn detect_same_cpu_deadlock(&self)`

  `fn warn_possible_deadlock(&self, spin_count: u64)`

  `fn warn_possible_deadlock(&self, _spin_count: u64)`

  `fn set_owner(&self)`

  `fn set_owner(&self)`

  `fn clear_owner(&self)`

  `pub unsafe fn force_unlock(&self)`

  `pub fn panic_force_unlock(&self)`

  `pub fn into_inner(self) -> T`


**`unsafe impl<T: Send> Sync for Spinlock<T>`**

**`unsafe impl<T: Send> Send for Spinlock<T>`**

**`impl<T> Drop for SpinlockGuard<'_, T>`**
  `fn drop(&mut self)`


**`impl<T> Deref for SpinlockGuard<'_, T>`**
  `fn deref(&self) -> &Self::Target`


**`impl<T> DerefMut for SpinlockGuard<'_, T>`**
  `fn deref_mut(&mut self) -> &mut Self::Target`


**`impl<T: Debug> Debug for SpinlockGuard<'_, T>`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


`mod tests`

---

## sys/src/klibc/util.rs

**Language:** Rust | **Size:** 7.9 KB | **Lines:** 298

**Imports:**
- `core::{
    fmt::Display,
    ops::{BitAnd, BitAndAssign, BitOrAssign, Not, Rem, Shl, Shr, Sub},
}`
- `crate::memory::PAGE_SIZE`

**Declarations:**

`const _: () = assert!(core::mem::size_of::<usize>() == core::mem::size_of::<u64>())`

**`impl UsizeExt for u64`**
  `fn as_usize(self) -> usize`


**`impl Display for PrintMemorySizeHumanFriendly`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl BufferExtension for [u8]`**
  `fn interpret_as<T>(&self) -> &T`

  `fn split_as<T>(&self) -> (&T, &[u8])`


**`impl<T> InBytes for alloc::vec::Vec<T>`**
  `fn in_bytes(&self) -> usize`


**`impl<T, const N: usize> InBytes for [T; N]`**
  `fn in_bytes(&self) -> usize`


---

## sys/src/klibc/validated_ptr.rs

**Language:** Rust | **Size:** 2.8 KB | **Lines:** 88

**Imports:**
- `alloc::vec::Vec`
- `common::pointer::Pointer`
- `headers::errno::Errno`

**Declarations:**

**`impl<T> ValidatedPtr<T>`**
  `pub fn from_userspace( raw: impl Pointer, len: usize, validator: &impl PtrValidator, ) -> Result<Self, Errno>`

  `pub fn from_kernel( raw: impl Pointer, len: usize, validator: &impl PtrValidator, ) -> Result<Self, Errno>`

  `pub fn from_trusted(ptr: *const T) -> Self`


**`impl<T: Copy> ValidatedPtr<T>`**
  `pub fn read(&self) -> T`

  `pub fn write_slice(&self, data: &[T])`


**`impl<T> ValidatedPtr<T>`**
  `pub fn write(&self, value: T)`

  `pub fn as_static_ref(&self) -> &'static T`

  `pub fn as_static_slice(&self, len: usize) -> &'static [T]`


**`impl<T: Clone> ValidatedPtr<T>`**
  `pub fn read_slice(&self, len: usize) -> Vec<T>`


---

## sys/src/lib.rs

**Language:** Rust | **Size:** 634 B | **Lines:** 25

**Imports:**
- `pub use klibc::{
    mmio::MMIO,
    spinlock::{Spinlock, SpinlockGuard},
}`

**Declarations:**

`mod asm`

---

## sys/src/logging/configuration.rs

**Language:** Rust | **Size:** 1.1 KB | **Lines:** 44

**Declarations:**

`const LOG_FOLLOWING_MODULES: &[&str] = &[]`

`const DONT_LOG_FOLLOWING_MODULES: &[&str] = &[ "solaya::interrupts::trap", "solaya::debugging::unwinder", "solaya::debugging::symbols", "solaya::processes::scheduler", "solaya::processes::process_table", "solaya::processes::timer", "solaya::io::tty_device", ]`

`const fn const_starts_with(haystack: &str, needle: &str) -> bool`

---

## sys/src/logging/mod.rs

**Language:** Rust | **Size:** 2.2 KB | **Lines:** 101

**Imports:**
- `core::fmt`

**Declarations:**

---

## sys/src/memory/address.rs

**Language:** Rust | **Size:** 6.1 KB | **Lines:** 259

**Imports:**
- `core::{
    fmt,
    ops::{Add, AddAssign, Sub},
}`

**Declarations:**

**`impl PhysAddr`**
  `pub const fn new(addr: usize) -> Self`

  `pub const fn zero() -> Self`

  `pub const fn as_usize(self) -> usize`

  `pub const fn is_page_aligned(self) -> bool`


**`impl VirtAddr`**
  `pub const fn new(addr: usize) -> Self`

  `pub const fn zero() -> Self`

  `pub const fn as_usize(self) -> usize`

  `pub const fn as_ptr<T>(self) -> *const T`

  `pub const fn as_mut_ptr<T>(self) -> *mut T`

  `pub const fn is_page_aligned(self) -> bool`


**`impl Add<usize> for PhysAddr`**
  `fn add(self, rhs: usize) -> Self`


**`impl AddAssign<usize> for PhysAddr`**
  `fn add_assign(&mut self, rhs: usize)`


**`impl Sub<usize> for PhysAddr`**
  `fn sub(self, rhs: usize) -> Self`


**`impl Sub for PhysAddr`**
  `fn sub(self, rhs: Self) -> usize`


**`impl Add<usize> for VirtAddr`**
  `fn add(self, rhs: usize) -> Self`


**`impl AddAssign<usize> for VirtAddr`**
  `fn add_assign(&mut self, rhs: usize)`


**`impl Sub<usize> for VirtAddr`**
  `fn sub(self, rhs: usize) -> Self`


**`impl Sub for VirtAddr`**
  `fn sub(self, rhs: Self) -> usize`


**`impl fmt::Display for PhysAddr`**
  `fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result`


**`impl fmt::Display for VirtAddr`**
  `fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result`


**`impl fmt::LowerHex for VirtAddr`**
  `fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result`


`mod tests`

---

## sys/src/memory/heap.rs

**Language:** Rust | **Size:** 9.3 KB | **Lines:** 292

**Imports:**
- `core::{
    alloc::{GlobalAlloc, Layout},
    marker::PhantomData,
    mem::{align_of, size_of},
    ptr::{NonNull, null_mut},
}`
- `crate::{
    Spinlock,
    klibc::util::{align_up, minimum_amount_of_pages},
}`
- `super::{PAGE_SIZE, page_allocator::PageAllocator}`

**Declarations:**

`struct AlignedSizeWithMetadata`
> Fields: `size: usize`

**`impl AlignedSizeWithMetadata`**
  `const fn new() -> Self`

  `fn from_layout(layout: Layout) -> Self`

  `const fn from_pages(pages: usize) -> Self`

  `const fn total_size(&self) -> usize`

  `const fn get_remaining_size(&self, needed_size: AlignedSizeWithMetadata) -> Self`


`struct FreeBlock`
> Fields: `next: Option<NonNull<FreeBlock>>`, `size: AlignedSizeWithMetadata`

`const _: [()`

**`impl FreeBlock`**
  `const fn new() -> Self`

  `const fn new_with_size(size: AlignedSizeWithMetadata) -> Self`

  `fn initialize(block_ptr: NonNull<FreeBlock>, size: AlignedSizeWithMetadata)`

  `fn split( mut block_ptr: NonNull<FreeBlock>, requested_size: AlignedSizeWithMetadata, ) -> NonNull<FreeBlock>`


**`impl<Allocator: PageAllocator> Default for Heap<Allocator>`**
  `fn default() -> Self`


**`impl<Allocator: PageAllocator> Heap<Allocator>`**
  `pub const fn new() -> Self`

  `pub fn allocated_memory(&self) -> usize`

  `fn is_page_allocator_allocation(&self, layout: &Layout) -> bool`

  `fn alloc(&mut self, layout: core::alloc::Layout) -> *mut u8`

  `fn dealloc(&mut self, ptr: *mut u8, layout: core::alloc::Layout)`

  `fn insert(&mut self, mut block_ptr: NonNull<FreeBlock>)`

  `fn split_if_necessary( &mut self, block_ptr: NonNull<FreeBlock>, requested_size: AlignedSizeWithMetadata, )`

  `fn find_and_remove( &mut self, requested_size: AlignedSizeWithMetadata, ) -> Option<NonNull<FreeBlock>>`


**`unsafe impl<Allocator: PageAllocator> Send for Heap<Allocator>`**

**`impl<Allocator: PageAllocator> Default for SpinlockHeap<Allocator>`**
  `fn default() -> Self`


**`impl<Allocator: PageAllocator> SpinlockHeap<Allocator>`**
  `pub const fn new() -> Self`


**`unsafe impl<Allocator: PageAllocator> GlobalAlloc for SpinlockHeap<Allocator>`**
  `unsafe fn alloc(&self, layout: core::alloc::Layout) -> *mut u8`

  `unsafe fn dealloc(&self, ptr: *mut u8, layout: core::alloc::Layout)`


---

## sys/src/memory/mod.rs

**Language:** Rust | **Size:** 752 B | **Lines:** 22

**Imports:**
- `pub use address::{PhysAddr, VirtAddr}`
- `pub use page::{PAGE_SIZE, Page, Pages, PagesAsSlice, PinnedHeapPages, page_slice_at_phys}`
- `core::mem::MaybeUninit`

**Declarations:**

---

## sys/src/memory/page.rs

**Language:** Rust | **Size:** 4.8 KB | **Lines:** 191

**Imports:**
- `alloc::{boxed::Box, vec}`
- `core::ops::{Add, Deref, DerefMut}`
- `crate::klibc::util::copy_slice`

**Declarations:**

**`impl Pages`**
  `pub const fn new(count: usize) -> Self`

  `pub const fn count(self) -> usize`

  `pub const fn as_bytes(self) -> usize`


**`impl Add<Pages> for usize`**
  `fn add(self, rhs: Pages) -> Self::Output`


**`impl Deref for Page`**
  `fn deref(&self) -> &Self::Target`


**`impl DerefMut for Page`**
  `fn deref_mut(&mut self) -> &mut Self::Target`


**`impl core::fmt::Debug for Page`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl Page`**
  `pub fn zero() -> Self`


**`impl PagesAsSlice for [Page]`**
  `fn as_u8_slice(&mut self) -> &mut [u8]`


**`impl PinnedHeapPages`**
  `pub fn new(number_of_pages: usize) -> Self`

  `pub fn new_pages(pages: Pages) -> Self`

  `pub fn fill(&mut self, data: &[u8], offset: usize)`

  `pub fn addr(&self) -> usize`

  `pub fn size(&self) -> usize`


**`impl Deref for PinnedHeapPages`**
  `fn deref(&self) -> &Self::Target`


**`impl DerefMut for PinnedHeapPages`**
  `fn deref_mut(&mut self) -> &mut Self::Target`


`mod tests`

---

## sys/src/memory/page_allocator.rs

**Language:** Rust | **Size:** 7.9 KB | **Lines:** 228

**Imports:**
- `super::page::Page`
- `crate::{
    debug,
    klibc::util::{align_down_ptr, minimum_amount_of_pages},
    memory::PAGE_SIZE,
}`
- `core::{
    fmt::Debug,
    mem::MaybeUninit,
    ops::Range,
    ptr::{NonNull, null_mut},
}`

**Declarations:**

`enum PageStatus`
> Variants: `Free`, `Used`, `Last`

**`impl PageStatus`**
  `fn is_free(&self) -> bool`


**`unsafe impl Send for MetadataPageAllocator<'_>`**

**`impl Debug for MetadataPageAllocator<'_>`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


**`impl Default for MetadataPageAllocator<'_>`**
  `fn default() -> Self`


**`impl<'a> MetadataPageAllocator<'a>`**
  `pub const fn new() -> Self`

  `pub fn init(&mut self, memory: &'a mut [MaybeUninit<u8>], reserved_areas: &[Range<*const u8>])`

  `pub fn total_heap_pages(&self) -> usize`

  `pub fn used_heap_pages(&self) -> usize`

  `fn page_idx_to_pointer(&self, page_index: usize) -> NonNull<MaybeUninit<Page>>`

  `fn page_pointer_to_page_idx(&self, page: NonNull<MaybeUninit<Page>>) -> usize`

  `pub fn alloc(&mut self, number_of_pages_requested: usize) -> Option<Range<NonNull<Page>>>`

  `fn is_range_free(&self, start_idx: usize, number_of_pages: usize) -> bool`

  `fn mark_range_as_used( &mut self, start_idx: usize, number_of_pages: usize, initialize_if_needed: bool, )`

  `fn range_to_start_aligned_and_number_of_pages<T>( &self, range: &Range<*const T>, ) -> (usize, usize)`

  `fn mark_pointer_range_as_used_without_initialize<T>(&mut self, range: &Range<*const T>)`

  `pub fn dealloc(&mut self, page: NonNull<Page>) -> usize`


---

## sys/src/memory/page_table.rs

**Language:** Rust | **Size:** 10.1 KB | **Lines:** 321

**Imports:**
- `alloc::boxed::Box`
- `core::ptr::null_mut`
- `super::address::PhysAddr`
- `crate::klibc::util::{get_bit, get_multiple_bits, set_multiple_bits, set_or_clear_bit}`

**Declarations:**

**`impl From<u8> for XWRMode`**
  `fn from(value: u8) -> Self`


**`impl XWRMode`**
  `pub fn is_writable(self) -> bool`

  `pub fn as_readonly(self) -> Self`

  `pub fn from_prot(prot: u32) -> Result<Self, headers::errno::Errno>`


**`impl PageTableEntry`**
  `pub fn set_validity(&mut self, is_valid: bool)`

  `pub fn get_validity(&self) -> bool`

  `pub fn set_user_mode_accessible(&mut self, is_user_mode_accessible: bool)`

  `pub fn get_user_mode_accessible(&self) -> bool`

  `pub fn set_xwr_mode(&mut self, mode: XWRMode)`

  `pub fn get_xwr_mode(&self) -> XWRMode`

  `pub fn is_leaf(&self) -> bool`

  `pub fn set_physical_address(&mut self, address: *mut PageTable)`

  `pub fn set_leaf_address(&mut self, address: PhysAddr)`

  `pub fn get_physical_address(&self) -> PhysAddr`

  `pub fn target_page_table(&self) -> &PageTable`

  `pub fn target_page_table_mut(&mut self) -> &mut PageTable`

  `pub fn get_target_page_table_raw(&self) -> *mut PageTable`


`const _: [()`

**`impl PageTable`**
  `pub fn zero() -> Self`

  `pub fn get_entry_for_virtual_address_mut( &mut self, virtual_address: usize, level: u8, ) -> &mut PageTableEntry`

  `pub fn get_entry_for_virtual_address( &self, virtual_address: usize, level: u8, ) -> &PageTableEntry`

  `pub fn get_physical_address(&self) -> PhysAddr`


**`unsafe impl Send for OwnedPageTable`**

**`impl OwnedPageTable`**
  `pub fn new() -> Self`


**`impl Default for OwnedPageTable`**
  `fn default() -> Self`


**`impl OwnedPageTable`**
  `pub fn as_raw(&self) -> *mut PageTable`

  `pub unsafe fn reclaim(ptr: *mut PageTable)`

  `pub fn reclaim_children(&self)`


**`impl core::ops::Deref for OwnedPageTable`**
  `fn deref(&self) -> &PageTable`


**`impl core::ops::DerefMut for OwnedPageTable`**
  `fn deref_mut(&mut self) -> &mut PageTable`


**`impl Drop for OwnedPageTable`**
  `fn drop(&mut self)`


**`impl core::fmt::Display for OwnedPageTable`**
  `fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result`


---

## sys/src/panic_support.rs

**Language:** Rust | **Size:** 295 B | **Lines:** 8

**Declarations:**

---

## system-tests/Cargo.toml

**Language:** TOML | **Size:** 243 B | **Lines:** 11

**Imports:**
- `anyhow`
- `tempfile`
- `tokio`
- `qemu-infra`

**Declarations:**

---

## system-tests/src/infra/mod.rs

**Language:** Rust | **Size:** 50 B | **Lines:** 3

**Declarations:**

---

## system-tests/src/lib.rs

**Language:** Rust | **Size:** 48 B | **Lines:** 4

**Declarations:**

`mod infra`

`mod tests`

---

## system-tests/src/tests/basics.rs

**Language:** Rust | **Size:** 2.8 KB | **Lines:** 111

**Imports:**
- `crate::infra::qemu::{QemuInstance, QemuOptions}`

**Declarations:**

`async fn boot_smp() -> anyhow::Result<()>`

`async fn boot_single_core() -> anyhow::Result<()>`

`async fn boot_with_network() -> anyhow::Result<()>`

`async fn shutdown() -> anyhow::Result<()>`

`async fn execute_program() -> anyhow::Result<()>`

`async fn execute_same_program_twice() -> anyhow::Result<()>`

`async fn execute_different_programs() -> anyhow::Result<()>`

`async fn credential_syscalls() -> anyhow::Result<()>`

`async fn unimplemented_syscall_kills_process_not_kernel() -> anyhow::Result<()>`

---

## system-tests/src/tests/block.rs

**Language:** Rust | **Size:** 1.5 KB | **Lines:** 52

**Imports:**
- `std::io::Write`
- `crate::infra::qemu::{QemuInstance, QemuOptions}`

**Declarations:**

`fn create_test_disk(path: &std::path::Path)`

`async fn block_read() -> anyhow::Result<()>`

---

## system-tests/src/tests/clocktest.rs

**Language:** Rust | **Size:** 466 B | **Lines:** 18

**Imports:**
- `crate::infra::qemu::QemuInstance`

**Declarations:**

`async fn clock_gettime_returns_nonzero() -> anyhow::Result<()>`

---

## system-tests/src/tests/connect4.rs

**Language:** Rust | **Size:** 441 B | **Lines:** 20

**Imports:**
- `crate::infra::qemu::QemuInstance`

**Declarations:**

`async fn connect4() -> anyhow::Result<()>`

---

## system-tests/src/tests/coreutils.rs

**Language:** Rust | **Size:** 842 B | **Lines:** 36

**Imports:**
- `crate::infra::qemu::QemuInstance`

**Declarations:**

`async fn echo() -> anyhow::Result<()>`

`async fn r#true() -> anyhow::Result<()>`

`async fn r#false() -> anyhow::Result<()>`

---

## system-tests/src/tests/doom.rs

**Language:** Rust | **Size:** 1.2 KB | **Lines:** 38

**Imports:**
- `std::time::Duration`
- `crate::infra::qemu::{QemuInstance, QemuOptions}`
- `tokio::io::AsyncWriteExt`

**Declarations:**

`async fn doom_starts() -> anyhow::Result<()>`

---

## system-tests/src/tests/ext2.rs

**Language:** Rust | **Size:** 2.8 KB | **Lines:** 105

**Imports:**
- `std::{path::Path, process::Command}`
- `crate::infra::qemu::{QemuInstance, QemuOptions}`

**Declarations:**

`fn create_ext2_image(path: &Path, files: &[(&str, &str)], dirs: &[&str])`

`async fn ext2_read_file() -> anyhow::Result<()>`

`async fn ext2_ls_mnt() -> anyhow::Result<()>`

---

## system-tests/src/tests/fork.rs

**Language:** Rust | **Size:** 824 B | **Lines:** 34

**Imports:**
- `crate::infra::qemu::QemuInstance`

**Declarations:**

`async fn fork_basic() -> anyhow::Result<()>`

`async fn fork_cow_isolation() -> anyhow::Result<()>`

---

## system-tests/src/tests/framebuffer.rs

**Language:** Rust | **Size:** 648 B | **Lines:** 21

**Imports:**
- `crate::infra::qemu::{QemuInstance, QemuOptions}`

**Declarations:**

`async fn framebuffer_device_exists() -> anyhow::Result<()>`

---

## system-tests/src/tests/job_control.rs

**Language:** Rust | **Size:** 4.2 KB | **Lines:** 135

**Imports:**
- `qemu_infra::PROMPT`
- `tokio::io::AsyncWriteExt`
- `crate::infra::qemu::QemuInstance`

**Declarations:**

`async fn ctrl_d_exits_cat() -> anyhow::Result<()>`

`async fn ctrl_z_stops_process() -> anyhow::Result<()>`

`async fn fg_resumes_stopped_process() -> anyhow::Result<()>`

`async fn cat_bg_then_fg_then_ctrl_c() -> anyhow::Result<()>`

`async fn bg_process_gets_sigttin() -> anyhow::Result<()>`

---

## system-tests/src/tests/mod.rs

**Language:** Rust | **Size:** 282 B | **Lines:** 22

**Declarations:**

`mod basics`

`mod block`

`mod clocktest`

`mod connect4`

`mod coreutils`

`mod doom`

`mod ext2`

`mod fork`

`mod framebuffer`

`mod job_control`

`mod mutex`

`mod net`

`mod panic`

`mod process_relation`

`mod rng`

`mod shell`

`mod signals`

`mod sleep`

`mod stress`

`mod syscall_tracer`

`mod sysinfo`

`mod vfs`

---

## system-tests/src/tests/mutex.rs

**Language:** Rust | **Size:** 288 B | **Lines:** 11

**Imports:**
- `crate::infra::qemu::QemuInstance`

**Declarations:**

`async fn mutex_contention() -> anyhow::Result<()>`

---

## system-tests/src/tests/net.rs

**Language:** Rust | **Size:** 3.2 KB | **Lines:** 105

**Imports:**
- `tokio::io::{AsyncReadExt, AsyncWriteExt}`
- `crate::infra::qemu::{QemuInstance, QemuOptions}`

**Declarations:**

`async fn dhcp() -> anyhow::Result<()>`

`async fn udp() -> anyhow::Result<()>`

`async fn tcp_echo() -> anyhow::Result<()>`

`async fn webserver() -> anyhow::Result<()>`

---

## system-tests/src/tests/panic.rs

**Language:** Rust | **Size:** 488 B | **Lines:** 15

**Imports:**
- `crate::infra::qemu::QemuInstance`

**Declarations:**

`async fn panic() -> anyhow::Result<()>`

---

## system-tests/src/tests/process_relation.rs

**Language:** Rust | **Size:** 1.5 KB | **Lines:** 55

**Imports:**
- `crate::infra::qemu::QemuInstance`

**Declarations:**

`async fn getppid_returns_valid_parent() -> anyhow::Result<()>`

`async fn pgid_and_sid_syscalls() -> anyhow::Result<()>`

`async fn setpgid_creates_new_process_group() -> anyhow::Result<()>`

`async fn setsid_creates_new_session() -> anyhow::Result<()>`

---

## system-tests/src/tests/rng.rs

**Language:** Rust | **Size:** 324 B | **Lines:** 10

**Imports:**
- `crate::infra::qemu::QemuInstance`

**Declarations:**

`async fn dev_random_and_at_random() -> anyhow::Result<()>`

---

## system-tests/src/tests/shell.rs

**Language:** Rust | **Size:** 685 B | **Lines:** 22

**Imports:**
- `crate::infra::qemu::QemuInstance`
- `qemu_infra::PROMPT`

**Declarations:**

`async fn background_execution() -> anyhow::Result<()>`

`async fn execute_nonexistent_program() -> anyhow::Result<()>`

---

## system-tests/src/tests/signals.rs

**Language:** Rust | **Size:** 3.1 KB | **Lines:** 107

**Imports:**
- `tokio::io::AsyncWriteExt`
- `crate::infra::qemu::{QemuInstance, QemuOptions}`

**Declarations:**

`async fn should_exit_program() -> anyhow::Result<()>`

`async fn should_rerun_udp_after_ctrl_c() -> anyhow::Result<()>`

`async fn should_not_exit_shell() -> anyhow::Result<()>`

`async fn self_kill_invokes_handler() -> anyhow::Result<()>`

`async fn ctrl_c_invokes_handler() -> anyhow::Result<()>`

`async fn sig_ign_survives_ctrl_c() -> anyhow::Result<()>`

---

## system-tests/src/tests/sleep.rs

**Language:** Rust | **Size:** 566 B | **Lines:** 23

**Imports:**
- `std::time::{Duration, Instant}`
- `crate::infra::qemu::QemuInstance`

**Declarations:**

`async fn sleep() -> anyhow::Result<()>`

---

## system-tests/src/tests/stress.rs

**Language:** Rust | **Size:** 1.4 KB | **Lines:** 51

**Imports:**
- `std::time::{Duration, Instant}`
- `crate::infra::qemu::QemuInstance`

**Declarations:**

`async fn stress_process() -> anyhow::Result<()>`

`async fn stress_thread() -> anyhow::Result<()>`

---

## system-tests/src/tests/syscall_tracer.rs

**Language:** Rust | **Size:** 1.2 KB | **Lines:** 39

**Imports:**
- `crate::infra::qemu::QemuInstance`

**Declarations:**

`async fn traced_process_emits_enter_exit() -> anyhow::Result<()>`

`async fn untraced_process_has_no_trace_output() -> anyhow::Result<()>`

---

## system-tests/src/tests/sysinfo.rs

**Language:** Rust | **Size:** 969 B | **Lines:** 26

**Imports:**
- `crate::infra::qemu::QemuInstance`

**Declarations:**

`async fn test_sysinfo_syscalls() -> anyhow::Result<()>`

`async fn rlimit() -> anyhow::Result<()>`

---

## system-tests/src/tests/vfs.rs

**Language:** Rust | **Size:** 5.4 KB | **Lines:** 173

**Imports:**
- `crate::infra::qemu::{QemuInstance, QemuOptions}`

**Declarations:**

`async fn sendfile() -> anyhow::Result<()>`

`async fn cat_proc_version() -> anyhow::Result<()>`

`async fn touch_and_cat() -> anyhow::Result<()>`

`async fn rm_file() -> anyhow::Result<()>`

`async fn ls_root() -> anyhow::Result<()>`

`async fn ls_proc() -> anyhow::Result<()>`

`async fn rm_nonexistent() -> anyhow::Result<()>`

`async fn vfs_roundtrip() -> anyhow::Result<()>`

`async fn devfs_null_and_zero() -> anyhow::Result<()>`

`async fn cat_dev_null() -> anyhow::Result<()>`

`async fn ls_dev() -> anyhow::Result<()>`

`async fn pread_pwrite() -> anyhow::Result<()>`

`async fn ls_dev_with_block() -> anyhow::Result<()>`

`async fn vfs_metadata() -> anyhow::Result<()>`

`async fn file_metadata_ops() -> anyhow::Result<()>`

`async fn ls_long_format() -> anyhow::Result<()>`

`async fn symlinks_and_links() -> anyhow::Result<()>`

---

## typos.toml

**Language:** TOML | **Size:** 133 B | **Lines:** 7

**Declarations:**

---

## userspace/Cargo.toml

**Language:** TOML | **Size:** 393 B | **Lines:** 15

**Imports:**
- `common`
- `headers`

**Declarations:**

`cargo-features = ["per-package-target"]`

---

## userspace/doom/dg_solaya.c

**Language:** C | **Size:** 8.8 KB | **Lines:** 309

**Imports:**
- `"doomgeneric.h"`
- `"doomkeys.h"`
- `<stdio.h>`
- `<stdlib.h>`
- `<string.h>`
- `<fcntl.h>`
- `<unistd.h>`
- `<time.h>`
- `<termios.h>`
- `<stdint.h>`

**Declarations:**

`static int fb_fd = -1`

`static int kb_fd = -1`

`static int log_fd = -1`

`static struct termios orig_termios`

`static void extract_wad(void)`

`static uint32_t frame_count = 0`

`static uint32_t fps_last_time = 0`

`static unsigned char linux_keycode_to_doom(uint16_t code)`

`static unsigned char convert_key(unsigned char c)`

`static int event_count = 0`

`static int event_index = 0`

`static int pending_release = 0`

`static unsigned char pending_release_key = 0`

---

## userspace/doom/i_video_solaya.c

**Language:** C | **Size:** 6.8 KB | **Lines:** 328

**Imports:**
- `"config.h"`
- `"v_video.h"`
- `"m_argv.h"`
- `"d_event.h"`
- `"d_main.h"`
- `"i_video.h"`
- `"i_system.h"`
- `"z_zone.h"`
- `"tables.h"`
- `"doomkeys.h"`
- *... and 7 more imports*

**Declarations:**

`static struct FB_ScreenInfo s_Fb`

---

## userspace/src/bin/bad-syscall.rs

**Language:** Rust | **Size:** 223 B | **Lines:** 13

**Imports:**
- `core::arch::asm`

**Declarations:**

`fn main()`

---

## userspace/src/bin/blktest.rs

**Language:** Rust | **Size:** 794 B | **Lines:** 31

**Imports:**
- `std::{
    fs::File,
    io::{Read, Seek, SeekFrom},
}`

**Declarations:**

`fn main()`

---

## userspace/src/bin/clocktest.rs

**Language:** Rust | **Size:** 423 B | **Lines:** 16

**Imports:**
- `std::time::Instant`

**Declarations:**

`fn main()`

---

## userspace/src/bin/connect4/game_board.rs

**Language:** Rust | **Size:** 9.0 KB | **Lines:** 319

**Imports:**
- `std::sync::atomic::{AtomicI64, AtomicUsize, Ordering}`

**Declarations:**

`const COLUMNS: u8 = 7`

`const ROWS: u8 = 6`

**`impl Player`**
  `fn opponent(&self) -> Self`

  `pub fn switch(&mut self)`


`enum Position`
> Variants: `Empty`, `C`, `H`

**`impl From<Player> for Position`**
  `fn from(value: Player) -> Self`


`struct SearchContext`
> Fields: `player: Player`, `counter: AtomicUsize`, `shared_alpha: AtomicI64`

**`impl GameBoard`**
  `pub fn new() -> Self`

  `pub fn print(&self)`

  `pub fn put(&mut self, player: Player, column: u8) -> Result<(), ()>`

  `fn calculate_score(&self, player: Player) -> i64`

  `fn evaluate_line( &self, start_row: u8, start_col: u8, dr: isize, dc: isize, player: Player, opponent: Player, ) -> i64`

  `pub fn is_game_over(&self) -> Option<Player>`

  `fn check_winner(&self) -> Option<Player>`

  `fn check_four_in_a_row(&self, row: u8, col: u8) -> Option<Player>`

  `fn minimax_inner( &self, depth: u8, alpha: i64, beta: i64, maximizing_player: bool, ctx: &SearchContext, ) -> i64`

  `fn for_valid_moves(&self, mut f: impl FnMut(u8) -> bool)`

  `pub fn find_best_move(&self, depth: u8, player: Player) -> (Option<u8>, usize)`


---

## userspace/src/bin/connect4/main.rs

**Language:** Rust | **Size:** 2.2 KB | **Lines:** 86

**Imports:**
- `std::io::{Write, stdout}`
- `game_board::{GameBoard, Player}`
- `userspace::util::read_line`

**Declarations:**

`mod game_board`

`fn main() -> Result<(), Box<dyn std::error::Error>>`

`fn human(board: &mut GameBoard)`

`fn computer(board: &mut GameBoard, depth: u8)`

---

## userspace/src/bin/cow-test.rs

**Language:** Rust | **Size:** 914 B | **Lines:** 28

**Imports:**
- `core::ptr::{read_volatile, write_volatile}`

**Declarations:**

`fn main()`

---

## userspace/src/bin/cred-test.rs

**Language:** Rust | **Size:** 2.7 KB | **Lines:** 81

**Declarations:**

`fn main()`

---

## userspace/src/bin/devfs_test.rs

**Language:** Rust | **Size:** 1.4 KB | **Lines:** 41

**Imports:**
- `std::io::{Read, Write}`

**Declarations:**

`fn main()`

---

## userspace/src/bin/dhcpd.rs

**Language:** Rust | **Size:** 5.2 KB | **Lines:** 198

**Imports:**
- `std::{net::UdpSocket, os::fd::AsRawFd}`
- `common::ioctl::{get_mac_address, set_ip_address}`

**Declarations:**

`const DHCP_SERVER_PORT: u16 = 67`

`const DHCP_CLIENT_PORT: u16 = 68`

`const BOOTREQUEST: u8 = 1`

`const BOOTREPLY: u8 = 2`

`const HTYPE_ETHERNET: u8 = 1`

`const HLEN_ETHERNET: u8 = 6`

`const DHCP_MAGIC_COOKIE: [u8`

`const OPT_SUBNET_MASK: u8 = 1`

`const OPT_ROUTER: u8 = 3`

`const OPT_MESSAGE_TYPE: u8 = 53`

`const OPT_SERVER_ID: u8 = 54`

`const OPT_REQUESTED_IP: u8 = 50`

`const OPT_PARAM_REQUEST: u8 = 55`

`const OPT_END: u8 = 255`

`const DHCPDISCOVER: u8 = 1`

`const DHCPOFFER: u8 = 2`

`const DHCPREQUEST: u8 = 3`

`const DHCPACK: u8 = 5`

`const DHCP_HEADER_SIZE: usize = 236`

`fn build_discover(mac: &[u8; 6], xid: u32) -> Vec<u8>`

`fn build_request(mac: &[u8; 6], xid: u32, offered_ip: [u8; 4], server_id: [u8; 4]) -> Vec<u8>`

`struct DhcpResponse`
> Fields: `msg_type: u8`, `yiaddr: [u8; 4]`, `server_id: [u8; 4]`

`fn parse_response(data: &[u8], expected_xid: u32) -> Option<DhcpResponse>`

`fn main()`

---

## userspace/src/bin/ext2test.rs

**Language:** Rust | **Size:** 1.4 KB | **Lines:** 45

**Declarations:**

`fn main()`

---

## userspace/src/bin/fbbench.rs

**Language:** Rust | **Size:** 1.3 KB | **Lines:** 51

**Imports:**
- `std::{
    fs::OpenOptions,
    io::{Seek, SeekFrom, Write},
    time::Instant,
}`

**Declarations:**

`fn main()`

---

## userspace/src/bin/fbtest.rs

**Language:** Rust | **Size:** 345 B | **Lines:** 12

**Imports:**
- `std::{fs::OpenOptions, io::Write}`

**Declarations:**

`fn main()`

---

## userspace/src/bin/fmeta-test.rs

**Language:** Rust | **Size:** 2.3 KB | **Lines:** 86

**Imports:**
- `std::{io::Write, os::unix::io::AsRawFd}`

**Declarations:**

`struct Stat`
> Fields: `st_dev: u64`, `st_ino: u64`, `st_mode: u32`, `st_nlink: u32`, `st_uid: u32`, `st_gid: u32`, `st_rdev: u64`, `_pad1: u64`, `st_size: i64`, `st_blksize: i32`, `_pad2: i32`, `st_blocks: i64`, `st_atime: i64`, `st_atime_nsec: i64`, `st_mtime: i64`, `st_mtime_nsec: i64`, `st_ctime: i64`, `st_ctime_nsec: i64`, `_unused: [i32; 2]`

`fn do_fstat(fd: i32) -> Stat`

`fn main()`

---

## userspace/src/bin/fork-test.rs

**Language:** Rust | **Size:** 407 B | **Lines:** 17

**Declarations:**

`fn main()`

---

## userspace/src/bin/getppid.rs

**Language:** Rust | **Size:** 115 B | **Lines:** 6

**Declarations:**

`fn main()`

---

## userspace/src/bin/init.rs

**Language:** Rust | **Size:** 369 B | **Lines:** 12

**Imports:**
- `userspace::spawn::spawn`

**Declarations:**

`fn main()`

---

## userspace/src/bin/kbtest.rs

**Language:** Rust | **Size:** 1.8 KB | **Lines:** 59

**Imports:**
- `std::{fs::OpenOptions, io::Read, os::unix::fs::OpenOptionsExt}`

**Declarations:**

`const O_NONBLOCK: i32 = 0x800`

`fn main()`

---

## userspace/src/bin/loop.rs

**Language:** Rust | **Size:** 218 B | **Lines:** 11

**Imports:**
- `std::{thread::sleep, time::Duration}`

**Declarations:**

`fn main()`

---

## userspace/src/bin/ls-test.rs

**Language:** Rust | **Size:** 498 B | **Lines:** 15

**Declarations:**

`fn main()`

---

## userspace/src/bin/metadata-test.rs

**Language:** Rust | **Size:** 1.7 KB | **Lines:** 47

**Imports:**
- `std::os::raw::c_int`

**Declarations:**

`fn main()`

---

## userspace/src/bin/mutex_test.rs

**Language:** Rust | **Size:** 554 B | **Lines:** 26

**Imports:**
- `std::{
    sync::{Arc, Mutex},
    thread,
}`

**Declarations:**

`fn main()`

---

## userspace/src/bin/panic.rs

**Language:** Rust | **Size:** 168 B | **Lines:** 8

**Imports:**
- `common::ioctl::trigger_kernel_panic`

**Declarations:**

`fn main()`

---

## userspace/src/bin/pgid-child.rs

**Language:** Rust | **Size:** 1.3 KB | **Lines:** 37

**Declarations:**

`fn main()`

---

## userspace/src/bin/pgid-test.rs

**Language:** Rust | **Size:** 705 B | **Lines:** 27

**Declarations:**

`fn main()`

---

## userspace/src/bin/pread-test.rs

**Language:** Rust | **Size:** 1.1 KB | **Lines:** 42

**Imports:**
- `std::{io::Write, os::fd::AsRawFd}`

**Declarations:**

`fn main()`

---

## userspace/src/bin/prog1.rs

**Language:** Rust | **Size:** 48 B | **Lines:** 3

**Declarations:**

`fn main()`

---

## userspace/src/bin/prog2.rs

**Language:** Rust | **Size:** 73 B | **Lines:** 5

**Declarations:**

`fn main()`

---

## userspace/src/bin/rlimit-test.rs

**Language:** Rust | **Size:** 1.0 KB | **Lines:** 32

**Declarations:**

`fn main()`

---

## userspace/src/bin/rng_test.rs

**Language:** Rust | **Size:** 1.2 KB | **Lines:** 44

**Imports:**
- `std::io::Read`

**Declarations:**

`const AT_RANDOM: u64 = 25`

`fn main()`

---

## userspace/src/bin/sendfile-test.rs

**Language:** Rust | **Size:** 1.2 KB | **Lines:** 35

**Imports:**
- `std::{io::Write, os::fd::AsRawFd}`

**Declarations:**

`fn main()`

---

## userspace/src/bin/sigtest.rs

**Language:** Rust | **Size:** 2.5 KB | **Lines:** 86

**Imports:**
- `std::sync::atomic::{AtomicI32, Ordering}`

**Declarations:**

`static CAUGHT_SIGNAL: AtomicI32 = AtomicI32::new(0)`

`unsafe extern "C" fn handler(sig: i32)`

`fn main()`

`fn test_self_kill()`

`fn test_ignore()`

`fn test_wait_for_signal()`

`fn install_handler(sig: i32)`

`fn install_ignore(sig: i32)`

`unsafe fn libc_rt_sigaction(sig: i32, act: *const u64, oact: *mut u64, sigsetsize: usize) -> i64`

---

## userspace/src/bin/sleep.rs

**Language:** Rust | **Size:** 621 B | **Lines:** 24

**Imports:**
- `std::{env, process::exit, thread::sleep, time::Duration}`

**Declarations:**

`fn main()`

---

## userspace/src/bin/stress-process.rs

**Language:** Rust | **Size:** 959 B | **Lines:** 37

**Imports:**
- `std::{env, process::exit}`
- `userspace::spawn::spawn`

**Declarations:**

`const DEFAULT_INSTANCES: usize = 32`

`fn main()`

---

## userspace/src/bin/stress-thread.rs

**Language:** Rust | **Size:** 1.1 KB | **Lines:** 41

**Imports:**
- `std::{env, process::exit, thread, time::Duration}`

**Declarations:**

`const DEFAULT_INSTANCES: usize = 32`

`fn main()`

---

## userspace/src/bin/symlink-test.rs

**Language:** Rust | **Size:** 1.6 KB | **Lines:** 62

**Imports:**
- `std::io::Write`

**Declarations:**

`fn main()`

---

## userspace/src/bin/sysinfo_test.rs

**Language:** Rust | **Size:** 2.0 KB | **Lines:** 62

**Declarations:**

`fn cstr(buf: &[core::ffi::c_char]) -> &str`

`fn main()`

---

## userspace/src/bin/tcp_echo.rs

**Language:** Rust | **Size:** 612 B | **Lines:** 26

**Imports:**
- `std::{
    io::{Read, Write},
    net::TcpListener,
}`

**Declarations:**

`const PORT: u16 = 1234`

`fn main()`

---

## userspace/src/bin/thread-test.rs

**Language:** Rust | **Size:** 252 B | **Lines:** 11

**Imports:**
- `std::thread`

**Declarations:**

`fn main()`

---

## userspace/src/bin/udp.rs

**Language:** Rust | **Size:** 2.0 KB | **Lines:** 69

**Imports:**
- `std::{
    io::{Read, Write, stdout},
    net::{SocketAddr, UdpSocket},
    sync::{Arc, Mutex},
    thread,
}`

**Declarations:**

`const PORT: u16 = 1234`

`const DELETE: u8 = 127`

`fn main()`

---

## userspace/src/bin/vfs-test.rs

**Language:** Rust | **Size:** 954 B | **Lines:** 30

**Imports:**
- `std::io::Write`

**Declarations:**

`fn main()`

---

## userspace/src/bin/webserver.rs

**Language:** Rust | **Size:** 1.2 KB | **Lines:** 44

**Imports:**
- `std::{
    io::{Read, Write},
    net::{Shutdown, TcpListener},
    thread,
}`

**Declarations:**

`const PORT: u16 = 1234`

`const INDEX_HTML: &str = include_str!("../../static/index.html")`

`fn handle_connection(mut stream: std::net::TcpStream)`

`fn main()`

---

## userspace/src/lib.rs

**Language:** Rust | **Size:** 29 B | **Lines:** 2

**Declarations:**

---

## userspace/src/spawn.rs

**Language:** Rust | **Size:** 176 B | **Lines:** 5

**Imports:**
- `std::process::{Child, Command}`

**Declarations:**

---

## userspace/src/util.rs

**Language:** Rust | **Size:** 958 B | **Lines:** 40

**Imports:**
- `std::{
    io::{Read, Write, stdout},
    string::String,
}`

**Declarations:**

`const DELETE: u8 = 127`

---

## userspace/static/index.html

**Language:** HTML | **Size:** 2.9 KB | **Lines:** 81

**Declarations:**

