menu "Userspace programs"

config USERSPACE_DOOM
	bool "Include Doom in userspace image"
	default y
	help
	  Adds the compiled Doom binary to the userspace artifact set.
	  Only useful at runtime when FB is also enabled.

endmenu
