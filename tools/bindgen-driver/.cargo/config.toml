# Override the repo-root .cargo/config.toml's default target (which assumes
# bare-metal riscv64). bindgen-driver is a host tool.
[build]
target = "x86_64-unknown-linux-gnu"

[target.x86_64-unknown-linux-gnu]
linker = "gcc"
