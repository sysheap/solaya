[package]
name = "headers"
edition = "2024"
description.workspace = true
authors.workspace = true
version.workspace = true

# No bindgen dep: the actual bindgen invocation lives in tools/bindgen-driver
# and is driven by cmake/bindgen.cmake. build.rs is a thin shim that copies
# the generated files from <repo>/build/headers/generated into OUT_DIR.

[lib]
test = false
bench = false

[lints]
workspace = true
